---
phase: "5.2"
phase_slug: user-printer-access-policy
date: 2026-05-27
---

# Phase 5.2 — Validation Strategy

## Dimension 8 (Nyquist)

| Requirement | Validation type | Command / check |
|-------------|-----------------|-----------------|
| ACCESS-01 | API test | `pytest tests/test_user_printer_access_api.py` |
| ACCESS-02 | Manual UAT | UsersPage assign + default |
| ACCESS-03 | API test | GET `/printers/{id}/users` read-only |
| ACCESS-04, UX-05-02 | Manual UAT | PrintersPage picker + advanced id |
| ACCESS-05 | API + manual | `test_ti_export_api`, `test_policy_compliance`, Jobs badge |
| Fase 5 D-29 | Regression | `pytest tests/test_watcher*` + grep watcher sem UserPrinterAccess |
| P52-AC-05 | API test | user sem assignments → outside_policy false |

## Human UAT (post-execute)

1. Admin atribui 3 impressoras a usuário, 1 padrão — persiste após reload.
2. Cadastra impressora escolhendo fila detectada — sem label "CUPS" na UI principal.
3. Exporta roteiro TI CSV e imprime página.
4. Job de usuário cadastrado em impressora não atribuída → badge; usuário sem vínculos → sem badge.
5. Para backend durante print — captura continua (regressão D-29 capture).
