---
phase: 04-dashboard-web
plan: "05"
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
  - "04-04"
files_modified:
  - frontend/src/components/filters/DatePresetGroup.tsx
  - frontend/src/components/filters/PrinterCombobox.tsx
  - frontend/src/components/filters/FilterBar.tsx
  - frontend/src/components/jobs/JobsTable.tsx
  - frontend/src/components/jobs/JobsPagination.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Tabela mostra 7 colunas na ordem definida com paginação server-side"
    - "Filtros sempre visíveis: presets, datas, usuário, impressora, arquivo"
    - "Presets atualizam URL e refetch imediato"
    - "Busca por arquivo com debounce 300ms"
    - "Nunca carrega dataset completo no browser"
  artifacts:
    - path: "frontend/src/components/jobs/JobsTable.tsx"
      provides: "tabela sticky header zebra"
      contains: "formatMediaLabel"
    - path: "frontend/src/components/filters/FilterBar.tsx"
      provides: "barra filtros + Limpar filtros"
      contains: "DatePresetGroup"
    - path: "frontend/src/components/filters/PrinterCombobox.tsx"
      provides: "Headless UI combobox"
      contains: "Combobox"
  key_links:
    - from: "frontend/src/components/jobs/JobsTable.tsx"
      to: "frontend/src/hooks/useJobs.ts"
      via: "data.items map"
      pattern: "useJobs"
    - from: "frontend/src/components/filters/FilterBar.tsx"
      to: "frontend/src/hooks/useUrlFilters.ts"
      via: "setFilters / applyDatePreset"
      pattern: "useUrlFilters"
---

<objective>
Vertical slice principal: admin filtra e navega jobs paginados — tabela como foco visual do produto (D-48).

Purpose: Entrega DASH-03, DASH-04, DASH-05 e performance DASH-06 via paginação server-side exclusiva.

Output: Tabela funcional com filtros na URL; paginação mostra "Mostrando 1–50 de N jobs"; refetch com opacidade reduzida (D-49).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-web/04-UI-SPEC.md
@.planning/phases/04-dashboard-web/04-CONTEXT.md
@.planning/phases/04-dashboard-web/04-RESEARCH.md
@frontend/src/hooks/useUrlFilters.ts
@frontend/src/hooks/useJobs.ts
@frontend/src/lib/media.ts
@backend/app/schemas/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: FilterBar — presets, datas, usuário, impressora, search debounced</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-UI-SPEC.md — FilterBar, DatePresetGroup, Copywriting filtros
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-15..D-23, D-43, D-60
    - .planning/phases/04-dashboard-web/04-RESEARCH.md — Pattern 5 Headless Combobox
  </read_first>
  <files>
    frontend/src/components/filters/DatePresetGroup.tsx,
    frontend/src/components/filters/PrinterCombobox.tsx,
    frontend/src/components/filters/FilterBar.tsx
  </files>
  <action>
    `DatePresetGroup` (D-16, D-18): segmented pills — Hoje | Últimos 7 dias | Mês atual; active com `--accent-tint` + `--accent` (UI-SPEC). onClick chama `applyDatePreset` de useUrlFilters que usa lib/dates presets (D-42) e `replaceState` na URL.

    `DateRangeInputs` dentro FilterBar (D-17): dois `type="date"` labels De/Até; onChange atualiza `date_from`/`date_to` na URL.

    Campo **Usuário** (D-19): Input texto placeholder "Filtrar por usuário…"; bind direto URL `username` (contains API).

    `PrinterCombobox` (D-20, D-43): @headlessui/react Combobox; lista de `usePrinters()`; filtro client-side typeahead; valor enviado match **exato** param `printer`; clearable; placeholder "Selecionar impressora…". **Sem** free text (D-20).

    Campo **Arquivo** (D-21): estado local + `useDebouncedValue` 300ms (D-60) antes de `setFilters({ search })`.

    Botão **Limpar filtros** ghost (D-22): `clearFilters()` → page=1 size=50 sem outros params.

    FilterBar: fundo `--bg-surface`, sempre visível acima da tabela (D-15), padding md, gap 16px. Não usar modal/drawer.

    Documentar em comentário curto: preset "Mês atual" na tabela ≠ bucket `stats.mes` nos cards (RESEARCH Pitfall 2) — cards independentes da API stats.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - PrinterCombobox importa de @headlessui/react
    - FilterBar contém DatePresetGroup e botão Limpar filtros
    - Debounce 300ms no campo arquivo (grep 300 em useDebouncedValue usage)
  </acceptance_criteria>
  <done>Filtros completos sincronizados com URL.</done>
</task>

<task type="auto">
  <name>Task 2: JobsTable + JobsPagination + estados loading/empty</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-UI-SPEC.md — Table Specification
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-24..D-30, D-28, D-29, D-49..D-51, D-59
  </read_first>
  <files>
    frontend/src/components/jobs/JobsTable.tsx,
    frontend/src/components/jobs/JobsPagination.tsx,
    frontend/src/App.tsx
  </files>
  <action>
    `JobsTable` (D-24..D-30, UI-SPEC):
    - Colunas: Data/Hora, Usuário, Impressora, Arquivo, Páginas (right), Papel, Origem
    - `formatDateTime(timestamp)`; `formatMediaLabel(media)` (D-31); `host_origin` ou "—"
    - Username truncate + `title` tooltip (D-30)
    - `<table>` semântico `<th scope="col">` (a11y UI-SPEC)
    - sticky header; row min-h 40px (D-26); zebra `--row-stripe`; hover `--row-hover` (D-27)
    - **Sem** sort UI (D-29)

    `useJobs(filters)` de useUrlFilters (D-28, D-59): defaults page=1 size=50; **nunca** fetch all pages em loop.

    Loading (D-49): inicial ~8 row skeletons; refetch: opacity 0.6 + barra 2px indeterminada topo tabela.

    Empty (D-50): sem filtros → "Nenhum job registrado ainda"; com filtros → "Nenhum job encontrado" + body + CTA Limpar filtros.

    Erro: ErrorBanner refetch (D-51).

    `JobsPagination` (D-28): prev/next; meta "Mostrando {from}–{to} de {total} jobs" (UI-SPEC); `setPage` na URL. Opcional: seletor size 50/100 se couber sem complexidade (max 500 API).

    `App.tsx`: ordem SummaryCards → FilterBar → JobsTable → JobsPagination (D-06, D-48). Tabela área flex-1 min-h para ser foco visual.

    Proibir virtualização salvo profiling (D-61).
  </action>
  <verify>
    <automated>curl -sf "http://localhost/api/v1/jobs?page=1&size=50" | python3 -c "import json,sys; j=json.load(sys.stdin); assert len(j['items'])<=50"</automated>
  </verify>
  <acceptance_criteria>
    - JobsTable tem 7 colunas th
    - useJobs usa filters da URL
    - grep -r "filter.*items" frontend/src/components/jobs não faz client-side filter em dataset
  </acceptance_criteria>
  <done>Tabela paginada é o centro do dashboard; DASH-03/04/05/06 satisfeitos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job_name/username in cells | Untrusted display data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | T | XSS job_name | mitigate | React children only; escape automático |
| T-04-12 | D | DoS browser 50k rows | mitigate | Server pagination only; size max 500 API |
| T-04-13 | I | Printer list leak | accept | Admin tool; mesma rede |

</threat_model>

<verification>
- Filtro username na URL retorna subset na tabela
- Paginação page=2 muda items
- validate-phase4 jobs shape ainda passa
</verification>

<success_criteria>
- 7 colunas corretas
- Filtros na URL compartilháveis
- Paginação server-side only
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-web/04-05-SUMMARY.md` when done
</output>
