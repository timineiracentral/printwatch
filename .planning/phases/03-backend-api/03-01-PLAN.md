---
phase: 03-backend-api
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/db/session.py
  - backend/app/db/migrations.py
  - backend/app/watcher/status.py
  - backend/app/api/__init__.py
  - backend/app/api/v1/__init__.py
  - backend/app/main.py
  - docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - DASH-06

must_haves:
  truths:
    - "App FastAPI expõe /api/v1/docs (Swagger), /api/v1/openapi.json e mantém /healthz operacional"
    - "CORSMiddleware lê ALLOWED_ORIGINS (CSV) sem wildcard; allow_methods=['GET']; allow_credentials=False"
    - "Dependency get_db_dep retorna sessões SQLAlchemy via Depends() sem quebrar o contextmanager existente"
    - "Função ensure_indexes(engine) cria 4 índices em print_jobs idempotentemente no lifespan startup (antes de purge_old_jobs)"
    - "Porta 8000 do serviço backend exposta no host (docker-compose.yml ports: 8000:8000)"
    - "Watcher status encapsulado em app/watcher/status.py — endpoints não importam _observer global de main.py"
    - "docker compose up -d backend continua sobindo sem erro; /healthz retorna {\"status\":\"ok\",\"watcher\":true}"
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "Settings.allowed_origins (str CSV) e Settings.api_timezone (str)"
      exports: ["settings"]
    - path: "backend/app/db/session.py"
      provides: "get_db_dep() generator para Depends()"
      exports: ["engine", "SessionLocal", "get_db", "get_db_dep"]
    - path: "backend/app/db/migrations.py"
      provides: "ensure_indexes(engine) — CREATE INDEX IF NOT EXISTS para 4 índices"
      exports: ["ensure_indexes"]
    - path: "backend/app/watcher/status.py"
      provides: "register_observer(obs) / is_alive() / clear() — singleton interno"
      exports: ["register_observer", "is_alive", "clear"]
    - path: "backend/app/api/v1/__init__.py"
      provides: "api_v1_router = APIRouter() com include_router() de todos os submódulos (mesmo que vazios neste plano)"
      exports: ["api_v1_router"]
    - path: "backend/app/main.py"
      provides: "FastAPI com docs_url=/api/v1/docs, CORSMiddleware, include_router(api_v1_router, prefix=/api/v1), ensure_indexes no lifespan"
    - path: "docker-compose.yml"
      provides: "Serviço backend com ports: 8000:8000 e ALLOWED_ORIGINS no environment"
      contains: "8000:8000"
    - path: ".env.example"
      provides: "ALLOWED_ORIGINS=http://localhost:5173,http://VM_HOST e API_TIMEZONE=America/Sao_Paulo"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/api/v1/__init__.py"
      via: "app.include_router(api_v1_router, prefix='/api/v1')"
      pattern: "include_router.*api_v1_router.*prefix.*api/v1"
    - from: "backend/app/main.py"
      to: "backend/app/db/migrations.py"
      via: "ensure_indexes(engine) chamado no lifespan ANTES de purge_old_jobs"
      pattern: "ensure_indexes\\(engine\\)"
    - from: "backend/app/main.py"
      to: "backend/app/watcher/status.py"
      via: "register_observer(_observer) após observer.start()"
      pattern: "register_observer"
---

<objective>
Vertical slice 1 da Fase 3: "API existe, documentada e segura, com infra completa para os endpoints virem".

Habilita FastAPI 0.136 como API REST pública: Swagger (`/api/v1/docs`), CORS controlado por env, dependency injection de Session via `get_db_dep`, índices SQLite criados idempotentemente no lifespan, encapsulamento do watcher para health checks futuros, e exposição da porta 8000 no docker-compose. Cria o esqueleto do roteador `/api/v1/*` (vazio nesta etapa) que os planos 03-03, 04, 05 preencherão.

Purpose: Toda funcionalidade subsequente depende deste plano. Sem ele, os outros planos não têm onde plugar rotas, não têm Session via Depends, e não têm performance garantida por índices.

Output: `docker compose up -d backend` sobe; `curl http://localhost:8000/api/v1/openapi.json` retorna JSON com `info.title == "PrintWatch"`; `curl http://localhost:8000/api/v1/docs` retorna HTML Swagger; `curl http://localhost:8000/healthz` continua respondendo `{"status":"ok","watcher":true}`; índices `idx_print_jobs_timestamp`, `idx_print_jobs_username_timestamp`, `idx_print_jobs_printer_timestamp`, `idx_print_jobs_job_id` existem em `print_jobs`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-backend-api/03-CONTEXT.md
@.planning/phases/03-backend-api/03-RESEARCH.md
@.planning/phases/02-log-pipeline-data-layer/02-CONTEXT.md
@backend/app/main.py
@backend/app/core/config.py
@backend/app/db/session.py
@backend/app/db/models.py
@docker-compose.yml
@.env.example
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Estender configurações (allowed_origins, api_timezone), criar get_db_dep() e watcher/status.py</name>
  <read_first>
    - backend/app/core/config.py — padrão atual (os.environ.get + classe Settings + instância singleton)
    - backend/app/db/session.py — engine + SessionLocal + get_db() contextmanager existente
    - backend/app/main.py — referência _observer global (a ser encapsulada)
    - .planning/phases/03-backend-api/03-RESEARCH.md §1.3 (get_db_dep), §5.1 (watcher/status.py), §1.4 (CORS env)
    - .planning/phases/03-backend-api/03-CONTEXT.md D-10 (timezone), D-27 (CORS), D-31 (reutilizar repository)
  </read_first>
  <files>
    backend/app/core/config.py,
    backend/app/db/session.py,
    backend/app/watcher/status.py
  </files>
  <action>
    Estender `backend/app/core/config.py` adicionando dois campos à classe `Settings` (preservando os 3 existentes):
    - `allowed_origins: str = os.environ.get("ALLOWED_ORIGINS", "http://localhost:5173,http://VM_HOST")`
    - `api_timezone: str = os.environ.get("API_TIMEZONE", "America/Sao_Paulo")`

    Estender `backend/app/db/session.py` adicionando `get_db_dep()` ABAIXO do `get_db()` contextmanager existente. A função é uma generator simples (sem `@contextmanager`) com mesma assinatura `Generator[Session, None, None]`: cria `db = SessionLocal()`, `yield db`, `finally: db.close()`. Exportar via __all__ se houver, ou apenas defini-la em escopo de módulo (FastAPI Depends usa import direto). MANTER `get_db()` contextmanager intacto — é usado em scripts standalone. Não alterar engine, NullPool, ou create_all.

    Criar `backend/app/watcher/status.py` com singleton de módulo encapsulando o observer global. Funções públicas:
    - `register_observer(obs)` — armazena referência (atributo `_obs` no módulo)
    - `is_alive() -> bool` — retorna `_obs is not None and _obs.is_alive()`
    - `clear()` — define `_obs = None` (para shutdown e testes)

    A motivação é desacoplar endpoints do health check da global `_observer` em `main.py` (R-4 do RESEARCH.md): facilita testabilidade com mocks via `monkeypatch.setattr`.
  </action>
  <verify>
    <automated>
      docker compose exec backend python -c "from app.core.config import settings; print(settings.allowed_origins); print(settings.api_timezone)"
      # Esperado: http://localhost:5173,http://VM_HOST e America/Sao_Paulo

      docker compose exec backend python -c "from app.db.session import get_db_dep; g = get_db_dep(); s = next(g); print(type(s).__name__); g.close()"
      # Esperado: Session

      docker compose exec backend python -c "from app.watcher import status; status.clear(); print(status.is_alive())"
      # Esperado: False
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/app/core/config.py contém `allowed_origins: str` lendo `ALLOWED_ORIGINS`
    - backend/app/core/config.py contém `api_timezone: str` lendo `API_TIMEZONE` com default `"America/Sao_Paulo"`
    - backend/app/db/session.py contém função `def get_db_dep()` (sem decorador `@contextmanager`)
    - backend/app/db/session.py mantém `get_db` contextmanager intacto (grep `@contextmanager` deve continuar achando 1 ocorrência)
    - backend/app/watcher/status.py contém funções `register_observer`, `is_alive`, `clear`
    - backend/app/watcher/status.py NÃO importa de `app.main` (evita import circular)
    - `docker compose exec backend python -c "from app.db.session import get_db_dep; next(get_db_dep())"` sai 0
    - `docker compose exec backend python -c "from app.watcher.status import is_alive, register_observer, clear; clear(); assert is_alive() is False"` sai 0
  </acceptance_criteria>
  <done>Settings estendido com allowed_origins/api_timezone; get_db_dep disponível para Depends; watcher.status encapsula observer global sem import circular.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar app/db/migrations.py (ensure_indexes) e app/api/v1/__init__.py (router agregador vazio)</name>
  <read_first>
    - backend/app/db/models.py — schema atual de print_jobs (colunas referenciadas pelos índices)
    - backend/app/db/session.py — engine importável
    - .planning/phases/03-backend-api/03-RESEARCH.md §2.4 (ensure_indexes via text DDL), §8 (estrutura de pastas)
    - .planning/phases/03-backend-api/03-CONTEXT.md D-29 (4 índices, idempotentes via CREATE INDEX IF NOT EXISTS)
  </read_first>
  <files>
    backend/app/db/migrations.py,
    backend/app/api/__init__.py,
    backend/app/api/v1/__init__.py
  </files>
  <action>
    Criar `backend/app/db/migrations.py` com função `ensure_indexes(engine)` que executa 4 statements `CREATE INDEX IF NOT EXISTS` via `sqlalchemy.text()` em uma única transação (`with engine.begin() as conn`):
    - `CREATE INDEX IF NOT EXISTS idx_print_jobs_timestamp ON print_jobs(timestamp DESC)`
    - `CREATE INDEX IF NOT EXISTS idx_print_jobs_username_timestamp ON print_jobs(username, timestamp DESC)`
    - `CREATE INDEX IF NOT EXISTS idx_print_jobs_printer_timestamp ON print_jobs(printer, timestamp DESC)`
    - `CREATE INDEX IF NOT EXISTS idx_print_jobs_job_id ON print_jobs(job_id)`

    Logar via `logger = logging.getLogger(__name__)` no formato `"ensure_indexes: applied 4 index DDL statements"`. Idempotente — `CREATE INDEX IF NOT EXISTS` é no-op se já existir. **NÃO** criar índice funcional sobre `strftime(...)` (D-29 explícito).

    Criar diretório `backend/app/api/` com `__init__.py` VAZIO (apenas package marker).

    Criar `backend/app/api/v1/__init__.py` com:
    - `from fastapi import APIRouter`
    - `api_v1_router = APIRouter()`
    - Placeholder: NÃO importar submódulos (`jobs`, `stats`, etc.) ainda — esses serão criados nos planos 03, 04, 05. Comentário: `# Submódulos (jobs, stats, printers, export, health) são incluídos pelos planos 03-03/04/05/06.`
    - O router pode ser incluído em main.py com prefix `/api/v1` mesmo vazio — Swagger mostrará apenas as rotas registradas até o momento.
  </action>
  <verify>
    <automated>
      docker compose exec backend python -c "from app.db.migrations import ensure_indexes; from app.db.session import engine; ensure_indexes(engine); ensure_indexes(engine); print('idempotent ok')"
      docker compose exec backend sqlite3 /app/data/printwatch.db ".indices print_jobs" 2>/dev/null || docker compose exec backend python -c "from sqlalchemy import inspect; from app.db.session import engine; print([i['name'] for i in inspect(engine).get_indexes('print_jobs')])"
      # Esperado contém: idx_print_jobs_timestamp, idx_print_jobs_username_timestamp, idx_print_jobs_printer_timestamp, idx_print_jobs_job_id

      docker compose exec backend python -c "from app.api.v1 import api_v1_router; print(type(api_v1_router).__name__)"
      # Esperado: APIRouter
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/app/db/migrations.py contém `def ensure_indexes(engine)` e 4 statements `CREATE INDEX IF NOT EXISTS`
    - backend/app/db/migrations.py contém `with engine.begin() as conn:` (transação atômica)
    - backend/app/db/migrations.py NÃO contém `strftime` (sem índice funcional — D-29)
    - backend/app/api/__init__.py existe (pode ser vazio)
    - backend/app/api/v1/__init__.py contém `api_v1_router = APIRouter()`
    - `inspect(engine).get_indexes('print_jobs')` retorna lista contendo todos os 4 índices nomeados após executar ensure_indexes
    - Executar ensure_indexes 2x consecutivos sai 0 nas duas (idempotência)
    - `from app.api.v1 import api_v1_router` sem erro
  </acceptance_criteria>
  <done>ensure_indexes() cria 4 índices idempotentes; api_v1_router está pronto para receber include_router de submódulos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Reescrever main.py (CORS, docs_url, include_router, ensure_indexes no lifespan, register_observer)</name>
  <read_first>
    - backend/app/main.py — estado atual (docs_url=None, sem CORS, sem include_router, _observer global)
    - backend/app/db/migrations.py — função ensure_indexes recém criada (Task 2)
    - backend/app/watcher/status.py — register_observer/clear (Task 1)
    - backend/app/api/v1/__init__.py — api_v1_router (Task 2)
    - backend/app/core/config.py — settings.allowed_origins (Task 1)
    - .planning/phases/03-backend-api/03-RESEARCH.md §1.1 (include_router), §1.2 (Swagger), §1.4 (CORS), §2.4 (ensure_indexes no lifespan ANTES de purge), §5.1 (encapsular observer)
    - .planning/phases/03-backend-api/03-CONTEXT.md D-24, D-25, D-26, D-27, D-29
  </read_first>
  <files>
    backend/app/main.py
  </files>
  <action>
    Reescrever `backend/app/main.py` mantendo a estrutura atual (lifespan + healthz) e adicionando:

    1. **Imports adicionais:**
       - `from fastapi.middleware.cors import CORSMiddleware`
       - `from sqlalchemy import text`
       - `from app.db.session import engine` (não havia)
       - `from app.db.migrations import ensure_indexes`
       - `from app.watcher import status as watcher_status`
       - `from app.api.v1 import api_v1_router`

    2. **Ordem do lifespan startup (D-29 exige índices ANTES de purge):**
       a. `ensure_indexes(engine)` — primeiro, idempotente
       b. `purge_old_jobs(session, ...)` — depois, lógica existente preservada
       c. Iniciar observer (lógica existente)
       d. `watcher_status.register_observer(_observer)` — após `_observer.start()`

    3. **Lifespan shutdown:**
       a. `_observer.stop()` e `join` (existente)
       b. `watcher_status.clear()` — limpar singleton

    4. **FastAPI app:**
       ```python
       app = FastAPI(
           title="PrintWatch",
           version="1.0.0",
           docs_url="/api/v1/docs",
           redoc_url=None,
           openapi_url="/api/v1/openapi.json",
           lifespan=lifespan,
       )
       ```
       (Supersede D-12 Fase 2: agora docs_url ativo.)

    5. **CORSMiddleware:**
       ```python
       origins = [o.strip() for o in settings.allowed_origins.split(",") if o.strip()]
       app.add_middleware(
           CORSMiddleware,
           allow_origins=origins,
           allow_credentials=False,
           allow_methods=["GET"],
           allow_headers=["*"],
       )
       ```
       (D-27: sem wildcard.)

    6. **include_router:**
       `app.include_router(api_v1_router, prefix="/api/v1")` — uma única linha, antes ou depois do `/healthz`.

    7. **Endpoint `/healthz` (mantido sem mudança):** lê do singleton `watcher_status.is_alive()` em vez da global `_observer` (refator: a global continua existindo para o lifespan, mas o handler do healthz consulta o singleton agora):
       ```python
       @app.get("/healthz")
       def healthz() -> dict[str, str | bool]:
           return {"status": "ok", "watcher": watcher_status.is_alive()}
       ```

    Manter `logging.basicConfig(level=logging.INFO)`. Manter `from app.core.config import settings`.
  </action>
  <verify>
    <automated>
      docker compose up -d backend
      sleep 3
      docker compose logs backend --tail 30 | grep -i "watcher started"
      curl -fsS http://localhost:8000/healthz | python -c "import json,sys; j=json.load(sys.stdin); assert j['status']=='ok' and j['watcher']==True, j; print('healthz ok')"
      curl -fsS http://localhost:8000/api/v1/openapi.json | python -c "import json,sys; j=json.load(sys.stdin); assert j['info']['title']=='PrintWatch', j['info']; print('openapi ok')"
      curl -fsS -o /tmp/docs.html http://localhost:8000/api/v1/docs && grep -q "Swagger" /tmp/docs.html && echo "swagger ok"
      docker compose exec backend python -c "from sqlalchemy import inspect; from app.db.session import engine; idx=[i['name'] for i in inspect(engine).get_indexes('print_jobs')]; assert 'idx_print_jobs_timestamp' in idx and 'idx_print_jobs_username_timestamp' in idx and 'idx_print_jobs_printer_timestamp' in idx and 'idx_print_jobs_job_id' in idx, idx; print('indexes ok')"
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/app/main.py contém `docs_url="/api/v1/docs"`
    - backend/app/main.py contém `openapi_url="/api/v1/openapi.json"`
    - backend/app/main.py contém `CORSMiddleware` com `allow_origins=origins` (variável) e `allow_credentials=False` e `allow_methods=["GET"]`
    - backend/app/main.py contém `app.include_router(api_v1_router, prefix="/api/v1")`
    - backend/app/main.py contém `ensure_indexes(engine)` chamado ANTES de `purge_old_jobs` no lifespan
    - backend/app/main.py contém `watcher_status.register_observer(_observer)` após `_observer.start()`
    - backend/app/main.py contém `watcher_status.clear()` no shutdown (após stop/join)
    - backend/app/main.py NÃO contém `docs_url=None`
    - backend/app/main.py endpoint `/healthz` usa `watcher_status.is_alive()` (não `_observer.is_alive()` direto)
    - `curl http://localhost:8000/api/v1/openapi.json` retorna 200 + JSON com `info.title == "PrintWatch"`
    - `curl http://localhost:8000/api/v1/docs` retorna 200 + HTML contendo "Swagger"
    - `curl http://localhost:8000/healthz` retorna 200 + `{"status":"ok","watcher":true}`
    - `inspect(engine).get_indexes('print_jobs')` contém os 4 índices nomeados
    - Container CUPS continua operando independentemente: `docker compose ps cups` mostra "running" mesmo após restart do backend (CAPTURE-04 preservado)
  </acceptance_criteria>
  <done>App FastAPI sobe com Swagger habilitado, CORS configurado, índices SQLite criados via lifespan, watcher encapsulado em singleton; healthz continua respondendo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Expor porta 8000 no docker-compose.yml e adicionar ALLOWED_ORIGINS/API_TIMEZONE em .env.example</name>
  <read_first>
    - docker-compose.yml — serviço `backend` atual (sem ports:)
    - .env.example — seção "Fase 2 — backend" para anexar a "Fase 3 — backend API"
    - .planning/phases/03-backend-api/03-RESEARCH.md §5.2 (expor porta 8000), §1.4 (ALLOWED_ORIGINS)
    - .planning/phases/03-backend-api/03-CONTEXT.md D-27 (CORS sem wildcard, default `http://localhost:5173,http://VM_HOST`)
  </read_first>
  <files>
    docker-compose.yml,
    .env.example
  </files>
  <action>
    Modificar `docker-compose.yml`:
    - No serviço `backend`, adicionar bloco `ports:` com `- "8000:8000"` ANTES de `volumes:`.
    - No bloco `environment:` adicionar `ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:5173,http://VM_HOST}` e `API_TIMEZONE: ${API_TIMEZONE:-America/Sao_Paulo}` (após `LOG_RETENTION_DAYS`).
    - Manter todo o resto (env_file, volumes ro/db_data, restart, depends_on).
    - Atualizar comentário do topo: `# PrintWatch — Docker Compose (Fase 1: CUPS; Fase 2: backend persistence; Fase 3: backend API)`.

    Modificar `.env.example`:
    - Anexar (no final do arquivo, após `LOG_RETENTION_DAYS=90`) bloco:
      ```
      # Fase 3 — backend API
      # CORS: lista de origens permitidas separadas por vírgula (D-27)
      # NUNCA usar "*" — rede REDACTED_IP/16 é o controle de acesso, mas browsers exigem origem explícita
      ALLOWED_ORIGINS=http://localhost:5173,http://VM_HOST
      # Timezone para serialização da API e interpretação de date_from/date_to (D-10, D-20)
      # Banco persiste em UTC; conversão é apenas na borda HTTP
      API_TIMEZONE=America/Sao_Paulo
      ```
  </action>
  <verify>
    <automated>
      grep -E "^\s+ports:" docker-compose.yml
      grep -E "8000:8000" docker-compose.yml
      grep -E "ALLOWED_ORIGINS" docker-compose.yml
      grep -E "API_TIMEZONE" docker-compose.yml
      grep -E "^ALLOWED_ORIGINS=" .env.example
      grep -E "^API_TIMEZONE=" .env.example
      docker compose up -d backend
      sleep 3
      curl -fsS http://localhost:8000/healthz
      docker compose exec backend env | grep -E "ALLOWED_ORIGINS|API_TIMEZONE"
    </automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml serviço `backend` contém bloco `ports:` com entrada `"8000:8000"`
    - docker-compose.yml serviço `backend` contém `ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:5173,http://VM_HOST}` em environment
    - docker-compose.yml serviço `backend` contém `API_TIMEZONE: ${API_TIMEZONE:-America/Sao_Paulo}` em environment
    - .env.example contém linha `ALLOWED_ORIGINS=http://localhost:5173,http://VM_HOST`
    - .env.example contém linha `API_TIMEZONE=America/Sao_Paulo`
    - .env.example contém comentário `# Fase 3 — backend API`
    - `curl http://localhost:8000/healthz` retorna 200 de fora do container (porta 8000 host)
    - `docker compose exec backend env | grep ALLOWED_ORIGINS` retorna a string default ou o valor do .env do usuário
  </acceptance_criteria>
  <done>Porta 8000 exposta no host; ALLOWED_ORIGINS e API_TIMEZONE em compose + .env.example; backend acessível externamente em http://localhost:8000.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rede REDACTED_IP/16 → backend:8000 | Primeira exposição de porta HTTP do backend; até a Fase 2 só CUPS estava exposto. |
| Browser (rede local) → backend:8000 | CORS controla origens cruzadas; mesmo MVP sem auth, browsers exigem origem explícita. |
| docker host → SQLite (volume db_data) | DDL `CREATE INDEX` em volume com permissões 600 — operação no startup, sem entrada externa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | CORS allow_origins="*" acidental | mitigate | D-27 explícito: ALLOWED_ORIGINS é lista de strings via env; sem fallback wildcard. Test em validate-phase3 #15 confirma origem inválida bloqueada. |
| T-03-01-02 | Tampering | Migration de índices roda sobre dado existente (volume db_data) | mitigate | Apenas `CREATE INDEX IF NOT EXISTS` — sem ALTER/DROP/UPDATE. Idempotente. Volume permissões 600 herdadas da Fase 2 não mudam. |
| T-03-01-03 | Denial of Service | API exposta na porta 8000 sem rate limiting | accept | MVP rede local REDACTED_IP/16; firewall externo (PROJECT.md). Rate limiting é v2. |
| T-03-01-04 | Information Disclosure | Swagger UI expõe rotas internas a qualquer cliente da rede local | accept | Decisão D-26 explícita. Rede REDACTED_IP/16 é o controle de acesso (D-28). Documentado em CONTEXT. |
| T-03-01-05 | Spoofing | Sem autenticação — qualquer cliente da rede pode chamar API | accept | MVP scope decision (PROJECT.md Out of Scope). Auth em v2. |
| T-03-01-06 | Elevation of Privilege | `_observer` exposto via watcher.status pode ser manipulado por código malicioso | accept | Singleton de processo; sem entrada externa. Risco interno apenas. |
</threat_model>

<verification>
Após execução deste plano:

1. `docker compose up -d backend` — sobe sem erro em < 10s
2. `curl -fsS http://localhost:8000/healthz | jq .status` retorna `"ok"` e `.watcher == true`
3. `curl -fsS http://localhost:8000/api/v1/openapi.json | jq .info.title` retorna `"PrintWatch"`
4. `curl -I http://localhost:8000/api/v1/docs` retorna `200 OK` com `content-type: text/html`
5. `docker compose exec backend python -c "from sqlalchemy import inspect; from app.db.session import engine; idx={i['name'] for i in inspect(engine).get_indexes('print_jobs')}; assert {'idx_print_jobs_timestamp','idx_print_jobs_username_timestamp','idx_print_jobs_printer_timestamp','idx_print_jobs_job_id'} <= idx"` sai 0
6. `docker compose restart backend && sleep 3 && curl -fsS http://localhost:8000/healthz` mostra que ensure_indexes é idempotente em restarts
7. CUPS continua sem dependência do backend: `docker compose stop backend && docker compose ps cups` mostra running (CAPTURE-04 preservado)
8. Volume db_data continua com permissões 600: `docker compose exec backend stat /app/data/printwatch.db | grep -E "0600|rw-------"`
</verification>

<success_criteria>
- App FastAPI expõe `/api/v1/docs`, `/api/v1/openapi.json`, `/api/v1/*` (router agregador), e `/healthz` em `http://localhost:8000`
- 4 índices SQLite criados idempotentemente no startup (Wave 2 depende deles para DASH-06)
- CORSMiddleware ativo com lista de origens via env (sem wildcard)
- Container backend acessível na porta 8000 do host (curl externo funciona)
- Watcher encapsulado em singleton (`app.watcher.status`) — endpoints subsequentes podem importar `is_alive` sem tocar em `_observer` global
- Fase 2 funcionando: page_log → DB → `/healthz` continua reportando watcher alive
</success_criteria>

<output>
Criar `.planning/phases/03-backend-api/03-01-SUMMARY.md` quando concluído.
</output>
