---
phase: 04-dashboard-web
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/api/client.ts
  - frontend/src/api/jobs.ts
  - frontend/src/api/stats.ts
  - frontend/src/api/printers.ts
  - frontend/src/api/export.ts
  - frontend/src/hooks/useUrlFilters.ts
  - frontend/src/hooks/useDebouncedValue.ts
  - frontend/src/hooks/useJobs.ts
  - frontend/src/hooks/useStatsSummary.ts
  - frontend/src/hooks/usePrinters.ts
  - frontend/src/lib/format.ts
autonomous: true
requirements:
  - DASH-04
  - DASH-05
  - DASH-06

must_haves:
  truths:
    - "Hooks Query buscam /api/v1/jobs com filtros da URL"
    - "stats/summary cacheado staleTime 60s sem recalcular tops no cliente"
    - "Mudança de filtro na URL dispara refetch jobs"
    - "search debounced 300ms antes de escrever na URL"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "JobOut, JobFilters, Page, StatsSummaryResponse"
      contains: "StatsSummaryResponse"
    - path: "frontend/src/api/client.ts"
      provides: "getJson base /api/v1"
      contains: "VITE_API_BASE_URL"
    - path: "frontend/src/hooks/useUrlFilters.ts"
      provides: "URL ↔ filtros sem React Router"
      contains: "replaceState"
    - path: "frontend/src/hooks/useJobs.ts"
      provides: "placeholderData keepPreviousData"
      contains: "keepPreviousData"
  key_links:
    - from: "frontend/src/hooks/useJobs.ts"
      to: "frontend/src/api/jobs.ts"
      via: "fetchJobs(filters)"
      pattern: "fetchJobs"
    - from: "frontend/src/hooks/useUrlFilters.ts"
      to: "frontend/src/lib/filters.ts"
      via: "parseFiltersFromUrl / filtersToSearchParams"
      pattern: "filtersToSearchParams"
---

<objective>
Camada de dados: tipos espelhando Pydantic, cliente HTTP, módulos API, hooks TanStack Query e sincronização URL ↔ filtros (sem React Router).

Purpose: Todos os componentes de UI (plans 03–06) consomem hooks prontos — evita scavenger hunt nos schemas backend.

Output: DevTools Network mostra chamadas paralelas a `/api/v1/stats/summary` e `/api/v1/jobs?page=1&size=50` ao montar App de teste; alterar `?username=x` na URL refetch jobs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-web/04-CONTEXT.md
@.planning/phases/04-dashboard-web/04-RESEARCH.md
@backend/app/schemas/jobs.py
@backend/app/schemas/stats.py
@backend/app/schemas/common.py
@backend/app/api/v1/jobs.py
@backend/app/api/v1/stats.py
@backend/app/api/v1/printers.py
@backend/app/api/v1/export.py
@frontend/src/lib/filters.ts
@frontend/src/lib/dates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tipos TypeScript + api/client + módulos REST</name>
  <read_first>
    - backend/app/schemas/jobs.py — JobOut, JobFilters
    - backend/app/schemas/stats.py — StatsSummaryResponse, TopEntry
    - backend/app/schemas/common.py — Page[T]
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-40, D-65
  </read_first>
  <files>
    frontend/src/types/api.ts,
    frontend/src/api/client.ts,
    frontend/src/api/jobs.ts,
    frontend/src/api/stats.ts,
    frontend/src/api/printers.ts,
    frontend/src/api/export.ts
  </files>
  <action>
    `types/api.ts`: interfaces espelhando Pydantic — `JobOut`, `JobFilters`, `Page<T>`, `TopEntry`, `StatsBucket`, `StatsSummaryResponse` (D-65). Campos opcionais onde API permite null.

    `api/client.ts` (D-40):
    - `const baseUrl = import.meta.env.VITE_API_BASE_URL ?? '/api/v1'`
    - `getJson<T>(path, params?)` com `Accept: application/json`
    - classe `ApiError` com status + detail

    `api/jobs.ts`: `fetchJobs(filters: JobFilters): Promise<Page<JobOut>>` → GET `/jobs` com `filtersToSearchParams`.

    `api/stats.ts`: `fetchStatsSummary(top?: number)` → GET `/stats/summary` (default top=5 da API).

    `api/printers.ts`: `fetchPrinters(): Promise<string[]>`.

    `api/export.ts` (D-35): `downloadCsv(filters: Omit<JobFilters,'page'|'size'>)` — fetch `/export/csv`, blob + anchor download; tratar 400 com `ExportCapError` e mensagem backend (D-37). **Não** enviar page/size.

    Sem segunda camada repository (D-66).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - types/api.ts exporta JobOut e StatsSummaryResponse
    - api/export.ts não referencia page nem size nos params
    - build TypeScript sem erros
  </acceptance_criteria>
  <done>Contratos API tipados; funções fetch prontas para hooks.</done>
</task>

<task type="auto">
  <name>Task 2: useUrlFilters + debounce + hooks Query</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-RESEARCH.md — Pattern 2, Pattern 3
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-23, D-39, D-44, D-59, D-60, D-13
  </read_first>
  <files>
    frontend/src/hooks/useUrlFilters.ts,
    frontend/src/hooks/useDebouncedValue.ts,
    frontend/src/hooks/useJobs.ts,
    frontend/src/hooks/useStatsSummary.ts,
    frontend/src/hooks/usePrinters.ts,
    frontend/src/lib/format.ts,
    frontend/src/hooks/useDebouncedValue.test.ts,
    frontend/src/App.tsx
  </files>
  <action>
    `useUrlFilters` (D-23, D-44): sincronizar `window.location.search` via `useSyncExternalStore` + `history.replaceState` (sem react-router). API: `filters`, `setFilters(partial)`, `setPage`, `clearFilters()` → defaults D-22, `applyDatePreset(name)`.

    `useDebouncedValue<T>(value, 300)` para campo search local (D-60, D-21): só após 300ms chamar `setFilters({ search })` na URL.

    `useJobs(filters)` (D-39, D-59): `queryKey: ['jobs', filters]`, `queryFn: () => fetchJobs(filters)`, `placeholderData: keepPreviousData` (paginação sem flash).

    `useStatsSummary()` (D-13): `staleTime: 60_000`; **não** derivar tops dos jobs da tabela.

    `usePrinters()` (D-20): `queryKey: ['printers']`, staleTime longo (~5min).

    `lib/format.ts`: `formatNumberPtBr(n)`, `formatTopLabel(name, pages)` → "Nome — 1.284 páginas" (D-12); `formatDateTime(iso)` → `dd/MM/yyyy HH:mm` via date-fns parseISO **sem** segundo shift timezone (RESEARCH Pitfall 4 — timestamp já SP).

    `useDebouncedValue.test.ts`: Vitest com `vi.useFakeTimers()` — valor só atualiza após 300ms (D-60, D-21); avançar timers e assert.

    App.tsx temporário: renderizar JSON.stringify de jobs.total + stats.hoje.jobs para smoke test manual (removido no plan 06).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run src/hooks/useDebouncedValue.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - useJobs importa keepPreviousData de @tanstack/react-query
    - useStatsSummary staleTime >= 30000
    - useUrlFilters não importa react-router-dom
    - grep -r "zustand\|redux" frontend/src/hooks retorna vazio
  </acceptance_criteria>
  <done>Estado servidor e URL funcionais; smoke via App temporário opcional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/v1 | Query params controlados pelo usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T | Query params malformados | mitigate | Repassar apenas chaves JobFilters; API retorna 422 |
| T-04-05 | D | Reflected XSS via URL | mitigate | Não renderizar search raw como HTML; React text nodes |
| T-04-06 | I | export fetch sem auth | accept | Mesmo boundary rede local que API |

</threat_model>

<verification>
- `cd frontend && npm run build` OK
- Com backend+nginx up: hooks disparam requests visíveis no Network tab
</verification>

<success_criteria>
- Tipos alinhados aos schemas Python
- Hooks Query + URL state prontos para UI
- Debounce 300ms implementado
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-web/04-02-SUMMARY.md` when done
</output>
