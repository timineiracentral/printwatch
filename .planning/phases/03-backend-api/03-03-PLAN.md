---
phase: 03-backend-api
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/app/schemas/__init__.py
  - backend/app/schemas/common.py
  - backend/app/schemas/jobs.py
  - backend/app/schemas/health.py
  - backend/app/services/jobs_service.py
  - backend/app/api/v1/jobs.py
  - backend/app/api/v1/printers.py
  - backend/app/api/v1/health.py
  - backend/app/api/v1/__init__.py
  - backend/tests/test_api_jobs.py
  - backend/tests/test_api_printers.py
  - backend/tests/test_api_health.py
  - backend/tests/conftest.py
autonomous: true
requirements:
  - DASH-06

must_haves:
  truths:
    - "GET /api/v1/jobs retorna {items, total, page, size} com agregação por job (D-04: chave printer + job_id + username + job_name + strftime minuto)"
    - "Filtros username (LIKE case-insensitive), printer (exact), search (LIKE em job_name), date_from/date_to (inclusivos, interpretados em America/Sao_Paulo) funcionam combinados com AND"
    - "Paginação: page>=1, 1<=size<=500, defaults page=1 size=50, valores fora do range retornam HTTP 422"
    - "Ordem default: timestamp DESC (jobs mais recentes primeiro)"
    - "Pages COUNT(*) por grupo agregado corresponde ao número real de linhas page_log do job"
    - "GET /api/v1/jobs/{id} retorna 200 com o job agregado contendo o id ou 404 se não encontrado"
    - "GET /api/v1/printers retorna lista plana de strings ordenadas alfabeticamente, fonte = DISTINCT printer FROM print_jobs (D-21: SEM consultar CUPS)"
    - "GET /api/v1/health retorna {status, db_reachable, watcher_alive}; HTTP 503 só se db_reachable=False; HTTP 200 com status='degraded' se apenas watcher_alive=False"
    - "Datetimes retornados nas respostas são strings ISO em America/Sao_Paulo (D-10) — banco continua persistindo em UTC"
  artifacts:
    - path: "backend/app/schemas/common.py"
      provides: "Page[T] genérico (TypeVar) com items, total, page, size"
      exports: ["Page"]
    - path: "backend/app/schemas/jobs.py"
      provides: "JobOut (response model), JobFilters (Query model com validação)"
      exports: ["JobOut", "JobFilters"]
    - path: "backend/app/services/jobs_service.py"
      provides: "list_jobs(db, filters)->tuple[list[dict], int]; get_job_by_id(db, id)->dict|None; list_printer_names(db)->list[str]; aggregated_query builder reutilizável"
      exports: ["list_jobs", "get_job_by_id", "list_printer_names"]
    - path: "backend/app/api/v1/jobs.py"
      provides: "router com GET '' (lista paginada) e GET '/{id}' (detalhe)"
      exports: ["router"]
    - path: "backend/app/api/v1/printers.py"
      provides: "router com GET '' (lista de strings)"
      exports: ["router"]
    - path: "backend/app/api/v1/health.py"
      provides: "router com GET '' (health check estendido)"
      exports: ["router"]
    - path: "backend/tests/conftest.py"
      provides: "fixtures client (TestClient), db_session (sqlite memory), seed_jobs (dados de teste)"
  key_links:
    - from: "backend/app/api/v1/jobs.py"
      to: "backend/app/services/jobs_service.py"
      via: "router endpoint chama list_jobs(db, filters)"
      pattern: "list_jobs\\(db.*filters\\)"
    - from: "backend/app/api/v1/__init__.py"
      to: "backend/app/api/v1/jobs.py"
      via: "include_router(jobs.router, prefix='/jobs', tags=['jobs'])"
      pattern: "include_router.*jobs\\.router.*/jobs"
    - from: "backend/app/api/v1/health.py"
      to: "backend/app/watcher/status.py"
      via: "watcher_status.is_alive() (sem import de _observer global)"
      pattern: "watcher_status\\.is_alive"
---

<objective>
Vertical slice "endpoints de leitura prontos para o frontend": `/api/v1/jobs`, `/api/v1/jobs/{id}`, `/api/v1/printers`, `/api/v1/health`.

Implementa a primeira parte do contrato HTTP do backend: listagem agregada por job com filtros + paginação (D-01–D-10), detalhe de um job, listagem informacional de impressoras (D-21), e health check estendido (D-25). Endpoint CSV (`/api/v1/export/csv`) é tema do Plano 03-05 — depende deste plano para reutilizar `jobs_service`.

Purpose: Frontend (Fase 4) consumirá estes endpoints. DASH-06 (dashboard < 2s para 50k registros) é validado pela combinação destes endpoints + índices SQLite criados no Plano 03-01.

Output: 4 endpoints funcionando em `http://localhost:8000/api/v1/*`, testáveis via `curl` e `pytest`. Swagger em `/api/v1/docs` lista todos eles com schemas. Cobertura de testes inclui: agregação correta, filtros, paginação, datetime em São Paulo, health degraded.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-backend-api/03-CONTEXT.md
@.planning/phases/03-backend-api/03-RESEARCH.md
@backend/app/main.py
@backend/app/db/models.py
@backend/app/db/session.py
@backend/app/services/retention.py
@backend/tests/conftest.py
@backend/tests/test_retention.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pydantic schemas (Page[T] genérico, JobOut, JobFilters, HealthResponse)</name>
  <read_first>
    - backend/app/db/models.py — campos da tabela print_jobs (PrintJob)
    - .planning/phases/03-backend-api/03-RESEARCH.md §1.5 (Generic Page), §1.6 (JobFilters com validação), §5.1 (HealthResponse)
    - .planning/phases/03-backend-api/03-CONTEXT.md D-08 (paginação), D-09 (filtros), D-10 (timezone serialização), D-25 (health)
  </read_first>
  <files>
    backend/app/schemas/__init__.py,
    backend/app/schemas/common.py,
    backend/app/schemas/jobs.py,
    backend/app/schemas/health.py
  </files>
  <action>
    Criar `backend/app/schemas/__init__.py` vazio.

    Criar `backend/app/schemas/common.py` com classe genérica para paginação:
    ```python
    from typing import Generic, TypeVar
    from pydantic import BaseModel
    T = TypeVar("T")
    class Page(BaseModel, Generic[T]):
        items: list[T]
        total: int
        page: int
        size: int
    ```

    Criar `backend/app/schemas/jobs.py` com dois modelos:

    1. `JobOut(BaseModel)` — response model do job agregado:
       - `id: Optional[int] = None` (representa o **primeiro** PrintJob.id do grupo; usado em `GET /jobs/{id}`)
       - `printer: str`
       - `username: str`
       - `job_id: int`
       - `job_name: Optional[str] = None`
       - `timestamp: datetime` (será serializado em ISO America/Sao_Paulo via field_serializer)
       - `pages: int` (COUNT(*) do grupo — D-03)
       - `color_mode: Optional[str] = None`
       - `host_origin: Optional[str] = None`
       - `media: Optional[str] = None`
       - `sides: Optional[str] = None`
       - `model_config = ConfigDict(from_attributes=False)` (mapeamos dict explicitamente)
       - `@field_serializer("timestamp")` que converte UTC → America/Sao_Paulo e retorna `isoformat()` (D-10). Se o datetime entrar naive, assumir UTC.

    2. `JobFilters(BaseModel)` — model_config `{"extra": "forbid"}` para rejeitar query params desconhecidos:
       - `page: int = Field(1, ge=1)`
       - `size: int = Field(50, ge=1, le=500)` (D-08)
       - `username: Optional[str] = None`
       - `printer: Optional[str] = None`
       - `search: Optional[str] = None`
       - `date_from: Optional[date] = None`
       - `date_to: Optional[date] = None`
       - `@model_validator(mode="after")` rejeita `date_from > date_to` com ValueError.

    Criar `backend/app/schemas/health.py`:
    ```python
    from typing import Literal
    from pydantic import BaseModel
    class HealthResponse(BaseModel):
        status: Literal["ok", "degraded", "down"]
        db_reachable: bool
        watcher_alive: bool
    ```
  </action>
  <verify>
    <automated>
      docker compose exec backend python -c "
      from app.schemas.common import Page
      from app.schemas.jobs import JobOut, JobFilters
      from app.schemas.health import HealthResponse
      from datetime import datetime, timezone, date
      # Page genérico
      p = Page[JobOut](items=[], total=0, page=1, size=50)
      assert p.total == 0
      # JobFilters validators
      try:
          JobFilters(page=0)
          assert False
      except Exception: pass
      try:
          JobFilters(size=600)
          assert False
      except Exception: pass
      try:
          JobFilters(date_from=date(2025,1,2), date_to=date(2025,1,1))
          assert False
      except Exception: pass
      # JobOut timestamp serialization (UTC → America/Sao_Paulo)
      j = JobOut(printer='p', username='u', job_id=1, timestamp=datetime(2026,5,26,20,0,tzinfo=timezone.utc), pages=1)
      dumped = j.model_dump(mode='json')
      assert '17:00' in dumped['timestamp'], dumped  # 20 UTC = 17 BRT
      print('schemas ok')
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/app/schemas/common.py contém `class Page(BaseModel, Generic[T])` com `items`, `total`, `page`, `size`
    - backend/app/schemas/jobs.py contém `class JobOut` com campos listados + `@field_serializer("timestamp")` aplicando timezone America/Sao_Paulo
    - backend/app/schemas/jobs.py contém `class JobFilters` com `model_config = ConfigDict(extra="forbid")` e `@model_validator` rejeitando date_from > date_to
    - backend/app/schemas/jobs.py validação `size > 500` levanta ValidationError
    - backend/app/schemas/jobs.py validação `page < 1` levanta ValidationError
    - backend/app/schemas/health.py contém `class HealthResponse` com `status: Literal["ok","degraded","down"]`
    - `Page[JobOut](items=[], total=0, page=1, size=50)` instancia sem erro
    - JobOut com timestamp UTC 20:00 serializa para `"...17:00..."` (BRT)
  </acceptance_criteria>
  <done>Schemas Pydantic prontos para serem usados como response_model e Query() pelos endpoints.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Service jobs_service — query agregada reutilizável, list_jobs, get_job_by_id, list_printer_names</name>
  <read_first>
    - backend/app/db/models.py — PrintJob
    - backend/app/services/retention.py — modelo de service simples com Session + SQLAlchemy
    - .planning/phases/03-backend-api/03-RESEARCH.md §2.1 (GROUP BY com strftime), §2.2 (count), §2.3 (filtros), §6.2 (top reutiliza subquery), §7 (printers)
    - .planning/phases/03-backend-api/03-CONTEXT.md D-04, D-05, D-06, D-09, D-10, D-19 (granularity preparada), D-21
  </read_first>
  <files>
    backend/app/services/jobs_service.py
  </files>
  <action>
    Criar `backend/app/services/jobs_service.py` com:

    1. Imports: `from datetime import datetime, time, timezone, date`; `from zoneinfo import ZoneInfo`; `from sqlalchemy import select, func, and_`; `from sqlalchemy.orm import Session`; `from app.core.config import settings`; `from app.db.models import PrintJob`; `from app.schemas.jobs import JobFilters`.

    2. Constante module-level `_TZ = ZoneInfo(settings.api_timezone)`.

    3. Função privada `_local_date_to_utc_range(d: date) -> tuple[datetime, datetime]`:
       - Retorna `(start_utc, end_utc)` onde `start_utc` é `00:00` local convertido para UTC e `end_utc` é `23:59:59.999999` local convertido para UTC.

    4. Função privada `_build_aggregated_subquery(filters: JobFilters):`
       - Constrói `select(...)` com colunas conforme RESEARCH §2.1: `printer`, `username`, `job_id`, `job_name`, `func.min(timestamp)` AS timestamp, `func.count()` AS pages, `func.max(color_mode/host_origin/media/sides)`.
       - Aplica filtros via `.where(...)` ANTES do GROUP BY (D-09):
         - `username` → `func.lower(PrintJob.username).contains(filters.username.lower())`
         - `printer` → `PrintJob.printer == filters.printer` (exact)
         - `search` → `func.lower(PrintJob.job_name).contains(filters.search.lower())`
         - `date_from` → `PrintJob.timestamp >= _local_date_to_utc_range(date_from)[0]`
         - `date_to` → `PrintJob.timestamp <= _local_date_to_utc_range(date_to)[1]`
       - GROUP BY: `printer`, `username`, `job_id`, `job_name`, `strftime('%Y-%m-%d %H:%M', timestamp)`.
       - Retorna a query buildada (sem `.subquery()` — chamadora decide).

    5. Função pública `list_jobs(db: Session, filters: JobFilters) -> tuple[list[dict], int]`:
       - `agg = _build_aggregated_subquery(filters).subquery()`
       - `total = db.execute(select(func.count()).select_from(agg)).scalar_one()`
       - `stmt = _build_aggregated_subquery(filters).order_by(func.min(PrintJob.timestamp).desc()).limit(filters.size).offset((filters.page - 1) * filters.size)` (D-07).
       - `rows = db.execute(stmt).mappings().all()`
       - Cada `row` é convertido para `dict` (`dict(row)`) e o id é populado via consulta auxiliar opcional (ver nota abaixo) ou deixado como `None` (a UI usa `job_id` para detalhe, e `GET /jobs/{id}` resolve pelo PrintJob.id real). **Decisão pragmática**: `id` no JobOut do listing fica `None`; só `GET /jobs/{id}` traz id real. Justificativa: aggregate group não tem id natural.

       Retornar `(items, total)`.

    6. Função pública `get_job_by_id(db: Session, job_db_id: int) -> dict | None`:
       - Busca o `PrintJob` pelo id natural: `row = db.execute(select(PrintJob).where(PrintJob.id == job_db_id)).scalar_one_or_none()`.
       - Se None: retorna None.
       - Senão: usa a chave `(printer, job_id, username, job_name, strftime(timestamp))` para construir filtros pontuais e agregar o job inteiro:
         - Reaplica o agregado mas com `WHERE printer = row.printer AND job_id = row.job_id AND username = row.username AND COALESCE(job_name,'') = COALESCE(:jn,'') AND strftime('%Y-%m-%d %H:%M', timestamp) = strftime('%Y-%m-%d %H:%M', :ts)`.
         - Pega a primeira (e única) linha agregada.
         - Inclui `id = row.id` no dict de retorno (referenciando o id natural do registro original).

    7. Função pública `list_printer_names(db: Session) -> list[str]`:
       - `stmt = select(PrintJob.printer).distinct().order_by(PrintJob.printer.asc())`
       - Retorna `[r[0] for r in db.execute(stmt)]` (filtra Nones se necessário).

    Adicionar logging `logger = logging.getLogger(__name__)` opcional para queries lentas (NÃO logar conteúdo dos jobs para evitar PII).
  </action>
  <verify>
    <automated>
      docker compose exec backend python -c "
      from app.services.jobs_service import _build_aggregated_subquery, list_printer_names
      from app.schemas.jobs import JobFilters
      from app.db.session import SessionLocal
      s = SessionLocal()
      # Smoke test do builder
      q = _build_aggregated_subquery(JobFilters())
      print('build ok')
      # Lista de printers (mesmo vazia ou com test_printer)
      names = list_printer_names(s)
      print('printers:', names)
      s.close()
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/jobs_service.py contém função `list_jobs(db, filters)` retornando `tuple[list[dict], int]`
    - backend/app/services/jobs_service.py contém função `get_job_by_id(db, id)` retornando `dict | None`
    - backend/app/services/jobs_service.py contém função `list_printer_names(db)` retornando `list[str]`
    - backend/app/services/jobs_service.py contém `func.strftime("%Y-%m-%d %H:%M", PrintJob.timestamp)` no GROUP BY (D-05)
    - backend/app/services/jobs_service.py contém `func.count()` (D-03) e `func.min(PrintJob.timestamp)` (D-06)
    - backend/app/services/jobs_service.py contém `func.lower(PrintJob.username).contains(` ou equivalente case-insensitive
    - backend/app/services/jobs_service.py contém `PrintJob.printer == filters.printer` (exact, NÃO LIKE para printer)
    - backend/app/services/jobs_service.py NÃO importa nada de `app.api.*` (services não dependem de routes)
    - Smoke test `_build_aggregated_subquery(JobFilters())` sai 0 (compila SQL sem executar)
  </acceptance_criteria>
  <done>Service com agregação/listagem/get_by_id/printers implementado; pronto para ser plugado em rotas.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Routers FastAPI — jobs.py, printers.py, health.py + agregador v1 atualizado</name>
  <read_first>
    - backend/app/main.py — `_observer` global e watcher_status (Plano 01)
    - backend/app/api/v1/__init__.py — api_v1_router placeholder (Plano 01)
    - backend/app/services/jobs_service.py — Task 2 recém criada
    - backend/app/schemas/* — Task 1
    - .planning/phases/03-backend-api/03-RESEARCH.md §1.1 (estrutura), §1.3 (Depends get_db_dep), §1.6 (Query model), §5.1 (health 503 só se DB)
    - .planning/phases/03-backend-api/03-CONTEXT.md D-25 (health), D-21 (printers — sem CUPS)
  </read_first>
  <files>
    backend/app/api/v1/jobs.py,
    backend/app/api/v1/printers.py,
    backend/app/api/v1/health.py,
    backend/app/api/v1/__init__.py
  </files>
  <action>
    Criar `backend/app/api/v1/jobs.py`:
    ```python
    from typing import Annotated
    from fastapi import APIRouter, Depends, HTTPException, Query
    from sqlalchemy.orm import Session

    from app.db.session import get_db_dep
    from app.schemas.common import Page
    from app.schemas.jobs import JobOut, JobFilters
    from app.services import jobs_service

    router = APIRouter()

    @router.get("", response_model=Page[JobOut])
    def list_jobs_endpoint(
        filters: Annotated[JobFilters, Query()],
        db: Session = Depends(get_db_dep),
    ):
        items, total = jobs_service.list_jobs(db, filters)
        return Page[JobOut](items=items, total=total, page=filters.page, size=filters.size)

    @router.get("/{job_id}", response_model=JobOut)
    def get_job_endpoint(
        job_id: int,
        db: Session = Depends(get_db_dep),
    ):
        row = jobs_service.get_job_by_id(db, job_id)
        if row is None:
            raise HTTPException(status_code=404, detail="job not found")
        return row
    ```
    Nota: `job_id` no path é o id natural do PrintJob (row.id), não o `job_id` do CUPS. Ambíguo nominalmente — mantemos pragmaticamente para alinhar com Fase 4 que terá link "ver detalhe".

    Criar `backend/app/api/v1/printers.py`:
    ```python
    from fastapi import APIRouter, Depends
    from sqlalchemy.orm import Session
    from app.db.session import get_db_dep
    from app.services import jobs_service

    router = APIRouter()

    @router.get("", response_model=list[str])
    def list_printers_endpoint(db: Session = Depends(get_db_dep)):
        return jobs_service.list_printer_names(db)
    ```

    Criar `backend/app/api/v1/health.py`:
    ```python
    from fastapi import APIRouter, Depends
    from fastapi.responses import JSONResponse
    from sqlalchemy import text
    from sqlalchemy.orm import Session

    from app.db.session import get_db_dep
    from app.schemas.health import HealthResponse
    from app.watcher import status as watcher_status

    router = APIRouter()

    @router.get("", response_model=HealthResponse)
    def health_endpoint(db: Session = Depends(get_db_dep)):
        try:
            db.execute(text("SELECT 1"))
            db_ok = True
        except Exception:
            db_ok = False

        watcher_ok = watcher_status.is_alive()

        if not db_ok:
            return JSONResponse(
                {"status": "down", "db_reachable": False, "watcher_alive": watcher_ok},
                status_code=503,
            )
        status_str = "ok" if watcher_ok else "degraded"
        return HealthResponse(status=status_str, db_reachable=True, watcher_alive=watcher_ok)
    ```

    Atualizar `backend/app/api/v1/__init__.py` para incluir os 3 routers + comentário sobre stats/export (planos 04/05):
    ```python
    from fastapi import APIRouter
    from app.api.v1 import jobs, printers, health
    # stats e export são adicionados pelos planos 03-04 e 03-05.

    api_v1_router = APIRouter()
    api_v1_router.include_router(jobs.router, prefix="/jobs", tags=["jobs"])
    api_v1_router.include_router(printers.router, prefix="/printers", tags=["printers"])
    api_v1_router.include_router(health.router, prefix="/health", tags=["health"])
    ```
  </action>
  <verify>
    <automated>
      docker compose up -d backend
      sleep 3
      curl -fsS 'http://localhost:8000/api/v1/jobs?page=1&size=10' | python -c "import json,sys; j=json.load(sys.stdin); assert {'items','total','page','size'} <= set(j.keys()), j; print('list ok')"
      curl -fsS 'http://localhost:8000/api/v1/jobs?size=999' -o /tmp/x.json -w '%{http_code}\n' | tee /tmp/code.txt; grep -q "422" /tmp/code.txt
      curl -fsS 'http://localhost:8000/api/v1/printers' | python -c "import json,sys; j=json.load(sys.stdin); assert isinstance(j, list); print('printers ok:', j)"
      curl -fsS 'http://localhost:8000/api/v1/health' | python -c "import json,sys; j=json.load(sys.stdin); assert {'status','db_reachable','watcher_alive'} <= set(j.keys()); print('health ok:', j)"
      curl -fsS 'http://localhost:8000/api/v1/openapi.json' | python -c "import json,sys; j=json.load(sys.stdin); paths=set(j['paths'].keys()); assert '/api/v1/jobs' in paths and '/api/v1/jobs/{job_id}' in paths and '/api/v1/printers' in paths and '/api/v1/health' in paths, paths; print('openapi ok')"
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/app/api/v1/jobs.py contém `@router.get("", response_model=Page[JobOut])` e `@router.get("/{job_id}", response_model=JobOut)`
    - backend/app/api/v1/jobs.py usa `Depends(get_db_dep)` (não importa `get_db` contextmanager)
    - backend/app/api/v1/printers.py contém `response_model=list[str]`
    - backend/app/api/v1/health.py retorna `JSONResponse(..., status_code=503)` quando db_ok=False
    - backend/app/api/v1/__init__.py contém `include_router(jobs.router, prefix="/jobs", tags=["jobs"])` e similares para printers/health
    - `curl http://localhost:8000/api/v1/jobs` retorna 200 + JSON com chaves `items, total, page, size`
    - `curl 'http://localhost:8000/api/v1/jobs?size=999'` retorna 422
    - `curl http://localhost:8000/api/v1/printers` retorna 200 + lista JSON
    - `curl http://localhost:8000/api/v1/health` retorna 200 + chaves `status, db_reachable, watcher_alive`
    - OpenAPI inclui os 4 paths (`/api/v1/jobs`, `/api/v1/jobs/{job_id}`, `/api/v1/printers`, `/api/v1/health`)
  </acceptance_criteria>
  <done>3 routers ativos, agregador atualizado, Swagger reflete endpoints, smoke tests via curl passam.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Atualizar conftest.py — fixtures client (TestClient) + db_session (memória) + seed_jobs</name>
  <read_first>
    - backend/tests/conftest.py — fixtures existentes
    - backend/tests/test_retention.py — padrão de uso de Session em testes
    - .planning/phases/03-backend-api/03-RESEARCH.md §9 (padrão TestClient + DB isolado)
  </read_first>
  <files>
    backend/tests/conftest.py
  </files>
  <action>
    Estender (não substituir) `backend/tests/conftest.py` adicionando fixtures novas, preservando as existentes da Fase 2:

    1. `engine_memory` (session-scope): `create_engine("sqlite:///:memory:", connect_args={"check_same_thread": False}, poolclass=StaticPool)` + `Base.metadata.create_all(engine)`.

    2. `db_session` (function-scope): cria SessionLocal a partir do engine_memory, `yield session`, `session.close()`.

    3. `seed_jobs` (function-scope, depende de db_session): popula DB com 3 jobs de teste:
       - Job A: 3 page-rows mesmo (printer="alpha", job_id=100, username="usr1", job_name="A.pdf", timestamp 2026-05-26 10:00:00 UTC, 10:00:30 UTC, 10:00:45 UTC) → após agregação: 1 job, pages=3
       - Job B: 1 page-row (printer="beta", job_id=200, username="usr2", job_name="B.pdf", timestamp 2026-05-25 14:00:00 UTC) → 1 job, pages=1
       - Job C: 2 page-rows (printer="alpha", job_id=300, username="USR3", job_name="C.pdf", timestamp 2026-05-26 11:30:00 UTC + 11:30:20 UTC) → 1 job, pages=2
       Retorna lista das instâncias inseridas para referência.

    4. `client` (function-scope, depende de db_session): cria `TestClient(app)` com `app.dependency_overrides[get_db_dep] = lambda: db_session` (yield) — depois `app.dependency_overrides.clear()`.

    Os testes podem combinar `seed_jobs + client` para validar end-to-end.
  </action>
  <verify>
    <automated>
      docker compose exec backend pytest backend/tests/test_retention.py -q
      # Esperado: testes Fase 2 continuam passando (sem regressão por causa das novas fixtures)
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/conftest.py contém fixture `engine_memory` usando `StaticPool` e `sqlite:///:memory:`
    - backend/tests/conftest.py contém fixture `db_session`
    - backend/tests/conftest.py contém fixture `seed_jobs` populando ≥3 page-rows agrupáveis em 3 jobs distintos
    - backend/tests/conftest.py contém fixture `client` usando `TestClient(app)` + `dependency_overrides`
    - Fixtures existentes da Fase 2 (test_retention, test_parser) continuam funcionando — `pytest backend/tests/` sem erro
  </acceptance_criteria>
  <done>Fixtures FastAPI prontas; testes da Fase 2 sem regressão.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Suite de testes — test_api_jobs.py (filtros + paginação + agregação + 404 + 422)</name>
  <read_first>
    - backend/tests/conftest.py — Task 4 (fixtures client, seed_jobs)
    - backend/app/api/v1/jobs.py — Task 3
    - .planning/phases/03-backend-api/03-CONTEXT.md D-04 (agregação), D-09 (filtros)
  </read_first>
  <files>
    backend/tests/test_api_jobs.py
  </files>
  <action>
    Criar `backend/tests/test_api_jobs.py` com casos cobrindo:

    - `test_list_jobs_empty_returns_page_schema(client)` — DB vazio → 200, `{items: [], total: 0, page: 1, size: 50}`.
    - `test_list_jobs_aggregates_by_job(client, seed_jobs)` — total agregado = 3 jobs (A, B, C); job A tem `pages == 3`.
    - `test_list_jobs_filter_username_case_insensitive(client, seed_jobs)` — `?username=USR1` retorna job A; `?username=usr1` mesmo resultado.
    - `test_list_jobs_filter_printer_exact_match(client, seed_jobs)` — `?printer=alpha` retorna jobs A e C; `?printer=Alpha` retorna [].
    - `test_list_jobs_filter_search_in_job_name(client, seed_jobs)` — `?search=B.pdf` retorna apenas job B.
    - `test_list_jobs_filter_date_range_local_inclusive(client, seed_jobs)` — `?date_from=2026-05-26&date_to=2026-05-26` retorna jobs A e C (do dia local SP); job B (2026-05-25 UTC, mas em SP é 11:00 do dia 25 — confirmar).
    - `test_list_jobs_pagination_size_limit(client)` — `?size=999` retorna 422.
    - `test_list_jobs_pagination_page_zero(client)` — `?page=0` retorna 422.
    - `test_list_jobs_invalid_date_range(client)` — `?date_from=2026-05-27&date_to=2026-05-26` retorna 422.
    - `test_list_jobs_extra_query_forbidden(client)` — `?unknown=1` retorna 422 (extra="forbid").
    - `test_list_jobs_order_timestamp_desc(client, seed_jobs)` — primeiro item tem timestamp maior que o último.
    - `test_get_job_by_id_returns_aggregated(client, seed_jobs)` — pegar o id natural de uma page-row do job A → GET /api/v1/jobs/{id} → 200 com `pages == 3`.
    - `test_get_job_by_id_not_found(client)` — GET /api/v1/jobs/999999 → 404.
    - `test_list_jobs_timestamp_in_sao_paulo(client, seed_jobs)` — `items[0]["timestamp"]` é string ISO contendo `-03:00` ou indicando offset São Paulo.

    Cada teste deve fazer assertions concretas (status_code + chaves específicas), não apenas "não falhou".
  </action>
  <verify>
    <automated>
      docker compose exec backend pytest backend/tests/test_api_jobs.py -v
      # Esperado: ≥ 13 passed
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_api_jobs.py contém ao menos 13 funções de teste cobrindo os casos listados
    - backend/tests/test_api_jobs.py contém asserções específicas para agregação (`pages == 3`)
    - backend/tests/test_api_jobs.py contém asserção de timezone (string `-03:00` ou similar)
    - `pytest backend/tests/test_api_jobs.py` → todos passing
  </acceptance_criteria>
  <done>Endpoint jobs+detalhe cobertos por suite de testes que valida agregação, filtros, paginação, validação, timezone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Suite de testes — test_api_printers.py + test_api_health.py</name>
  <read_first>
    - backend/tests/test_api_jobs.py — Task 5 (padrão de testes)
    - backend/app/api/v1/printers.py, health.py — Task 3
  </read_first>
  <files>
    backend/tests/test_api_printers.py,
    backend/tests/test_api_health.py
  </files>
  <action>
    Criar `backend/tests/test_api_printers.py`:
    - `test_list_printers_empty(client)` — DB vazio → 200, `[]`.
    - `test_list_printers_distinct_sorted(client, seed_jobs)` — retorna `["alpha", "beta"]` (ordenado alfabético, sem duplicatas).
    - `test_list_printers_does_not_call_cups(client, seed_jobs, monkeypatch)` — patch `subprocess.run` para raise se chamado; endpoint deve completar sem invocar nada externo. (D-21: SEM acoplamento com CUPS.)

    Criar `backend/tests/test_api_health.py`:
    - `test_health_ok_when_watcher_alive(client, monkeypatch)` — `monkeypatch.setattr("app.watcher.status._obs", FakeObserver(alive=True))` ou via `register_observer(FakeObserver(alive=True))`. GET /api/v1/health → 200, status="ok", db_reachable=True, watcher_alive=True.
    - `test_health_degraded_when_watcher_down(client, monkeypatch)` — `watcher_status.clear()` → 200, status="degraded", db_reachable=True, watcher_alive=False.
    - `test_health_503_when_db_unreachable(client, monkeypatch)` — patch `db.execute` para raise SQLAlchemy error → 503, status="down".
    - `test_healthz_continues_to_work(client)` — GET /healthz → 200 (Fase 2 endpoint preservado).
  </action>
  <verify>
    <automated>
      docker compose exec backend pytest backend/tests/test_api_printers.py backend/tests/test_api_health.py -v
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_api_printers.py contém 3 testes (empty, distinct_sorted, does_not_call_cups)
    - backend/tests/test_api_health.py contém 4 testes (ok, degraded, 503, healthz preservado)
    - test_does_not_call_cups usa monkeypatch para asseverar que subprocess/sockets NÃO são chamados
    - test_health_503_when_db_unreachable asserta `response.status_code == 503` e `response.json()["status"] == "down"`
    - `pytest backend/tests/test_api_printers.py backend/tests/test_api_health.py` → todos passing
  </acceptance_criteria>
  <done>Endpoints printers + health cobertos; comportamento de degraded/down provado por testes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente HTTP → /api/v1/jobs?username= | Filtros aceitam strings arbitrárias; SQLAlchemy parametriza (sem SQL injection). |
| Cliente HTTP → /api/v1/jobs/{job_id} | ID é integer (FastAPI valida tipo); IDs inexistentes → 404, não 500. |
| DB → API response | Datetimes timezone-naive em SQLite assumidos UTC; serializados como BRT — risco de confusão de timezone no consumidor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | Mensagens de erro 422 podem expor schema interno | accept | Pydantic ValidationError é detalhado mas não revela código sensível. Comportamento default da FastAPI. |
| T-03-03-02 | Tampering | Query params com strings que tentam SQL injection | mitigate | SQLAlchemy 2.0 sempre parametriza; nenhum f-string em raw SQL. |
| T-03-03-03 | Denial of Service | Filtro username com `%` no LIKE pode fazer full table scan | accept | Mitigado por índice `username, timestamp` + filtros normalmente combinados com date_from/date_to. Para 50k registros, < 500ms (DASH-06). |
| T-03-03-04 | Information Disclosure | response_model=list[str] em /printers expõe nomes de impressoras | accept | Decisão D-21 — informação pública dentro da rede REDACTED_IP/16. |
| T-03-03-05 | Spoofing | /api/v1/health pode ser sondado por terceiros para descobrir o sistema | accept | Banner padrão FastAPI em /docs já revela. MVP sem auth. |
</threat_model>

<verification>
Após execução deste plano:

1. `pytest backend/tests/test_api_jobs.py backend/tests/test_api_printers.py backend/tests/test_api_health.py` → todos passing (≥ 20 testes)
2. `curl http://localhost:8000/api/v1/jobs?page=1&size=10` retorna 200 com `{items, total, page, size}`
3. `curl 'http://localhost:8000/api/v1/jobs?username=user.example'` filtra corretamente
4. `curl http://localhost:8000/api/v1/printers` retorna lista ordenada
5. `curl http://localhost:8000/api/v1/health` retorna `{"status":"ok"|"degraded","db_reachable":true,...}`
6. `curl http://localhost:8000/api/v1/jobs/999999` retorna 404
7. `curl 'http://localhost:8000/api/v1/jobs?size=999'` retorna 422
8. Tempo de resposta para `/api/v1/jobs?size=50` em DB com 50k registros: < 500ms (validado pelo Plano 03-06 Nyquist)
</verification>

<success_criteria>
- 4 endpoints HTTP funcionando: GET /api/v1/jobs, GET /api/v1/jobs/{id}, GET /api/v1/printers, GET /api/v1/health
- Agregação por job correta (3 page-rows mesmo job → 1 item com pages=3)
- Filtros funcionam combinados (username + printer + search + date_range)
- Paginação valida ranges (page≥1, 1≤size≤500)
- Timestamps serializados em America/Sao_Paulo (D-10)
- /printers não invoca CUPS (D-21)
- /health retorna 503 só se DB inacessível (D-25)
- Suite pytest ≥ 20 testes cobrindo happy + edge cases
- DASH-06 (parcial — endpoints rápidos; índices garantem performance no Plano 01)
</success_criteria>

<output>
Criar `.planning/phases/03-backend-api/03-03-SUMMARY.md` quando concluído.
</output>
