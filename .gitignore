# Secrets — nunca versionar credenciais (D-19)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.test
.env.production
*.pem
*.key

# OS / editor
.DS_Store
Thumbs.db
*.swp
*~

# Python (Fase 2+)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Node (Fase 4+)
node_modules/
dist/
build/

# Docker local overrides
docker-compose.override.yml

# Runtime / logs locais
*.log
logs/

# SQLite local (usar volume Docker em produção)
*.db
*.sqlite
*.sqlite3

# IDE (config local, não compartilhada)
.idea/
.vscode/settings.json

# Chaves SSH e certificados TLS — nunca versionar
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.ppk
*.crt
*.p12

# git filter-repo replacements — contém padrões com dados reais
replacements.txt

# GSD / planning — apenas local, nunca versionar
.planning/

# Exports CSV temporários (downloads locais)
exports/

# Scripts de smoke test temporários (execução pontual, não para versionamento)
backend/scripts/smoke_*.sh
backend/scripts/smoke_*.py

.cursor/

.docs/