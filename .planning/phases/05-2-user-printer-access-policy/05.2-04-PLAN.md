---
phase: 05.2-user-printer-access-policy
plan: "04"
type: execute
wave: 3
depends_on:
  - "05.2-02"
files_modified:
  - frontend/src/api/settings/users.ts
  - frontend/src/hooks/useUserPrinterAccess.ts
  - frontend/src/pages/settings/UsersPage.tsx
  - frontend/src/pages/settings/PrintersPage.tsx
  - frontend/src/components/settings/UnmappedQueuesBanner.tsx
  - frontend/src/types/api.ts
autonomous: true
requirements:
  - ACCESS-02
  - ACCESS-03
  - ACCESS-04
  - UX-05-02

must_haves:
  truths:
    - "UsersPage edit dialog: multi-select impressoras + marcar padrão + salvar via PUT printer-access"
    - "PrintersPage: usuários com acesso read-only; sem edição de N:N na impressora"
    - "Create printer: picker fila detectada (unmapped); label Identificador no servidor só em seção avançada"
    - "Tabela impressoras sem jargão CUPS na coluna principal (Fila no servidor / Fila detectada)"
    - "UnmappedQueuesBanner usa terminologia D-25"
    - "D-06: PrintersPage lista usuários read-only; D-09–D-11 selector sem cap (D-11 uso recente MVP opcional)"
    - "D-23–D-24: só unmapped-queues; sem discovered-queues/lpstat"
    - "P52-AC-02 verificável: picker unmapped + save printer"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-52-04a | Dialog estreito com muitas impressoras | low | max-w-2xl + scroll; aviso >10 |
| T-52-04b | Editar assignments em create user sem id | low | Seção só após user criado (redirect edit ou segundo passo) |
</threat_model>

<objective>
Settings UI: ficha usuário como fonte única de escrita; impressora com picker operacional e vista invertida read-only.
</objective>

<context>
@.planning/phases/05-2-user-printer-access-policy/05.2-CONTEXT.md (D-05–D-12, D-23–D-27)
@.planning/phases/05-master-data-organization/05-UI-SPEC.md
@frontend/src/pages/settings/UsersPage.tsx
@frontend/src/pages/settings/PrintersPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client + hook printer-access</name>
  <read_first>
    - frontend/src/api/settings/users.ts
    - frontend/src/hooks/useUsers.ts
    - frontend/src/api/settings/printers.ts
  </read_first>
  <action>
    Em types/api.ts adicionar PrinterAccessItem, PrinterAccessRead, PrinterAccessReplace.
    Em api/settings/users.ts:
    - getUserPrinterAccess(userId)
    - putUserPrinterAccess(userId, body)
    - getPrinterUsers(printerId) para vista invertida
    Hook useUserPrinterAccess(userId) com useQuery + useMutation (invalidate ['users', id, 'printer-access']).
  </action>
  <acceptance_criteria>
    - Types compilam (`cd frontend && npm run build` exit 0)
    - Hook exporta data, isLoading, save mutation
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: UsersPage impressoras permitidas</name>
  <read_first>
    - frontend/src/pages/settings/UsersPage.tsx
    - frontend/src/components/filters/PrinterCombobox.tsx (padrão combobox)
    - frontend/src/hooks/usePrintersRegistry.ts
  </read_first>
  <action>
    No dialog de edição (quando editing != null):
    - Seção "Impressoras permitidas" com multi-select pesquisável de impressoras ativas (usePrintersRegistry list).
    - Agrupar options por department/location se disponível no PrinterRead.
    - Ordenar: impressoras já selecionadas primeiro; depois alfabético (MVP; uso recente via jobs = follow-up se API helper existir).
    - Controle default: radio ou botão "Definir como padrão" por item selecionado (único).
    - Botão "Salvar impressoras" chama putUserPrinterAccess com assignments derivados do state local.
    - Aviso não bloqueante se selected.length > 10 (D-12).
    - Expandir Dialog para max-w-2xl e max-h scroll interno.
    Create flow: após create bem-sucedido, opcionalmente manter dialog aberto em modo edit para assignments OU mensagem "Salve o usuário e configure impressoras em editar" — preferir auto-switch para edit após create.
  </action>
  <acceptance_criteria>
    - Editar user, assign 2 printers, 1 default, reload page → selections persistem
    - Nenhum input de printer-access no dialog create sem user id
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: PrintersPage picker + read-only users + labels</name>
  <read_first>
    - frontend/src/pages/settings/PrintersPage.tsx
    - frontend/src/components/settings/UnmappedQueuesBanner.tsx
    - .planning/phases/05-2-user-printer-access-policy/05.2-CONTEXT.md (D-25, D-26, UX-05-02)
  </read_first>
  <action>
    PrintersPage create/edit:
    - Campo principal: Select "Fila detectada" populado por unmapped.data (obrigatório no create se houver filas; permitir vazio se lista vazia com input manual avançado).
    - Seção colapsável `<details>` "Avançado" com Input label "Identificador no servidor" bound to cups_queue_name (sync com select: ao escolher fila, preencher cups_queue_name).
    - Tabela header "Fila CUPS" → "Fila no servidor".
    Edit dialog: painel read-only listando usuários (GET /printers/{id}/users) com link `navigate(/settings/users)` e hint "Edite na ficha do usuário".
    UnmappedQueuesBanner: textos "Fila não cadastrada", CTA "Cadastrar fila detectada".
  </action>
  <acceptance_criteria>
    - UI visível sem string "CUPS" em labels primários (grep PrintersPage.tsx labels — permitido em tooltip/aria técnico)
    - openCreate(queue) pré-seleciona fila detectada
    - Printer detail mostra lista de usuários sem botão salvar assignments
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run build`
- [ ] Manual: criar impressora via fila detectada; assign user; ver lista invertida
</verification>
