---
phase: 05.2-user-printer-access-policy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/*_user_printer_access.py
  - backend/app/db/models.py
  - backend/app/schemas/user_printer_access.py
  - backend/tests/test_user_printer_access_model.py
autonomous: true
requirements:
  - ACCESS-01

must_haves:
  truths:
    - "D-01–D-04: N:N permissivo; is_default único; 1:1 fila registry; sem enforcement spooler"
    - "D-32–D-34: policy falha não afeta watcher; watcher sem import UserPrinterAccess; zero vínculos OK"
    - "Tabela user_printer_access existe com FK users/printers, is_active, is_default, timestamps"
    - "UniqueConstraint (user_id, printer_id) e índices em user_id e printer_id"
    - "Modelo UserPrinterAccess não importado pelo watcher (grep watcher vazio)"
    - "Alembic upgrade head aplica migration; downgrade -1 remove tabela"
    - "Invariante Fase 5 D-29 preservado: zero mudança em print_jobs schema ou watcher"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-52-01a | FK órfã se printer/user deletado | low | ON DELETE RESTRICT ou soft-delete only (master data já soft-delete) |
| T-52-01b | Migration quebra deploy | medium | Testar upgrade/downgrade local; revision encadeada correta |
</threat_model>

<objective>
Migration Alembic + modelo ORM + schemas Pydantic base para `user_printer_access` (fundação da fase).
</objective>

<context>
@.planning/phases/05-2-user-printer-access-policy/05.2-CONTEXT.md
@.planning/phases/05-2-user-printer-access-policy/05.2-RESEARCH.md
@backend/alembic/versions/085a2d5c5767_master_data_tables.py
@backend/app/db/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration user_printer_access</name>
  <read_first>
    - backend/alembic/versions/085a2d5c5767_master_data_tables.py
    - backend/alembic/env.py
    - .planning/phases/05-2-user-printer-access-policy/05.2-RESEARCH.md (Schema section)
  </read_first>
  <action>
    Gerar revision `alembic revision -m "user_printer_access"` com `down_revision = "085a2d5c5767"`.
    `upgrade()`: create_table `user_printer_access` com colunas id (PK autoincrement), user_id (FK users.id), printer_id (FK printers.id), is_active BOOLEAN NOT NULL default 1, is_default BOOLEAN NOT NULL default 0, created_at/updated_at DateTime NOT NULL.
    UniqueConstraint `uq_user_printer` em (user_id, printer_id).
    create_index `ix_user_printer_access_user_id` e `ix_user_printer_access_printer_id`.
  </action>
  <acceptance_criteria>
    - Arquivo em backend/alembic/versions/ com revision encadeada a 085a2d5c5767
    - `cd backend && alembic upgrade head` exit 0
    - `cd backend && alembic downgrade -1` exit 0; `alembic upgrade head` novamente exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ORM UserPrinterAccess + relationships</name>
  <read_first>
    - backend/app/db/models.py (User, Printer classes)
    - SQLAlchemy Association Object pattern (05.2-RESEARCH.md)
  </read_first>
  <action>
    Adicionar classe `UserPrinterAccess` em models.py com Mapped columns conforme migration.
    Relationships: `User.printer_access_rows: Mapped[list[UserPrinterAccess]]` e `Printer.user_access_rows`; opcional `User.allowed_printers` view-only via association se útil para service.
    NÃO adicionar colunas em PrintJob.
  </action>
  <acceptance_criteria>
    - `UserPrinterAccess` em backend/app/db/models.py
    - `grep -r "UserPrinterAccess" backend/app/watcher` retorna vazio (exit 1 / no matches)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Pydantic schemas</name>
  <read_first>
    - backend/app/schemas/user.py
    - backend/app/schemas/printer.py
  </read_first>
  <action>
    Criar backend/app/schemas/user_printer_access.py:
    - `PrinterAccessItem`: printer_id int, is_default bool default False, is_active bool default True
    - `PrinterAccessRead`: id, user_id, printer_id, printer_display_name (optional enrich), is_default, is_active
    - `PrinterAccessReplace`: assignments: list[PrinterAccessItem] (min 0, max 50 items validator)
  </action>
  <acceptance_criteria>
    - Schemas importáveis sem circular import
    - `PrinterAccessReplace` rejeita lista com mais de 50 itens (ValidationError em teste unitário)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd backend && alembic upgrade head`
- [ ] `cd backend && pytest tests/test_user_printer_access_model.py -q` (criar se necessário smoke import)
- [ ] Confirmar print_jobs e watcher inalterados (`git diff backend/app/watcher`)
</verification>
