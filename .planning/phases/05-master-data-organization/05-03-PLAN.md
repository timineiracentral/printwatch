---
phase: 05-master-data-organization
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - backend/app/api/v1/printers.py
  - backend/app/schemas/printer.py
  - backend/app/services/printers_service.py
  - backend/app/api/v1/__init__.py
  - backend/tests/test_printers_api.py
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-06
  - SERVER-04

must_haves:
  truths:
    - "D-11 registry canônico; D-12 unmapped-queues; D-14 cups_queue_name único; D-18 soft-delete"
    - "CRUD completo /api/v1/printers com soft-delete is_active=false"
    - "cups_queue_name único globalmente; comparação com normalize_printer_name no create/update"
    - "GET /api/v1/printers/unmapped-queues retorna filas DISTINCT em jobs sem printer cadastrado"
    - "GET /api/v1/printers lista registry (não DISTINCT legado)"
    - "POST/PATCH disparam hook para matcher on-save (stub ou chamada real se plan 05 já merged)"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-05-03 | Mass assignment em printer create | medium | Pydantic schemas explícitos; sem campos extras |
| T-05-04 | Unmapped endpoint SQL injection | low | SQLAlchemy parametrizado apenas |
</threat_model>

<objective>
API registry de impressoras — fonte canônica substituindo DISTINCT do log (INV-06).
</objective>

<context>
@.planning/phases/05-master-data-organization/05-CONTEXT.md
@backend/app/api/v1/printers.py
@backend/app/core/normalize.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schemas e service layer printers</name>
  <read_first>
    - backend/app/api/v1/jobs.py
    - backend/app/services/jobs_service.py
    - backend/app/db/models.py
  </read_first>
  <action>
    Criar `schemas/printer.py`: PrinterCreate, PrinterUpdate, PrinterRead (id, display_name, cups_queue_name, ip_address, manufacturer_model, location, department_id, is_active, timestamps).
    Criar `services/printers_service.py` com list (active only default), get_by_id, create, update, soft_delete.
    No create/update: aplicar `normalize_printer_name` em cups_queue_name antes de persistir.
    Validar unicidade cups_queue_name com normalize na comparação.
  </action>
  <acceptance_criteria>
    - `PrinterRead` inclui campo `id: int`
    - Service raise HTTP 409 em cups_queue_name duplicado
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rotas CRUD + unmapped-queues</name>
  <read_first>
    - backend/app/api/v1/__init__.py
    - backend/app/api/v1/printers.py
  </read_first>
  <action>
    Substituir implementação atual de printers.py:
    - GET "" → list[PrinterRead]
    - POST "" → create
    - GET "/{id}" → detail
    - PATCH "/{id}" → update
    - DELETE "/{id}" → soft delete (is_active=false)
    - GET "/unmapped-queues" → list[str] filas em print_jobs.printer sem match em registry (usar normalize em Python ou subquery)
    Registrar router em api_v1_router.
    Após create/update bem-sucedido: chamar função `schedule_match_for_queue(cups_queue_name)` (import lazy de matcher se disponível, senão no-op com TODO removido no plan 05).
  </action>
  <acceptance_criteria>
    - `curl -s http://localhost:8000/api/v1/openapi.json` lista paths `/api/v1/printers` e `/api/v1/printers/unmapped-queues`
    - `pytest tests/test_printers_api.py -q` exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] CRUD manual via Swagger
- [ ] `pytest tests/test_printers_api.py -q`
</verification>
