---
phase: 04-dashboard-web
plan: "06"
type: execute
wave: 3
depends_on:
  - "04-04"
  - "04-05"
files_modified:
  - frontend/src/components/export/ExportCsvButton.tsx
  - frontend/src/components/layout/PageHeader.tsx
  - frontend/src/App.tsx
  - scripts/validate-phase4.sh
autonomous: false
requirements:
  - EXPORT-01
  - DASH-01
  - DASH-06

must_haves:
  truths:
    - "Botão Exportar CSV visível no header baixa CSV com filtros ativos"
    - "Export não envia page/size"
    - "Erro 400 cap 100k exibe mensagem ao usuário"
    - "validate-phase4.sh --quick verde; checkpoint humano ROADMAP critérios 1-5"
  artifacts:
    - path: "frontend/src/components/export/ExportCsvButton.tsx"
      provides: "download com loading state"
      contains: "downloadCsv"
    - path: "scripts/validate-phase4.sh"
      provides: "suite completa + checkpoint #17"
      contains: "CHECKPOINT HUMANO"
  key_links:
    - from: "frontend/src/components/export/ExportCsvButton.tsx"
      to: "/api/v1/export/csv"
      via: "downloadCsv(filters)"
      pattern: "export/csv"
---

<objective>
Vertical slice final: export CSV + integração completa + validação Nyquist e checkpoint humano (D-67).

Purpose: Fecha EXPORT-01 e critérios de sucesso ROADMAP Fase 4 (performance, cards vs banco, filtros, export).

Output: Dashboard completo em `http://<VM_HOST>`; operador aprova checkpoint após validar cards, filtros, export e tempo de abertura &lt;2s.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-dashboard-web/04-CONTEXT.md
@.planning/phases/04-dashboard-web/04-UI-SPEC.md
@frontend/src/api/export.ts
@frontend/src/hooks/useUrlFilters.ts
@scripts/validate-phase3.sh
@scripts/validate-phase4.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExportCsvButton wired ao PageHeader</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-34..D-37
    - .planning/phases/04-dashboard-web/04-UI-SPEC.md — Button secondary, Export loading copy
    - frontend/src/api/export.ts
  </read_first>
  <files>
    frontend/src/components/export/ExportCsvButton.tsx,
    frontend/src/components/layout/PageHeader.tsx,
    frontend/src/App.tsx
  </files>
  <action>
    `ExportCsvButton` (D-34, D-36):
    - Variant secondary Button "Exportar CSV" (UI-SPEC)
    - Lê filtros atuais de `useUrlFilters()` — repassa `username`, `printer`, `search`, `date_from`, `date_to` apenas (D-35 **sem** page/size)
    - onClick: `downloadCsv(...)` com estado loading "Exportando…" + disabled (D-36, UI-SPEC)
    - Catch `ExportCapError` (400): toast ou inline alert com `detail` backend + sufixo "Reduza o período ou adicione filtros." (D-37)
    - Network fail: "Falha ao exportar. Verifique a conexão e tente novamente." (UI-SPEC)

    `PageHeader`: substituir placeholder por `<ExportCsvButton />` no slot actions (D-34 sempre visível).

    `App.tsx`: revisão final — AppShell → PageHeader → SummaryCards → FilterBar → JobsTable → JobsPagination. Sem código morto; sem React Router (D-44).

    Verificar EXPORT-02 indiretamente: CSV gerado pelo backend (BOM, `;`, pt-BR) — botão só dispara download.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -D - "http://localhost/api/v1/export/csv" | grep -qi "content-disposition: attachment"</automated>
  </verify>
  <acceptance_criteria>
    - ExportCsvButton chama downloadCsv sem page/size nos params
    - PageHeader renderiza ExportCsvButton
    - Loading state durante download
  </acceptance_criteria>
  <done>EXPORT-01 funcional no header.</done>
</task>

<task type="auto">
  <name>Task 2: Completar validate-phase4.sh (full checks + export via nginx)</name>
  <read_first>
    - scripts/validate-phase3.sh — checks 08-10 CSV, CORS 14-15
    - .planning/phases/04-dashboard-web/04-VALIDATION.md
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-67
  </read_first>
  <files>
    scripts/validate-phase4.sh
  </files>
  <action>
    Expandir `validate-phase4.sh` para paridade com validate-phase3 via nginx BASE_URL=http://localhost:

    Checks adicionais (se não existirem do plan 01):
    - CSV BOM UTF-8 + separador `;` + header Usuário (espelhar 08-10 phase3)
    - /jobs date_from > date_to → 422
    - /jobs size=999 → 422
    - CORS preflight localhost:5173 (dev — D-57) via proxy ou direto backend se documentado WARN
    - curl -w time_total jobs &lt; 0.5s WARN se DB grande (DASH-06 hint)
    - frontend npm test --run

    `--quick`: todos automáticos, sem read -p.

    Documentar no header: produção usa :80; dev Vite :5173 com proxy.
  </action>
  <verify>
    <automated>bash scripts/validate-phase4.sh --quick</automated>
  </verify>
  <acceptance_criteria>
    - validate-phase4.sh --quick exit 0 em stack healthy
    - Pelo menos 14 checks automáticos numerados
  </acceptance_criteria>
  <done>Script Nyquist completo para CI local.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — ROADMAP critérios 1-5 + D-67</name>
  <read_first>
    - .planning/ROADMAP.md — Fase 4 Success Criteria
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-62, D-67
  </read_first>
  <files>
    .planning/phases/04-dashboard-web/04-VERIFICATION.md
  </files>
  <action>
    Pré-requisito: `bash scripts/validate-phase4.sh --quick` verde.

    Operador valida em browser `http://<VM_HOST>` ou `http://localhost`:

    1. **DASH-06 / crit.1:** Hard refresh; DevTools Network — DOMContentLoaded + primeira paint cards+tabela &lt; 2s em rede local (D-62).
    2. **DASH-02 / crit.2:** Comparar "Jobs hoje" e "Páginas hoje" com `curl http://localhost/api/v1/stats/summary` e opcionalmente SQL no container backend.
    3. **DASH-04 / crit.3:** Filtrar usuário + impressora; tabela só mostra matches; URL reflete params.
    4. **DASH-05 / crit.4:** Busca parcial por nome de arquivo retorna resultados corretos.
    5. **EXPORT-01 / crit.5:** Exportar CSV com filtros ativos; abrir no Excel pt-BR — separador `;`, acentos OK (EXPORT-02).

    Registrar evidência breve em `04-VERIFICATION.md` (data, VM_HOST, pass/fail por critério).

    Resume signal: "approved" ou descrever falhas.
  </action>
  <verify>
    <automated>bash scripts/validate-phase4.sh --quick</automated>
    <human-check>
      Browser em http://VM_HOST: confirmar 5 critérios ROADMAP; export CSV abre no Excel; tempo &lt;2s percebido.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Operador confirmou 5 critérios ou documentou falhas em 04-VERIFICATION.md
    - validate-phase4 --quick verde antes do checkpoint
  </acceptance_criteria>
  <done>Fase 4 pronta para /gsd-verify-work; D-67 satisfeito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV download | Dados sensíveis impressão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | I | CSV exposto na rede | accept | Rede local; sem auth MVP |
| T-04-15 | T | CSV formula injection | accept | Documentar risco Excel; dados do backend |
| T-04-16 | D | Export 100k cap bypass | mitigate | Backend 400; UI mensagem D-37 |

</threat_model>

<verification>
- `bash scripts/validate-phase4.sh --quick` verde
- Checkpoint humano aprovado
- `cd frontend && npm test -- --run` verde
</verification>

<success_criteria>
- Export CSV com filtros ativos
- Dashboard completo integrado
- Validação Nyquist + checkpoint humano
- Todos requisitos DASH-01..06 e EXPORT-01 cobertos na fase
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-web/04-06-SUMMARY.md` when done
</output>
