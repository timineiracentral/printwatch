---
phase: 05-master-data-organization
plan: "07"
type: execute
wave: 4
depends_on:
  - "05-03"
  - "05-04"
  - "05-05"
  - "05-06"
files_modified:
  - frontend/package.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/routes/
  - frontend/src/pages/settings/
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/api/printers.ts
  - frontend/src/hooks/usePrinters.ts
  - frontend/src/components/filters/PrinterCombobox.tsx
  - frontend/src/types/api.ts
autonomous: true
requirements:
  - SETTINGS-01
  - SETTINGS-02
  - SETTINGS-03
  - SETTINGS-04
  - SERVER-04
  - INV-03
  - INV-06

must_haves:
  truths:
    - "D-08 rotas /settings/*; D-09 audit vs manager; D-10 UX PaperCut-like; D-13 banner unmapped; D-18 soft-delete UI"
    - "D-29–D-31 capture invariants preservados (watcher independente, sem org models no watcher)"
    - "Rotas /settings/printers|departments|cost-centers|users|import funcionam no AppShell"
    - "Sidebar mostra Jobs + grupo Configurações com 5 links"
    - "CRUD UI para 4 entidades com dialog modal e soft-delete"
    - "Banner unmapped-queues em Settings > Impressoras quando count > 0"
    - "Página Import com download template + upload + painel de resultado"
    - "Audit dashboard / inalterado: SummaryCards, FilterBar, JobsTable funcionam"
    - "PrinterCombobox usa registry API (display_name ou cups_queue_name)"
---

<objective>
Settings UI completa conforme 05-UI-SPEC.md; migração FilterBar para registry; react-router-dom.
</objective>

<context>
@.planning/phases/05-master-data-organization/05-UI-SPEC.md
@.planning/phases/05-master-data-organization/05-CONTEXT.md
@frontend/src/components/layout/Sidebar.tsx
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: react-router-dom e estrutura de rotas</name>
  <read_first>
    - frontend/src/main.tsx
    - frontend/src/App.tsx
    - frontend/package.json
  </read_first>
  <action>
    Adicionar dependência `react-router-dom`.
    Criar `routes/index.tsx` com BrowserRouter:
    - `/` → JobsPage (mover conteúdo atual de App.tsx)
    - `/settings` → SettingsLayout com Outlet
    - children: printers, departments, cost-centers, users, import
    Atualizar Sidebar: NavLink para Jobs e cada settings route; ícones lucide (Printer, Building2, Wallet, Users, Upload).
  </action>
  <acceptance_criteria>
    - `npm run build` exit 0
    - Navegar /settings/printers renderiza sem erro no console
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: API client + hooks CRUD settings</name>
  <read_first>
    - frontend/src/api/client.ts
    - frontend/src/api/printers.ts
    - backend OpenAPI paths
  </read_first>
  <action>
    Estender client para POST/PATCH/DELETE e FormData upload.
    Criar `api/settings/*.ts` e hooks `usePrintersRegistry`, `useDepartments`, etc. com TanStack Query invalidate on mutate.
    Tipos Printer, Department, CostCenter, User em types/api.ts.
  </action>
  <acceptance_criteria>
    - hooks exportam list + create + update + deactivate
    - TypeScript build sem erros em páginas settings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Páginas CRUD + unmapped banner + Import</name>
  <read_first>
    - .planning/phases/05-master-data-organization/05-UI-SPEC.md
    - frontend/src/components/ui/ (Button, Dialog, Table existentes)
  </read_first>
  <action>
    Implementar `pages/settings/PrintersPage.tsx` com DataTable, dialog create/edit, banner unmapped (GET unmapped-queues), CTA cadastrar com queue pré-preenchida.
    Implementar DepartmentsPage, CostCentersPage, UsersPage — mesmo padrão CRUD.
    Implementar ImportPage: 4 cards, download template link, file input, exibir resultado errors expandível.
    Seguir tokens Fase 4; textos PT-BR.
  </action>
  <acceptance_criteria>
    - SETTINGS-01: 5 rotas acessíveis via sidebar
    - Banner aparece quando mock/unmapped length > 0 (teste manual ou story)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Migrar PrinterCombobox para registry</name>
  <read_first>
    - frontend/src/components/filters/PrinterCombobox.tsx
    - frontend/src/hooks/usePrinters.ts
  </read_first>
  <action>
    Alterar fetchPrinters para retornar PrinterRead[]; combobox exibe `display_name` com fallback cups_queue_name; value enviado ao filtro jobs permanece cups_queue_name normalizado (compat API jobs filter).
    Garantir `/` jobs page regressão: filtros, tabela, export CSV funcionam.
  </action>
  <acceptance_criteria>
    - `npm run build` exit 0
    - GET /jobs?printer= funciona com valor do registry
    - P5-AC-07: página / carrega SummaryCards + JobsTable
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd frontend && npm run build`
- [ ] Smoke manual: CRUD printer, import CSV users, audit / jobs filter
</verification>
