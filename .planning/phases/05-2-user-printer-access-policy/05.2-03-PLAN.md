---
phase: 05.2-user-printer-access-policy
plan: "03"
type: execute
wave: 2
depends_on:
  - "05.2-01"
files_modified:
  - backend/app/services/policy_service.py
  - backend/app/services/jobs_service.py
  - backend/app/schemas/jobs.py
  - backend/app/api/v1/jobs.py
  - backend/app/services/export_service.py
  - backend/tests/test_policy_compliance.py
autonomous: true
requirements:
  - ACCESS-05

must_haves:
  truths:
    - "JobOut inclui outside_policy: bool"
    - "Regra D-20/D-22: só true para user cadastrado ativo + printer_id + fora do set; false se sem atribuições"
    - "Agregação jobs inclui printer_id via func.max(PrintJob.printer_id)"
    - "JobFilters.outside_policy opcional filtra lista"
    - "Export CSV jobs inclui coluna outside_policy"
    - "Nenhuma alteração no watcher INSERT"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-52-03a | N+1 queries por job | medium | Batch maps em policy_service |
| T-52-03b | Falso positivo user desconhecido | medium | Testes D-21 casos negativos |
</threat_model>

<objective>
Enriquecimento read-time `outside_policy` em jobs API e export — compliance soft sem bloqueio.
</objective>

<context>
@.planning/phases/05-2-user-printer-access-policy/05.2-CONTEXT.md (D-17–D-22, D-30)
@backend/app/services/jobs_service.py
@backend/app/schemas/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: policy_service + unit tests</name>
  <read_first>
    - backend/app/db/models.py (User, UserPrinterAccess, PrintJob)
    - .planning/phases/05-2-user-printer-access-policy/05.2-RESEARCH.md (outside_policy Algorithm)
  </read_first>
  <action>
    Criar `policy_service.py`:
    - `load_policy_context(db) -> PolicyContext` com dict username_lower→user_id e dict user_id→set[printer_id] (apenas is_active)
    - `compute_outside_policy(ctx, username: str, printer_id: int | None) -> bool` implementando D-20–D-22
    Testes em test_policy_compliance.py:
    - user desconhecido → False
    - printer_id None → False
    - user sem assignments → False
    - user com assignments, printer fora → True
    - user com assignments, printer dentro → False
  </action>
  <acceptance_criteria>
    - `pytest tests/test_policy_compliance.py -q` exit 0 (mínimo 5 casos acima)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: jobs_service aggregation + JobOut</name>
  <read_first>
    - backend/app/services/jobs_service.py
    - backend/app/schemas/jobs.py
    - backend/app/api/v1/jobs.py
  </read_first>
  <action>
    Em `_build_aggregated_query`: adicionar `func.max(PrintJob.printer_id).label("printer_id")`.
    Em `list_jobs` e `get_job_by_id`: após obter rows, chamar `load_policy_context` uma vez; para cada item setar `outside_policy = compute_outside_policy(...)`.
    Em `JobOut`: campo `outside_policy: bool = False`.
    Em `JobFilters`: `outside_policy: Optional[bool] = None`; quando True/False, filtrar items após enrichment (aceitável MVP) ou adicionar WHERE se implementável sem quebrar agregação.
  </action>
  <acceptance_criteria>
    - GET /api/v1/jobs retorna JSON com chave `outside_policy` em cada item
    - `?outside_policy=true` retorna apenas jobs com flag true
    - `grep -r "policy_service" backend/app/watcher` vazio
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Export CSV coluna outside_policy</name>
  <read_first>
    - backend/app/api/v1/export.py
    - backend/app/services/export_service.py (ou módulo que gera CSV de jobs)
  </read_first>
  <action>
    Adicionar coluna CSV header `fora_da_politica` (ou `outside_policy`) com valores `sim`/`nao` ou `true`/`false` consistente com export existente.
    Reutilizar mesma lógica `compute_outside_policy` — não duplicar regra.
  </action>
  <acceptance_criteria>
    - GET export jobs CSV inclui header de política
    - Job de user cadastrado fora da lista → `sim`/true na coluna
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `pytest tests/test_policy_compliance.py tests/test_jobs_api.py -q`
- [ ] Regressão: watcher tests passam
</verification>
