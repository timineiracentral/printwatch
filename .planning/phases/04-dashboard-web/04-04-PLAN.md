---
phase: 04-dashboard-web
plan: "04"
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - frontend/src/components/summary/SummaryCard.tsx
  - frontend/src/components/summary/SummaryCards.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - DASH-02

must_haves:
  truths:
    - "Quatro cards exibem jobs hoje, páginas hoje, top usuário mês, top impressora mês"
    - "Tops no formato Nome — N páginas com separador de milhar pt-BR"
    - "Cards vazios mostram Sem dados no período"
    - "Dados vêm exclusivamente de GET /api/v1/stats/summary"
  artifacts:
    - path: "frontend/src/components/summary/SummaryCards.tsx"
      provides: "4 cards grid"
      contains: "useStatsSummary"
    - path: "frontend/src/components/summary/SummaryCard.tsx"
      provides: "metric e top variants"
      min_lines: 20
  key_links:
    - from: "frontend/src/components/summary/SummaryCards.tsx"
      to: "/api/v1/stats/summary"
      via: "useStatsSummary → fetchStatsSummary"
      pattern: "stats/summary"
---

<objective>
Vertical slice: admin vê sumários do dia e tops do mês ao abrir o dashboard — primeiro dado real na UI.

Purpose: Entrega DASH-02 com contrato API Fase 3; valida integração Query + formatação pt-BR antes da tabela.

Output: Cards populados com `stats.hoje.jobs`, `stats.hoje.pages`, `mes.top_users[0]`, `mes.top_printers[0]`; skeleton no loading inicial (D-49).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-web/04-UI-SPEC.md
@.planning/phases/04-dashboard-web/04-CONTEXT.md
@frontend/src/hooks/useStatsSummary.ts
@frontend/src/lib/format.ts
@backend/app/schemas/stats.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SummaryCard + SummaryCards com loading/empty/error</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-UI-SPEC.md — SummaryCard, Copywriting cards
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-10..D-14, D-49, D-51
    - backend/app/schemas/stats.py — TopEntry, StatsBucket
  </read_first>
  <files>
    frontend/src/components/summary/SummaryCard.tsx,
    frontend/src/components/summary/SummaryCards.tsx
  </files>
  <action>
    `SummaryCard` variants (D-04, UI-SPEC):
    - `metric`: label 12px secondary + valor (Display 24px para números jobs/páginas hoje — D-10)
    - `top`: label + linha única `formatTopLabel(name, pages)` → "Maria Silva — 1.284 páginas" (D-12)

    Labels pt-BR (UI-SPEC Copywriting):
    - Jobs hoje / Páginas hoje / Top usuário do mês / Top impressora do mês

    `SummaryCards`:
    - `useStatsSummary()` (D-13) — **não** recalcular tops a partir de jobs (D-13, D-14 sem charts)
    - Valores: `data.hoje.jobs`, `data.hoje.pages`, `data.mes.top_users[0]`, `data.mes.top_printers[0]` (D-10, D-11)
    - Loading inicial: 4 Skeleton cards (D-49)
    - Empty top: "Sem dados no período" quando array vazio (D-12)
    - Erro: ErrorBanner com refetch (D-51)

    Grid: `grid-cols-4` gap 16px ≥1280px; `grid-cols-2` 1024–1279 (D-06 spacing, UI-SPEC).

    Cards discretos: border 1px, radius 12px, shadow mínima (D-04). Sem gráficos (D-14).
  </action>
  <verify>
    <automated>curl -sf http://localhost/api/v1/stats/summary | python3 -c "import json,sys; j=json.load(sys.stdin); assert 'hoje' in j and 'mes' in j"</automated>
  </verify>
  <acceptance_criteria>
    - SummaryCards importa useStatsSummary
    - Nenhum import de chart.js, recharts, victory
    - formatTopLabel usado para tops
  </acceptance_criteria>
  <done>Componentes de cards prontos com todos os estados.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar SummaryCards no App abaixo do PageHeader</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-06 ordem sumário → filtros → tabela, D-48
    - frontend/src/components/layout/AppShell.tsx
  </read_first>
  <files>
    frontend/src/App.tsx
  </files>
  <action>
    Em `App.tsx`: dentro de AppShell main, ordem vertical (D-06, D-48):
    1. PageHeader (já existe)
    2. SummaryCards (gap 24px abaixo header)
    3. Placeholder div para FilterBar (plan 05) e JobsTable (plan 05)

    Remover smoke JSON.stringify do plan 02 se ainda presente.

    Garantir cards são secundários visualmente — não competem com futura tabela (D-48).
  </action>
  <verify>
    <automated>cd frontend && npm run build && curl -sf http://localhost/ | head -c 200</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx renderiza SummaryCards
    - Seção gap 24px entre header e cards
  </acceptance_criteria>
  <done>DASH-02 visível no dashboard; valores batem com curl stats/summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stats JSON → UI | Dados agregados sem PII extra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | I | Username em top card | accept | Admin autorizado na rede; escape React default |
| T-04-10 | T | Client-side top recalc | mitigate | Proibir reduce/sum em jobs.items para tops — só API |

</threat_model>

<verification>
- curl stats/summary vs valores exibidos (manual ou checkpoint plan 06)
- Cards skeleton no hard refresh
</verification>

<success_criteria>
- 4 cards corretos per DASH-02
- Formato pt-BR nos tops
- Sem charts
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-web/04-04-SUMMARY.md` when done
</output>
