---
phase: 01-infrastructure-print-server
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - scripts/validate-phase1.sh
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
user_setup: []

must_haves:
  truths:
    - "Repositório contém docker-compose.yml com único serviço cups e volumes preparados para Fase 2"
    - ".env.example documenta ALLOWED_NETWORK=REDACTED_IP/16 e credenciais admin como placeholders"
    - "Script validate-phase1.sh existe e valida presença de arquivos e variáveis obrigatórias"
  artifacts:
    - path: docker-compose.yml
      provides: "Orquestração Fase 1 — somente CUPS"
      contains: "cups:"
    - path: .env.example
      provides: "Documentação de variáveis de ambiente Fase 1"
      contains: "ALLOWED_NETWORK"
    - path: scripts/validate-phase1.sh
      provides: "Smoke tests Wave 0"
      exports: ["--quick"]
  key_links:
    - from: docker-compose.yml
      to: cups/
      via: "build: ./cups"
      pattern: "build:\\s*\\./cups"
    - from: docker-compose.yml
      to: .env
      via: "env_file"
      pattern: "env_file:\\s*\\.env"
---

## Phase Goal

**As a** admin de TI, **I want to** subir o PrintWatch via Docker Compose com apenas o serviço CUPS configurado, **so that** a base da cadeia compose→CUPS→impressora→page_log esteja pronta para build e validação.

<objective>
Criar o scaffold de deploy da Fase 1: `docker-compose.yml` (somente CUPS), `.env.example` completo e script `validate-phase1.sh` Wave 0 para feedback automatizado antes do container CUPS existir.

Purpose: Estabelecer contratos de deploy e validação Nyquist sem violar D-01/D-02 (sem backend/frontend/nginx).
Output: `docker-compose.yml`, `.env.example`, `scripts/validate-phase1.sh --quick`
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-print-server/01-CONTEXT.md
@.planning/phases/01-infrastructure-print-server/01-RESEARCH.md
@SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar .env.example com variáveis da Fase 1</name>
  <files>.env.example</files>
  <read_first>
    @SPEC.md (§5)
    @.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-08, D-19, D-20)
    @.planning/phases/01-infrastructure-print-server/01-RESEARCH.md (Pattern 4, .env.example)
  </read_first>
  <action>
    Criar `.env.example` na raiz do repositório com comentários em PT-BR documentando cada variável. Valores obrigatórios (per D-08, D-19, D-20, DEPLOY-02):

    - `ALLOWED_NETWORK=REDACTED_IP/16` — rede única permitida no CUPS (D-06: NÃO incluir 192.168/10/172 genéricos da SPEC)
    - `CUPS_ADMIN_USER=admin` — placeholder, nunca valor real (D-19)
    - `CUPS_ADMIN_PASSWORD=changeme` — placeholder, nunca valor real (D-19)
    - `TEST_PRINTER_NAME=test_printer` — nome da fila lpadmin
    - `TEST_PRINTER_URI=ipp://192.0.2.50/ipp/print` — URI IPP da impressora HP/Samsung de teste (D-12)
    - `TEST_PRINTER_DRIVER=everywhere` — driver IPP Everywhere; documentar fallback `cups-pdf` para teste sem hardware

    NÃO incluir variáveis de Fases 2+ (`DB_PATH`, `LOG_RETENTION_DAYS`) neste arquivo — escopo Fase 1 only (D-04). Adicionar bloco de comentário no topo: copiar para `.env` antes de `docker compose up -d`.
  </action>
  <acceptance_criteria>
    - Arquivo `.env.example` existe na raiz
    - `grep -E '^ALLOWED_NETWORK=ALLOWED_NETWORK_RANGE' .env.example` retorna match
    - `grep -E '^CUPS_ADMIN_USER=' .env.example` e `grep -E '^CUPS_ADMIN_PASSWORD=' .env.example` retornam match
    - `grep -E '^TEST_PRINTER_NAME=' .env.example` retorna match
    - Nenhuma linha contém senha real ou IP hardcoded de produção (apenas placeholders)
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' .env.example | grep -cE '^(ALLOWED_NETWORK|CUPS_ADMIN_USER|CUPS_ADMIN_PASSWORD|TEST_PRINTER_NAME|TEST_PRINTER_URI|TEST_PRINTER_DRIVER)=' | grep -qx '6'</automated>
  </verify>
  <done>.env.example documenta todas as variáveis necessárias para Fase 1 (DEPLOY-02, D-20)</done>
</task>

<task type="auto">
  <name>Task 2: Criar docker-compose.yml somente com serviço CUPS</name>
  <files>docker-compose.yml</files>
  <read_first>
    @SPEC.md (§3.5)
    @.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-01, D-02, D-03, D-04)
    @.planning/phases/01-infrastructure-print-server/01-RESEARCH.md (Pattern 4)
  </read_first>
  <action>
    Criar `docker-compose.yml` na raiz seguindo SPEC §3.5 mas implementando **apenas** o serviço `cups` (D-01, D-02):

    - `services.cups.build: ./cups`
    - `ports: ["631:631"]`
    - `env_file: .env`
    - `environment`: passar `ALLOWED_NETWORK`, `CUPS_ADMIN_USER`, `CUPS_ADMIN_PASSWORD` com defaults `${ALLOWED_NETWORK:-REDACTED_IP/16}`
    - `volumes`: `cups_logs:/var/log/cups`, `cups_spool:/var/spool/cups`
    - `restart: unless-stopped`
    - Seção `volumes`: declarar `cups_logs` e `cups_spool`

    Adicionar comentários indicando serviços futuros (backend Fase 2, frontend/nginx Fase 4) **sem** serviços placeholder ativos (D-03). NÃO incluir `backend`, `frontend`, `nginx`, `db_data` como serviços ativos.

    Usar `docker compose config` para validar sintaxe (não exige Dockerfile existente para validação de schema, mas build falhará até Plan 02 — esperado).
  </action>
  <acceptance_criteria>
    - `docker compose config --quiet` exit 0 (ou exit 0 após Plan 02 se exigir Dockerfile — validar YAML agora)
    - `grep -c '^  backend:' docker-compose.yml` retorna 0 (serviço ativo)
    - `grep -c '^  frontend:' docker-compose.yml` retorna 0
    - `grep -c '^  nginx:' docker-compose.yml` retorna 0
    - `grep 'cups_logs' docker-compose.yml` retorna match
    - `grep '631:631' docker-compose.yml` retorna match
  </acceptance_criteria>
  <verify>
    <automated>docker compose config --quiet 2>&1; test $? -eq 0 || (grep -q 'build:.*cups' docker-compose.yml && grep -q '631:631' docker-compose.yml && exit 0)</automated>
  </verify>
  <done>docker-compose.yml pronto para build do container CUPS na Plan 02 (DEPLOY-01 scaffold, D-01/D-03)</done>
</task>

<task type="auto">
  <name>Task 3: Criar scripts/validate-phase1.sh Wave 0</name>
  <files>scripts/validate-phase1.sh</files>
  <read_first>
    @.planning/phases/01-infrastructure-print-server/01-VALIDATION.md
    @.planning/phases/01-infrastructure-print-server/01-RESEARCH.md (Validation Architecture, Wave 0 Gaps)
  </read_first>
  <action>
    Criar `scripts/validate-phase1.sh` executável (`chmod +x`) com `set -euo pipefail`. Implementar modo `--quick` (default) com smoke tests que NÃO exigem container rodando:

    1. Verificar existência de: `docker-compose.yml`, `.env.example`, `cups/Dockerfile` (WARN se ausente — Plan 02), `scripts/setup-printer.sh` (WARN se ausente — Plan 03)
    2. Verificar chaves em `.env.example`: ALLOWED_NETWORK, CUPS_ADMIN_USER, CUPS_ADMIN_PASSWORD, TEST_PRINTER_NAME, TEST_PRINTER_URI, TEST_PRINTER_DRIVER
    3. Verificar `ALLOWED_NETWORK=REDACTED_IP/16` em `.env.example` (D-08)
    4. Verificar compose só tem serviço cups ativo: `grep -E '^  [a-z_]+:' docker-compose.yml` retorna apenas `cups:` (ignorar linhas comentadas)
    5. Função `run_full()` reservada (stub echo "full suite após Plan 04") — preenchida na Plan 04

    Saída: `[PASS]`/`[FAIL]`/`[WARN]` por check; exit 0 se todos checks obrigatórios passarem; exit 1 se algum FAIL.
  </action>
  <acceptance_criteria>
    - `bash scripts/validate-phase1.sh --quick` exit 0 após Tasks 1-2 completas
    - Script contém função ou flag `--quick`
    - Script verifica ALLOWED_NETWORK=REDACTED_IP/16
    - Runtime &lt; 30 segundos
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/validate-phase1.sh --quick</automated>
  </verify>
  <done>Wave 0 Nyquist operacional — feedback automatizado em cada commit da Fase 1 (D-21)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositório → `.env` local | `.env.example` é público; `.env` real contém credenciais (D-19) |
| Host → Docker Compose | Compose expõe porta 631 na VM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env.example` | mitigate | Apenas placeholders `changeme`; comentário explícito para não commitar `.env` (D-19) |
| T-01-02 | Tampering | `docker-compose.yml` | mitigate | Somente serviço cups; sem portas 80/8000 expostas nesta fase (D-02) |
| T-01-03 | Elevation | Compose env passthrough | mitigate | Credenciais admin só via `.env`, não hardcoded no YAML |
| T-01-SC | Tampering | apt/npm installs | accept | Fase 1 não instala pacotes npm/pip; apt auditado em Plan 02 |
</threat_model>

<verification>
Após execução: `bash scripts/validate-phase1.sh --quick` verde; `docker compose config` válido; `.env.example` com 6 variáveis documentadas.
</verification>

<success_criteria>
- DEPLOY-02: `.env.example` completo com ALLOWED_NETWORK e placeholders admin
- DEPLOY-01 scaffold: compose válido com único serviço cups e volumes persistentes
- Wave 0: script de validação rápida operacional
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-print-server/01-01-SUMMARY.md` when done
</output>
