---
phase: 06-costing-chargeback
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/*_cost_rates.py
  - backend/app/db/models.py
  - backend/app/services/parser.py
  - backend/app/services/color_mode.py
  - backend/tests/test_color_mode.py
  - docs/cups-color-capture.md
autonomous: true
requirements:
  - COST-01
  - COST-02

must_haves:
  truths:
    - "D-01/D-02: cost_rates com histórico valid_from; sem overwrite silencioso"
    - "D-05/D-07: parser normaliza aliases CUPS → mono|color|NULL"
    - "D-08: color_mode_source captured|manual suportado no schema"
    - "Watcher não importa cost_rates nem cost_service"
    - "alembic upgrade head aplica cost_rates + color_mode_source em print_jobs"
  artifacts:
    - path: backend/app/db/models.py
      provides: CostRate model
    - path: docs/cups-color-capture.md
      provides: Runbook CUPS color capture
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-06-01 | Migration quebra DB | high | downgrade -1 test; batch_alter_table SQLite |
| T-06-02 | Parser rejeita linhas válidas | medium | Testes com amostras reais page_log; unknown→NULL não drop |
</threat_model>

<objective>
Fundação de schema e normalização de color_mode: tabela cost_rates, coluna color_mode_source, helper de aliases, documentação CUPS.
</objective>

<context>
@.planning/phases/06-costing-chargeback/06-CONTEXT.md
@.planning/phases/06-costing-chargeback/06-RESEARCH.md
@backend/alembic/versions/c4e8f1a92b03_user_printer_access.py
@backend/app/services/parser.py
@scripts/fix-cups-color-queue.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration cost_rates + print_jobs.color_mode_source</name>
  <read_first>
    - backend/alembic/versions/c4e8f1a92b03_user_printer_access.py
    - backend/app/db/models.py
    - .planning/phases/06-costing-chargeback/06-RESEARCH.md (Schema)
  </read_first>
  <action>
    Gerar revision `alembic revision -m "cost_rates"` com down_revision `c4e8f1a92b03`.
    upgrade(): create_table cost_rates (rate_mono/rate_color Numeric(12,4), valid_from DateTime NOT NULL, timestamps).
    create_index ix_cost_rates_valid_from on valid_from.
    batch_alter_table print_jobs: add color_mode_source String(20) nullable.
    downgrade(): drop cost_rates; drop column color_mode_source.
  </action>
  <acceptance_criteria>
    - Revision encadeada a c4e8f1a92b03 em backend/alembic/versions/
    - `cd backend && alembic upgrade head` exit 0
    - `cd backend && alembic downgrade -1 && alembic upgrade head` exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Model CostRate + color_mode normalizer</name>
  <read_first>
    - backend/app/db/models.py
    - backend/app/services/parser.py
  </read_first>
  <action>
    Adicionar classe CostRate em models.py com Mapped Decimal columns.
    Criar backend/app/services/color_mode.py com normalize_color_mode(raw: str|None) -> tuple[str|None, str|None] retornando (canonical mono|color|None, source hint).
    Atualizar parser.parse_page_log_line: após group(6), chamar normalize; se canonical, set color_mode_source='captured'.
    Adicionar PrintJob.color_mode_source Mapped optional.
  </action>
  <acceptance_criteria>
    - `class CostRate` em backend/app/db/models.py
    - `normalize_color_mode('grayscale')` retorna `('mono', ...)` em test_color_mode.py
    - `grep -r "CostRate\|cost_rates" backend/app/watcher` sem matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CUPS capture runbook</name>
  <read_first>
    - scripts/fix-cups-color-queue.sh
    - .planning/phases/06-costing-chargeback/06-CONTEXT.md (specifics)
  </read_first>
  <action>
    Criar docs/cups-color-capture.md em pt-BR: pré-requisitos docker cups running; uso do script com QUEUE e IPP URI; como validar page_log (campo color); link para pendências → correção manual Fase 6.
  </action>
  <acceptance_criteria>
    - docs/cups-color-capture.md existe e menciona fix-cups-color-queue.sh
    - `pytest -q backend/tests/test_color_mode.py` exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [BLOCKING] `cd backend && alembic upgrade head` após merge de migration
- pytest test_color_mode + suite existente verde
</verification>
