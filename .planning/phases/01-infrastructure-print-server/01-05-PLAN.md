---
phase: 01-infrastructure-print-server
plan: 05
type: execute
wave: 5
depends_on:
  - "01-03"
files_modified:
  - scripts/bootstrap-vm.sh
  - scripts/verify-vm-network.sh
  - docs/vm-setup.md
  - docs/vm-deploy-runbook.md
autonomous: false
requirements:
  - DEPLOY-01
  - SERVER-01
  - SERVER-03
user_setup:
  - service: ubuntu-vm
    why: "Deploy real em VM_HOST — ambiente alvo da Fase 1"
    env_vars:
      - name: VM_SSH_HOST
        source: "VM_HOST (ou hostname printwatch)"
      - name: VM_SSH_USER
        source: "Usuário sudo com acesso SSH à VM XCP-ng"

must_haves:
  truths:
    - "VM Ubuntu 22.04 acessível via SSH com IP VM_HOST"
    - "Docker e Docker Compose plugin instalados na VM"
    - "docker compose up -d --build sobe CUPS na VM sem erro"
    - "Porta 631 publicada e acessível na rede REDACTED_IP/16"
    - "test_printer cadastrada via setup-printer.sh na VM"
    - "bash scripts/validate-phase1.sh --quick exit 0 executado NA VM"
  artifacts:
    - path: scripts/bootstrap-vm.sh
      provides: "Bootstrap idempotente Docker + compose + impressora"
      contains: "docker compose up"
    - path: scripts/verify-vm-network.sh
      provides: "Checks rede/firewall porta 631"
      contains: "631"
    - path: docs/vm-deploy-runbook.md
      provides: "Runbook operacional deploy VM"
      contains: "VM_HOST"
  key_links:
    - from: scripts/bootstrap-vm.sh
      to: docker-compose.yml
      via: "docker compose up -d --build"
      pattern: "docker compose up"
    - from: scripts/bootstrap-vm.sh
      to: scripts/setup-printer.sh
      via: "exec após compose up"
      pattern: "setup-printer"
    - from: docs/vm-deploy-runbook.md
      to: docs/phase1-validation.md
      via: "pré-requisito checkpoint IPP"
      pattern: "phase1-validation"

blocker_resolved:
  id: B-01-04-VM
  description: "Checkpoint 01-04 Task 3 bloqueado — VM real não provisionada; validação remota IPP impossível sem deploy em VM_HOST"
  inserted: "2026-05-26"
---

<objective>
Implantar o PrintWatch na VM Ubuntu real (VM_HOST): acesso SSH, Docker/Compose, deploy do stack CUPS, validação de rede/firewall, subida do CUPS e cadastro da impressora de teste — pré-requisito obrigatório antes do checkpoint de validação remota IPP (01-04 Task 3).

Purpose: O trabalho até aqui foi local/dev; SERVER-01 e DEPLOY-01 exigem runtime na VM alvo. Sem este plano, Windows → IPP → CUPS → page_log não pode ser testado.
Output: Scripts de bootstrap/verificação, runbook operacional, VM com CUPS operacional e test_printer cadastrada.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-16, D-17, D-18)
@.planning/phases/01-infrastructure-print-server/01-RESEARCH.md
@docs/vm-setup.md
@docker-compose.yml
@scripts/setup-printer.sh
@scripts/validate-phase1.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scripts/bootstrap-vm.sh idempotente</name>
  <files>scripts/bootstrap-vm.sh</files>
  <read_first>
    @docs/vm-setup.md (seções 3–5)
    @.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-16, D-17)
    @docker-compose.yml
  </read_first>
  <action>
    Criar `scripts/bootstrap-vm.sh` executável (`set -euo pipefail`) para rodar **na VM** após clone do repo:

    1. **Pré-checks:** Ubuntu 22.04 (`/etc/os-release`); aviso se IP != VM_HOST (não abortar — operador pode estar em transição)
    2. **Docker:** instalar `docker.io` + `docker-compose-plugin` se ausentes; adicionar `$USER` ao grupo `docker` se necessário (avisar re-login)
    3. **.env:** se `.env` não existir, copiar de `.env.example` e abortar com mensagem clara se `CUPS_ADMIN_PASSWORD=changeme` (operador deve editar)
    4. **Deploy:** `docker compose up -d --build`
    5. **Aguardar CUPS:** loop até `docker compose exec -T cups curl -sf http://127.0.0.1:631/ >/dev/null` (timeout 120s)
    6. **Impressora:** `./scripts/setup-printer.sh`
    7. **Smoke:** `bash scripts/validate-phase1.sh --quick`

    Flags: `--skip-docker-install` (se Docker já instalado), `--dry-run` (só echo, sem mutação).

    Mensagens `[PASS]`/`[FAIL]`/`[WARN]` consistentes com validate-phase1.sh.
  </action>
  <acceptance_criteria>
    - Script existe, executável, idempotente (segunda execução não quebra)
    - Chama docker compose up, setup-printer e validate --quick
    - Falha cedo se .env ausente ou senha default
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/bootstrap-vm.sh && grep -q 'docker compose up' scripts/bootstrap-vm.sh && grep -q 'setup-printer' scripts/bootstrap-vm.sh</automated>
  </verify>
  <done>Bootstrap automatizado disponível para operador na VM</done>
</task>

<task type="auto">
  <name>Task 2: Criar scripts/verify-vm-network.sh e runbook</name>
  <files>scripts/verify-vm-network.sh, docs/vm-deploy-runbook.md, docs/vm-setup.md</files>
  <read_first>
    @.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-05, D-07, D-17)
    @docs/vm-setup.md
    @cups/cupsd.conf.template
  </read_first>
  <action>
    **scripts/verify-vm-network.sh** (rodar na VM após bootstrap):

    - Verificar container `cups` running
    - `curl -sf -o /dev/null -w '%{http_code}' http://127.0.0.1:631/` → esperado 200
    - `ss -tlnp | grep ':631'` ou `docker compose port cups 631` — porta publicada
    - Verificar ACL no cupsd.conf gerado: `grep 'Allow from' /etc/cups/cupsd.conf` via docker exec
    - Se `ufw` ativo: avisar se regra 631/tcp ausente (WARN, não FAIL — firewall pode ser externo)
    - Opcional: aceitar `--from-ip` para testar curl de outro host documentado no runbook

    **docs/vm-deploy-runbook.md** em PT-BR — sequência operacional explícita:

    1. Acesso SSH à VM (`ssh user@VM_HOST`)
    2. Hostname + IP estático (link vm-setup.md §1–2)
    3. Clone repo + `cp .env.example .env` + editar credenciais/impressora
    4. `./scripts/bootstrap-vm.sh`
    5. `./scripts/verify-vm-network.sh`
    6. Gate de prontidão: checklist antes de phase1-validation (CUPS up, 631, test_printer, validate --quick verde)
    7. Próximo passo: retomar 01-04 Task 3 (job remoto Windows IPP)

    Atualizar `docs/vm-setup.md`: link para runbook no topo; distinguir "preparação manual" vs "deploy automatizado".
  </action>
  <acceptance_criteria>
    - verify-vm-network.sh existe e testa porta 631 + ACL
    - vm-deploy-runbook.md documenta os 6 passos do bloqueio reportado pelo operador
    - vm-setup.md referencia runbook
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'VM_HOST' docs/vm-deploy-runbook.md && grep -q '631' scripts/verify-vm-network.sh && grep -q 'vm-deploy-runbook' docs/vm-setup.md</automated>
  </verify>
  <done>Validação de rede/firewall documentada e automatizada onde possível</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Checkpoint — deploy real na VM VM_HOST</name>
  <files>docs/vm-deploy-runbook.md</files>
  <read_first>
    @docs/vm-deploy-runbook.md
    @scripts/bootstrap-vm.sh
    @scripts/verify-vm-network.sh
  </read_first>
  <what-built>
    Scripts bootstrap-vm.sh e verify-vm-network.sh; runbook operacional completo. Artefatos locais prontos — falta execução na VM real.
  </what-built>
  <how-to-verify>
    1. SSH na VM Ubuntu 22.04 (XCP-ng) com sudo
    2. Confirmar/configurar IP VM_HOST e hostname printwatch (vm-setup.md §1–2)
    3. Clone do repo, `.env` configurado (senha CUPS + TEST_PRINTER_*)
    4. Executar `./scripts/bootstrap-vm.sh` → exit 0
    5. Executar `./scripts/verify-vm-network.sh` → exit 0
    6. De outro host em REDACTED_LAN: `curl -I http://VM_HOST:631/` → HTTP 200
    7. `docker compose exec cups lpstat -p` → test_printer enabled
    8. `bash scripts/validate-phase1.sh --quick` na VM → exit 0
  </how-to-verify>
  <action>
    Operador executa deploy na VM real conforme runbook. Gate blocking — 01-04 Task 3 (IPP remoto Windows) permanece bloqueado até este checkpoint ser aprovado.
  </action>
  <acceptance_criteria>
    - CUPS running na VM com porta 631 acessível na LAN REDACTED_IP/16
    - test_printer cadastrada
    - validate-phase1.sh --quick verde NA VM
    - Evidência: output dos comandos ou "approved" do operador
  </acceptance_criteria>
  <resume-signal>Digite "approved" após deploy na VM confirmado, ou descreva bloqueio (ex.: sem acesso SSH, firewall corporativo)</resume-signal>
  <verify>
    <automated>bash scripts/validate-phase1.sh --quick</automated>
  </verify>
  <done>VM produção pronta — desbloqueia checkpoint IPP remoto (01-04 Task 3)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador → VM SSH | Credenciais sudo; .env com senha CUPS |
| LAN REDACTED_LAN → :631 | Exposição CUPS restrita por ACL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Elevation | bootstrap-vm.sh sudo | mitigate | Script não eleva sozinho; operador com sudo explícito |
| T-01-12 | Information Disclosure | .env na VM | mitigate | .env gitignored; runbook alerta senha default |
</threat_model>

<verification>
Gate: bootstrap-vm.sh + verify-vm-network.sh existem; checkpoint humano confirma CUPS operacional em VM_HOST.
</verification>

<success_criteria>
- DEPLOY-01: docker compose up funciona na VM Ubuntu 22.04 real
- SERVER-01: porta 631 acessível na rede REDACTED_IP/16
- SERVER-03: test_printer cadastrada na VM
- Bloqueio B-01-04-VM resolvido — 01-04 Task 3 pode retomar
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-print-server/01-05-SUMMARY.md` when done
</output>
