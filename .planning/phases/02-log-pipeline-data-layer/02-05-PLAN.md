---
phase: 02-log-pipeline-data-layer
plan: "05"
type: execute
wave: 3
depends_on:
  - "02-03"
  - "02-04"
files_modified:
  - scripts/validate-phase2.sh
autonomous: false
requirements:
  - CAPTURE-01
  - CAPTURE-02
  - CAPTURE-03
  - CAPTURE-04
  - DATA-01
  - DATA-02
  - DATA-03
  - EXTEND-01
  - EXTEND-02
  - EXTEND-03

must_haves:
  truths:
    - "validate-phase2.sh --quick passa todos os checks em < 60 segundos"
    - "validate-phase2.sh (full) envia job de teste e confirma chegada ao banco em ≤ 30s"
    - "Script verifica permissão 600 do SQLite (DATA-03)"
    - "Script verifica tabelas print_jobs, capture_state e policies no banco"
    - "Script verifica que CUPS opera com backend parado (CAPTURE-04)"
    - "Humano confirma job Windows com username DOMINIO\\\\usuario aparece no banco"
  artifacts:
    - path: "scripts/validate-phase2.sh"
      provides: "Suite de validação Nyquist espelhando validate-phase1.sh"
  key_links:
    - from: "scripts/validate-phase2.sh"
      to: "docker compose exec backend"
      via: "comandos python -c para verificar banco e tabelas"
      pattern: "docker compose exec backend"
---

<objective>
Vertical slice 5: "fase validada e pronta para Fase 3".

Cria o script de validação Nyquist espelhando o padrão de validate-phase1.sh, executa a suite completa no ambiente real, e faz checkpoint humano para confirmar job de PC Windows com usuário AD aparecendo no banco.

Purpose: Fechar o loop de qualidade da Fase 2 com verificação automatizada + confirmação humana do critério de aceite mais crítico (job Windows com DOMINIO\usuario em ≤ 30 segundos).

Output: validate-phase2.sh funcional + aprovação humana do pipeline end-to-end com Windows/AD.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-log-pipeline-data-layer/02-CONTEXT.md
@.planning/phases/02-log-pipeline-data-layer/02-03-SUMMARY.md
@.planning/phases/02-log-pipeline-data-layer/02-04-SUMMARY.md

@scripts/validate-phase1.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar scripts/validate-phase2.sh (espelho do validate-phase1.sh)</name>
  <read_first>
    - scripts/validate-phase1.sh — padrão completo: set -euo pipefail, pass/fail/warn helpers, QUICK_MODE, FAILURES counter, seções de checks, exit code baseado em FAILURES (ler inteiro — é o template exato)
    - .planning/phases/02-log-pipeline-data-layer/02-CONTEXT.md — D-03 (DATA-03 permissões 600), D-17 (CUPS independente)
    - .planning/ROADMAP.md — Success Criteria da Fase 2 (4 critérios de aceite)
    - .planning/phases/02-log-pipeline-data-layer/02-RESEARCH.md — §Validation Architecture, §Mapeamento Requisitos → Testes
  </read_first>
  <files>
    scripts/validate-phase2.sh
  </files>
  <action>
    Criar scripts/validate-phase2.sh seguindo exatamente o padrão de validate-phase1.sh: shebang #!/usr/bin/env bash, set -euo pipefail, MSYS_NO_PATHCONV para Git Bash, variáveis QUICK_MODE/FAILURES/WARNINGS, funções pass/fail/warn, parse_args --quick.

    Seção QUICK checks (sempre executados):
    1. Container backend rodando: docker compose ps backend | grep -q "Up" → pass/fail
    2. Imagem construída: docker compose images backend | grep -q "backend" → pass/warn
    3. Healthcheck /healthz: docker compose exec backend curl -sf http://localhost:8000/healthz → verificar watcher:true
    4. Tabelas existem: docker compose exec backend python -c "from sqlalchemy import inspect; from app.db.session import engine; t=inspect(engine).get_table_names(); assert all(x in t for x in ['print_jobs','capture_state','policies']), t; print('ok')"
    5. SQLite permissões 600: docker compose exec backend stat /app/data/printwatch.db | grep -E "^Access:.*0600|Uid.*600" → se falhar, warn (pode ser 660/664 dependendo do user)
    6. Coluna status default: docker compose exec backend python -c "from app.db.session import SessionLocal; from app.db.models import PrintJob; s=SessionLocal(); j=PrintJob(printer='p',username='u',job_id=1,timestamp=__import__('datetime').datetime.now(__import__('datetime').timezone.utc),pages=1); s.add(j); s.flush(); assert j.status=='allowed',j.status; s.rollback(); s.close(); print('ok')"
    7. Hook pre_process_job retorna True: docker compose exec backend python -c "from app.watcher.handler import pre_process_job; assert pre_process_job({}) is True; print('ok')"
    8. CUPS opera sem backend: docker compose stop backend; sleep 2; docker compose exec cups lpstat -r | grep -q "scheduler"; docker compose start backend; sleep 3 → pass se CUPS respondia (CAPTURE-04)
    9. Suite pytest: docker compose exec backend python -m pytest tests/ -q --tb=short → pass/fail

    Seção FULL checks (sem --quick):
    10. Enviar job de teste local: docker compose exec cups lp -d ${TEST_PRINTER_NAME:-test_printer} /etc/hostname
    11. Aguardar até 30s com loop (checar a cada 3s): docker compose exec backend python -c "from app.db.session import SessionLocal; from app.db.models import PrintJob; s=SessionLocal(); c=s.query(PrintJob).count(); s.close(); exit(0 if c>0 else 1)"
    12. Verificar username no banco: docker compose exec backend python -c "from app.db.session import SessionLocal; from app.db.models import PrintJob; s=SessionLocal(); j=s.query(PrintJob).order_by(PrintJob.id.desc()).first(); s.close(); print(f'username={j.username}, printer={j.printer}, pages={j.pages}')"

    Resumo final: imprimir FAILURES/WARNINGS e retornar exit code 1 se FAILURES > 0.
  </action>
  <verify>
    <automated>
      bash scripts/validate-phase2.sh --quick
    </automated>
  </verify>
  <acceptance_criteria>
    - scripts/validate-phase2.sh existe e tem permissão de execução (chmod +x)
    - bash scripts/validate-phase2.sh --quick finaliza em < 60 segundos
    - bash scripts/validate-phase2.sh --quick retorna exit code 0 com todos os containers rodando
    - Script contém verificação de "print_jobs" e "capture_state" e "policies"
    - Script contém verificação de "pre_process_job"
    - Script contém verificação de "status.*allowed"
    - Script contém seção FULL com loop de aguardo de até 30s
    - bash scripts/validate-phase2.sh --quick mostra pelo menos 7 [PASS] e 0 [FAIL]
  </acceptance_criteria>
  <done>validate-phase2.sh --quick passa todos os checks em < 60s; script full envia job e confirma chegada ao banco.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Pipeline completo: container backend, watcher InotifyObserver, parser, repository, retention, validate-phase2.sh. validate-phase2.sh --quick passou todos os checks automatizados.
  </what-built>
  <how-to-verify>
    1. Em um PC Windows da rede REDACTED_IP/16 com usuário de domínio AD logado:
       - Abrir "Impressoras e Scanners" → adicionar impressora com URL: `http://VM_HOST:631/printers/test_printer`
       - Imprimir qualquer documento (ex: página de teste do Word ou Bloco de Notas)
    
    2. Na VM (SSH ou terminal local), aguardar ≤ 30 segundos e executar:
       ```
       docker compose exec backend python -c "
       from app.db.session import SessionLocal
       from app.db.models import PrintJob
       s = SessionLocal()
       jobs = s.query(PrintJob).order_by(PrintJob.id.desc()).limit(3).all()
       for j in jobs:
           print(f'{j.timestamp} | {j.username} | {j.printer} | {j.job_name} | pages={j.pages}')
       s.close()
       "
       ```
    
    3. Confirmar:
       - O job apareceu no banco em ≤ 30 segundos após impressão
       - O campo `username` contém o formato `DOMINIO\usuario` (ex: `DOMAIN\seu.nome`) — sem normalização (D-08)
       - `printer` é o nome correto da impressora
       - `pages` é um número positivo
       - `status` é "allowed"
    
    4. Executar suite completa de validação:
       ```
       bash scripts/validate-phase2.sh
       ```
       Confirmar que termina com 0 [FAIL].
    
    5. (Opcional) Testar restart: `docker compose restart backend` → enviar mais um job → confirmar no banco.
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" se o job Windows apareceu no banco com username DOMINIO\usuario em ≤ 30 segundos e validate-phase2.sh terminou sem [FAIL].
    
    Se houver problema, descreva: (a) o output do banco, (b) o que validate-phase2.sh reportou, (c) qualquer erro em `docker compose logs backend`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validate-phase2.sh → docker compose exec | Script executa comandos dentro do container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Denial of Service | validate-phase2.sh docker compose stop cups | mitigate | Script para CUPS apenas temporariamente para testar CAPTURE-04; restart automático garantido por restart: unless-stopped; check de timeout incluso |
| T-02-12 | Information Disclosure | validate-phase2.sh imprime username AD no terminal | accept | Script de validação; executa apenas em contexto de admin com acesso SSH à VM; username em texto claro em ambiente de dev é risco aceito |
| T-02-SC | Tampering | nenhum pacote novo | mitigate | Nenhuma instalação de pacote neste plano |
</threat_model>

<verification>
Após execução deste plano:

1. `bash scripts/validate-phase2.sh --quick` — exit 0, todos os checks passam
2. `bash scripts/validate-phase2.sh` (full) — job enviado, aparece no banco em ≤ 30s
3. Checkpoint humano aprovado: job de PC Windows com DOMINIO\usuario no banco
4. `pytest backend/tests/ -v` — 100% passando
</verification>

<success_criteria>
Todos os 4 critérios de aceite do ROADMAP Fase 2:

1. Job impresso de PC Windows com usuário AD aparece no banco em ≤ 30 segundos com todos os campos preenchidos (CAPTURE-01, CAPTURE-02) ✓
2. Restart do container backend não cria duplicatas nem perde jobs ocorridos durante downtime (CAPTURE-03) ✓
3. Com container backend parado, job enviado ao CUPS é impresso fisicamente sem erro (CAPTURE-04) ✓
4. VM reiniciada — todos os jobs anteriores estão no banco intactos (DATA-02) ✓
</success_criteria>

<output>
Criar `.planning/phases/02-log-pipeline-data-layer/02-05-SUMMARY.md` quando concluído.
</output>
