---
phase: 02-log-pipeline-data-layer
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - backend/app/services/retention.py
  - backend/app/services/__init__.py
  - backend/tests/test_retention.py
  - backend/app/main.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "purge_old_jobs deleta registros com timestamp < now() - LOG_RETENTION_DAYS dias (DATA-01)"
    - "purge_old_jobs é chamada no startup do container backend"
    - "purge_old_jobs retorna contagem de registros deletados"
    - "registros dentro do período de retenção NÃO são deletados"
    - "Volume db_data persiste o banco após docker compose restart (DATA-02)"
    - "Testes de retenção passam com SQLite in-memory sem container"
  artifacts:
    - path: "backend/app/services/retention.py"
      provides: "purge_old_jobs(session, retention_days) → int"
      exports: ["purge_old_jobs"]
    - path: "backend/tests/test_retention.py"
      provides: "Testes unitários de purge com timestamps past/current"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/services/retention.py"
      via: "purge_old_jobs chamada no lifespan startup"
      pattern: "purge_old_jobs"
    - from: "backend/app/services/retention.py"
      to: "backend/app/db/models.py"
      via: "delete(PrintJob).where(PrintJob.timestamp < cutoff)"
      pattern: "delete.*PrintJob"
---

<objective>
Vertical slice 4 (paralelo ao Plano 03): "retenção de dados configurável".

Implementa o serviço de purge por LOG_RETENTION_DAYS e o integra ao startup do FastAPI. Após este plano, o container backend aplica purge automático na inicialização, respeitando a janela de retenção configurada.

Purpose: Cumprir DATA-01 (retenção configurável) sem depender de cron do OS host — o purge roda dentro do processo Python.

Output: retention.py testado + integração no lifespan do main.py (via edição do arquivo criado no Plano 03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-log-pipeline-data-layer/02-CONTEXT.md
@.planning/phases/02-log-pipeline-data-layer/02-RESEARCH.md
@.planning/phases/02-log-pipeline-data-layer/02-01-SUMMARY.md

@backend/app/db/models.py
@backend/app/db/session.py
@backend/app/core/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: retention.py com purge_old_jobs + testes</name>
  <read_first>
    - backend/app/db/models.py — PrintJob com campo timestamp DateTime (contrato Plano 01)
    - backend/app/db/session.py — SessionLocal, engine (para fixture in-memory)
    - backend/app/core/config.py — settings.log_retention_days (valor padrão 90)
    - .planning/phases/02-log-pipeline-data-layer/02-RESEARCH.md — §Verificado: Purge por retenção (DATA-01), código de referência com delete(PrintJob).where(PrintJob.timestamp < cutoff)
  </read_first>
  <files>
    backend/app/services/retention.py,
    backend/app/services/__init__.py,
    backend/tests/test_retention.py
  </files>
  <behavior>
    - Test: purge_old_jobs com retenção de 90 dias e 1 registro com timestamp = agora - 91 dias → retorna 1, registro deletado
    - Test: purge_old_jobs com retenção de 90 dias e 1 registro com timestamp = agora - 89 dias → retorna 0, registro mantido
    - Test: purge_old_jobs com banco vazio → retorna 0
    - Test: purge_old_jobs deleta múltiplos registros antigos de uma vez e mantém os recentes
    - Todos os testes usam SQLite in-memory com timezone-aware datetimes (datetime.now(tz=timezone.utc))
  </behavior>
  <action>
    Criar backend/app/services/__init__.py vazio (se não existir do Plano 02).

    Criar backend/app/services/retention.py com função purge_old_jobs(session, retention_days: int) -> int. Usar datetime.now(tz=timezone.utc) - timedelta(days=retention_days) como cutoff. Executar session.execute(delete(PrintJob).where(PrintJob.timestamp < cutoff)); session.commit(). Retornar result.rowcount. Adicionar logging.info com contagem deletada.

    Criar backend/tests/test_retention.py com fixture engine_in_memory (mesmo padrão de test_repository.py: SQLite in-memory + Base.metadata.create_all). Inserir registros com timestamps calculados via datetime.now(tz=timezone.utc) - timedelta(days=N). Testar todos os 4 casos acima.
  </action>
  <verify>
    <automated>
      cd backend && pytest tests/test_retention.py -v
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/retention.py contém "def purge_old_jobs"
    - backend/app/services/retention.py contém "timezone.utc" (timestamps timezone-aware — pitfall 6 do RESEARCH.md)
    - backend/app/services/retention.py contém "delete(PrintJob)"
    - pytest tests/test_retention.py -v mostra 0 failed
    - Teste de registro antigo: count() após purge retorna 0 para registro 91 dias atrás com retention_days=90
    - Teste de registro recente: count() após purge retorna 1 para registro 89 dias atrás com retention_days=90
  </acceptance_criteria>
  <done>purge_old_jobs testado com SQLite in-memory; deleta somente registros fora da janela de retenção.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrar purge no lifespan do FastAPI (main.py) + verificar DATA-02</name>
  <read_first>
    - backend/app/main.py — lifespan existente do Plano 03 (adicionar chamada de purge no startup)
    - backend/app/core/config.py — settings.log_retention_days
    - backend/app/services/retention.py — purge_old_jobs (Task 1 deste plano)
    - .planning/phases/02-log-pipeline-data-layer/02-CONTEXT.md — D-16 (LOG_RETENTION_DAYS, purge no startup)
  </read_first>
  <files>
    backend/app/main.py
  </files>
  <action>
    Editar backend/app/main.py: no início do bloco lifespan (antes do yield), após criar a sessão, chamar purge_old_jobs(session, settings.log_retention_days) e logar o resultado. Abrir sessão via context manager SessionLocal(), chamar purge, fechar. Não alterar nada além desta adição no lifespan.

    Verificar que docker-compose.yml já contém volume db_data mapeado para /app/data (criado no Plano 01). Não modificar docker-compose.yml neste plano.
  </action>
  <verify>
    <automated>
      docker compose restart backend
      sleep 5
      docker compose logs backend 2>&1 | grep -i "purge\|retention\|deleted\|registros" | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/app/main.py contém "purge_old_jobs" e "log_retention_days"
    - docker compose logs backend após restart mostra linha de log sobre purge (mesmo que "0 registros deletados")
    - docker compose exec backend curl -s http://localhost:8000/healthz retorna {"status":"ok","watcher":true} (pipeline não quebrado pela adição do purge)
    - docker compose restart → docker compose exec backend python -c "from app.db.session import SessionLocal; from app.db.models import PrintJob; s=SessionLocal(); c=s.query(PrintJob).count(); s.close(); print(c)" — retorna mesma contagem de antes do restart (DATA-02: nenhum registro perdido)
  </acceptance_criteria>
  <done>Purge automático no startup do container; registros anteriores preservados após restart (DATA-02 verificado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LOG_RETENTION_DAYS env var → purge cutoff | Valor de env var controla qual período de dados é deletado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | purge_old_jobs cutoff com timezone naive | mitigate | Usar datetime.now(tz=timezone.utc) — timezone-aware previne purge incorreto por offset de fuso (pitfall 6 do RESEARCH.md) |
| T-02-10 | Denial of Service | purge deletando dados recentes por erro de config | accept | MVP: LOG_RETENTION_DAYS=90 default; admin controla via .env; sem UI de confirmação no MVP — risco aceito para rede interna |
| T-02-SC | Tampering | pip install | mitigate | Nenhum pacote novo neste plano; todos já verificados no Package Legitimacy Audit |
</threat_model>

<verification>
Após execução deste plano:

1. `cd backend && pytest tests/ -v` — todos os testes passando (incluindo test_retention.py)
2. `docker compose restart backend && docker compose logs backend` — mostra linha de purge no startup
3. `docker compose exec backend curl -s http://localhost:8000/healthz` — retorna watcher:true
4. Volume db_data: `docker compose down && docker compose up -d` — banco preservado, query de count retorna mesma contagem
</verification>

<success_criteria>
- purge_old_jobs testado: registro 91 dias → deletado; registro 89 dias → preservado
- Purge chamado automaticamente no startup (verificado em logs)
- Nenhum registro perdido após docker compose restart (DATA-02)
- Suite de testes completa continua passando
</success_criteria>

<output>
Criar `.planning/phases/02-log-pipeline-data-layer/02-04-SUMMARY.md` quando concluído.
</output>
