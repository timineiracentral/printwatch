---
phase: 03-backend-api
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/normalization.py
  - backend/app/services/parser.py
  - backend/scripts/__init__.py
  - backend/scripts/backfill_printer_quotes.py
  - backend/tests/test_normalization.py
  - backend/tests/test_parser.py
  - .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "Função normalize_printer_name(raw) é idempotente: normalize(normalize(x)) == normalize(x) para todo input"
    - "parser.parse_page_log_line aplica normalize_printer_name no campo printer — dados novos entram limpos no banco"
    - "Script backfill_printer_quotes é idempotente: rodar 2 vezes seguidas, a segunda execução faz 0 UPDATEs"
    - "Investigação observacional registrada em .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md ANTES de codificar o normalizador (D-22 mandatório)"
    - "Teste de regressão em test_parser.py usa linha bruta capturada do page_log real (não inventada)"
    - "GAP-02-01-parser-printer-quote.md movido para .planning/todos/resolved/ ao final do plano"
  artifacts:
    - path: "backend/app/services/normalization.py"
      provides: "normalize_printer_name(raw: str | None) -> str | None — strip + remove aspas extremas idempotentemente"
      exports: ["normalize_printer_name"]
    - path: "backend/app/services/parser.py"
      provides: "parse_page_log_line aplicando normalize_printer_name ao campo printer"
    - path: "backend/scripts/backfill_printer_quotes.py"
      provides: "Script standalone idempotente que limpa linhas existentes em print_jobs"
    - path: "backend/tests/test_normalization.py"
      provides: "5+ casos cobrindo: aspa só no início, aspas duplas extremas, aspas simples, input None, input já limpo (no-op), idempotência"
    - path: "backend/tests/test_parser.py"
      provides: "Teste de regressão usando linha bruta capturada (P-3-quoted)"
    - path: ".planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md"
      provides: "Comandos rodados, output bruto da linha problemática, classificação da causa (a/b/c), decisão de fix"
  key_links:
    - from: "backend/app/services/parser.py"
      to: "backend/app/services/normalization.py"
      via: "from app.services.normalization import normalize_printer_name + chamada em parse_page_log_line"
      pattern: "normalize_printer_name\\(m\\.group\\(1\\)\\)"
    - from: "backend/scripts/backfill_printer_quotes.py"
      to: "backend/app/db/session.py"
      via: "Reutiliza SessionLocal — sem segunda camada de conexão"
      pattern: "from app.db.session import SessionLocal"
---

<objective>
Vertical slice "GAP-02-01: parser captura aspa em printer — bug eliminado em dado novo + retroativo".

Resolve o gap de qualidade de dado herdado da Fase 2 (D-22 do CONTEXT.md) em três passos:
1. **Investigação observacional ANTES de codificar** — capturar a linha bruta do `page_log` real para identificar a causa.
2. **Fix prospectivo** — função `normalize_printer_name` idempotente aplicada no parser, garantindo que dados NOVOS entram limpos.
3. **Fix retroativo** — script standalone idempotente que limpa as linhas afetadas no volume `db_data` existente.

Purpose: API e CSV consomem o campo `printer` em filtros (exact match — D-09) e em chave de agregação (D-04). Sem este fix, GET /api/v1/jobs?printer=test_printer retornaria 0 resultados para jobs já gravados.

Output: Após o plano, (a) novos jobs gravados via watcher contêm `printer` sem aspas; (b) jobs antigos no volume tiveram suas aspas removidas via backfill; (c) suite pytest cobre regressão com a linha bruta capturada; (d) GAP-02-01 movido para resolved.

**autonomous: false** — Task 1 exige interação com a VM real (VM_HOST) para capturar a linha bruta. O executor humano deve fornecer o output do `tail` antes de codificar o normalizador.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-backend-api/03-CONTEXT.md
@.planning/phases/03-backend-api/03-RESEARCH.md
@.planning/todos/pending/GAP-02-01-parser-printer-quote.md
@.planning/phases/02-log-pipeline-data-layer/02-VERIFICATION.md
@backend/app/services/parser.py
@backend/app/db/session.py
@backend/app/db/models.py
@backend/tests/test_parser.py
</context>

<tasks>

<task type="manual" tdd="false">
  <name>Task 1: Investigação observacional — capturar linha bruta do page_log e classificar causa</name>
  <read_first>
    - .planning/todos/pending/GAP-02-01-parser-printer-quote.md — hipóteses (a) linha externa, (b) job-name interno, (c) printers.conf
    - .planning/phases/03-backend-api/03-RESEARCH.md §3.1 (investigação observacional D-22)
    - .planning/phases/02-log-pipeline-data-layer/02-VERIFICATION.md — referência ao job_id 4 com printer=`"test_printer`
    - backend/app/services/parser.py — PAGE_LOG_REGEX atual `r"^(\S+)\s+(\S+)\s+..."`
  </read_first>
  <files>
    .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md
  </files>
  <action>
    NÃO escrever código nesta task. Apenas investigar e documentar.

    Conectar à VM `VM_HOST` via SSH (`ssh admin-user@VM_HOST`) e executar:

    1. `docker compose exec cups tail -n 50 /var/log/cups/page_log` — capturar o output completo das últimas 50 linhas (preferir uma linha cujo job tenha o `printer = "test_printer` no banco — referência job_id 4 do `02-VERIFICATION.md`).
    2. `docker compose exec cups grep -c '^"' /var/log/cups/page_log || true` — contar linhas que começam com aspa.
    3. `docker compose exec cups cat /etc/cups/printers.conf | grep -E "^<|^Info|^DeviceURI"` — verificar se o nome da impressora em printers.conf tem aspas (hipótese c).

    Criar `.planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md` com:
    - **Cabeçalho:** ID do gap, data, executor, ambiente (VM IP, container CUPS).
    - **Seção "Comandos executados":** colar comandos exatos com timestamp.
    - **Seção "Output bruto":** colar **uma linha completa** do page_log que reproduz o bug (com `printer` começando com aspa) dentro de bloco ` ``` `. Esta linha será reutilizada no teste de regressão (Task 4).
    - **Seção "Classificação da causa":** marcar uma das opções com `[x]`:
      - `[ ] (a) Linha completa do page_log tem aspas externas`
      - `[ ] (b) Aspa do %{job-name} (campo com espaço) vaza no regex para %p`
      - `[ ] (c) printers.conf armazena o nome com aspas`
      - `[ ] (d) Outra — descrever`
    - **Seção "Decisão de fix":** confirmar que o approach do RESEARCH.md §3.2/§3.3 (normalize_printer_name idempotente aplicada no parser + backfill) cobre a causa observada.

    Se a classificação for (c), abrir tarefa adicional no escopo (fora deste plano): corrigir printers.conf via `cupsctl` ou `lpadmin -p` — mas para o presente plano, o normalizer ainda elimina o sintoma no parser+banco, então o plano segue.

    **Não prosseguir para Task 2 sem o documento de investigação fechado e a linha bruta capturada.**
  </action>
  <verify>
    <automated>
      test -f .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md
      grep -E "^\[x\] \(.\)" .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md
      grep -E "^\`\`\`" .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md
    </automated>
    <manual>
      Operador SSH na VM, executa tail e grep, cola output no documento, marca causa.
    </manual>
  </verify>
  <acceptance_criteria>
    - .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md existe
    - Documento contém ao menos uma linha bruta do page_log dentro de um bloco de código (` ``` `)
    - Documento contém marca `[x]` em exatamente uma das opções (a)/(b)/(c)/(d)
    - A linha bruta capturada contém o caractere `"` no campo printer (caso reproduzível) — confirmando o bug
    - Documento referencia explicitamente o normalizer + backfill como fix escolhido (link para §3.2/§3.3 do RESEARCH)
  </acceptance_criteria>
  <done>Causa do bug classificada com evidência bruta; linha de referência capturada para o teste de regressão da Task 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD — Criar tests/test_normalization.py com casos cobrindo idempotência e edge cases</name>
  <read_first>
    - backend/tests/test_parser.py — padrão pytest existente
    - backend/tests/conftest.py — fixtures disponíveis
    - .planning/phases/03-backend-api/03-RESEARCH.md §3.5 (casos de teste)
    - .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md — linha bruta para usar como caso real (referência)
  </read_first>
  <files>
    backend/tests/test_normalization.py
  </files>
  <action>
    **Gate RED:** Criar `backend/tests/test_normalization.py` ANTES de implementar `normalize_printer_name`. A suite pytest deve **falhar** com `ImportError` no primeiro `from app.services.normalization import normalize_printer_name`.

    Casos de teste mínimos (6):
    1. `test_normalize_strips_leading_double_quote` — input `'"test_printer'` → `'test_printer'`
    2. `test_normalize_strips_paired_double_quotes` — input `'"test_printer"'` → `'test_printer'`
    3. `test_normalize_strips_paired_single_quotes` — input `"'test_printer'"` → `'test_printer'`
    4. `test_normalize_strips_whitespace` — input `'  test_printer  '` → `'test_printer'`
    5. `test_normalize_returns_none_for_none` — input `None` → `None`
    6. `test_normalize_idempotent` — para cada caso 1–4, asseverar `normalize(normalize(x)) == normalize(x)`
    7. `test_normalize_preserves_clean_input` — input `'test_printer'` → `'test_printer'` (no-op)

    **Gate verificação RED:** executar `docker compose exec backend pytest backend/tests/test_normalization.py -q` — esperado `ImportError` ou todos os testes falhando.
  </action>
  <verify>
    <automated>
      # Gate RED: rodar antes da Task 3
      docker compose exec backend pytest backend/tests/test_normalization.py -q 2>&1 | tail -10
      # Esperado: ImportError ou FAILED (módulo normalization ainda não existe)
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_normalization.py contém ao menos 7 funções de teste cobrindo os casos listados
    - backend/tests/test_normalization.py contém asserção de idempotência (`normalize(normalize(x)) == normalize(x)`)
    - Antes de Task 3: `pytest backend/tests/test_normalization.py` falha (ImportError)
  </acceptance_criteria>
  <done>Suite RED criada e falhando — pronta para implementação da Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TDD GREEN — Implementar normalize_printer_name + integrar no parser</name>
  <read_first>
    - backend/tests/test_normalization.py — Task 2 recém-criada (especificação executável)
    - backend/app/services/parser.py — PAGE_LOG_REGEX + parse_page_log_line atual
    - .planning/phases/03-backend-api/03-RESEARCH.md §3.2 (implementação do normalizer), §3.3 (integração no parser)
  </read_first>
  <files>
    backend/app/services/normalization.py,
    backend/app/services/parser.py
  </files>
  <action>
    **GREEN — implementar exatamente o suficiente para passar os testes da Task 2:**

    1. Criar `backend/app/services/normalization.py`:
       ```python
       from __future__ import annotations
       from typing import Optional

       def normalize_printer_name(raw: Optional[str]) -> Optional[str]:
           if raw is None:
               return None
           s = raw.strip()
           # Remove pares de aspas extremas idempotentemente (D-22 idempotência).
           while len(s) >= 2 and s[0] in ('"', "'") and s[0] == s[-1]:
               s = s[1:-1].strip()
           # Caso degenerado: aspa só no início (sem fechamento) — sintoma real observado.
           s = s.lstrip('"').lstrip("'").strip()
           return s
       ```

    2. Editar `backend/app/services/parser.py`:
       - Adicionar `from app.services.normalization import normalize_printer_name` no topo.
       - No retorno de `parse_page_log_line`, substituir `"printer": m.group(1),` por `"printer": normalize_printer_name(m.group(1)),`.
       - Não alterar PAGE_LOG_REGEX (o fix é no pós-processamento; ajustar regex é refator separado e não-prioritário — D-22 escolheu strip).

    **REFACTOR (curto):** garantir que a docstring de `normalize_printer_name` documente: idempotência, motivação (GAP-02-01), exemplos.

    Verificar:
    - `pytest backend/tests/test_normalization.py` — todos os 7 testes verdes
    - `pytest backend/tests/test_parser.py` — testes existentes continuam verdes (sem regressão na Fase 2)
  </action>
  <verify>
    <automated>
      docker compose exec backend pytest backend/tests/test_normalization.py -q
      # Esperado: 7 passed

      docker compose exec backend pytest backend/tests/test_parser.py -q
      # Esperado: todos os testes existentes ainda passam
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/normalization.py contém `def normalize_printer_name(raw: Optional[str]) -> Optional[str]`
    - backend/app/services/normalization.py contém docstring mencionando "idempotent" ou "idempotência" e "GAP-02-01"
    - backend/app/services/parser.py contém `from app.services.normalization import normalize_printer_name`
    - backend/app/services/parser.py contém `normalize_printer_name(m.group(1))` na construção do dict de retorno
    - `pytest backend/tests/test_normalization.py` → 7 passed
    - `pytest backend/tests/test_parser.py` → continua todos passing (sem regressão)
  </acceptance_criteria>
  <done>normalize_printer_name implementado, plugado no parser, suite RED virou GREEN, parser tests sem regressão.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Teste de regressão com linha bruta capturada na investigação</name>
  <read_first>
    - .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md — linha bruta capturada (Task 1)
    - backend/tests/test_parser.py — estrutura existente
    - .planning/phases/03-backend-api/03-RESEARCH.md §3.5 (template do teste)
  </read_first>
  <files>
    backend/tests/test_parser.py
  </files>
  <action>
    Adicionar ao final de `backend/tests/test_parser.py` um teste de regressão usando a linha bruta capturada no documento de investigação (Task 1):

    ```python
    def test_parser_strips_printer_quote_regression_gap_02_01():
        # Linha bruta capturada de /var/log/cups/page_log na VM VM_HOST
        # Ver: .planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md
        raw_line = '<COLAR-AQUI-A-LINHA-EXATA-DA-INVESTIGACAO>'
        parsed = parse_page_log_line(raw_line)
        assert parsed is not None, "Regex deve casar com linha real do page_log"
        assert parsed["printer"] == "test_printer", (
            f"GAP-02-01: printer deve ser limpo de aspas. Got: {parsed['printer']!r}"
        )
        assert not parsed["printer"].startswith('"'), "Não pode começar com aspa"
        assert not parsed["printer"].startswith("'"), "Não pode começar com apóstrofo"
    ```

    O placeholder `<COLAR-AQUI-A-LINHA-EXATA-DA-INVESTIGACAO>` deve ser substituído pela linha real do documento de investigação. Se a linha real contiver aspas duplas, escapar para string Python (`r"..."` ou `'...'` com aspas alternadas).

    Verificar:
    - `pytest backend/tests/test_parser.py::test_parser_strips_printer_quote_regression_gap_02_01 -q` → PASSED
    - Sem o normalizer (revert teórico), este teste falharia — confirmando que ele é uma regressão real.
  </action>
  <verify>
    <automated>
      docker compose exec backend pytest backend/tests/test_parser.py::test_parser_strips_printer_quote_regression_gap_02_01 -v
      # Esperado: PASSED

      # Sanity check — todos os tests do parser
      docker compose exec backend pytest backend/tests/test_parser.py -q
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_parser.py contém função `def test_parser_strips_printer_quote_regression_gap_02_01`
    - O teste **NÃO** contém o placeholder literal `<COLAR-AQUI-A-LINHA-EXATA-DA-INVESTIGACAO>` (deve ter sido substituído pela linha real)
    - O teste contém asserção `parsed["printer"] == "test_printer"`
    - O teste contém comentário referenciando `03-INVESTIGATION-printer-quote.md`
    - `pytest backend/tests/test_parser.py::test_parser_strips_printer_quote_regression_gap_02_01` → PASSED
    - `pytest backend/tests/test_parser.py` → todos passing (incluindo o novo)
  </acceptance_criteria>
  <done>Teste de regressão real (com linha capturada da VM) verde — fix prospectivo provado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Script standalone de backfill idempotente para limpar print_jobs existente</name>
  <read_first>
    - backend/app/services/normalization.py — função criada na Task 3 (lógica de strip)
    - backend/app/db/session.py — SessionLocal disponível
    - backend/app/db/models.py — modelo PrintJob (coluna printer)
    - .planning/phases/03-backend-api/03-RESEARCH.md §3.4 (backfill script standalone, NÃO no lifespan)
  </read_first>
  <files>
    backend/scripts/__init__.py,
    backend/scripts/backfill_printer_quotes.py
  </files>
  <action>
    Criar `backend/scripts/__init__.py` vazio (package marker).

    Criar `backend/scripts/backfill_printer_quotes.py` com:

    - Importações: `logging`, `SessionLocal` de `app.db.session`, `PrintJob` de `app.db.models`, `normalize_printer_name` de `app.services.normalization`, `select`, `update` de `sqlalchemy`.
    - Função `main()` que:
      1. Abre sessão via `SessionLocal()`.
      2. Conta linhas afetadas: `SELECT COUNT(*) FROM print_jobs WHERE printer LIKE '"%' OR printer LIKE '\'%' OR printer LIKE '%"' OR printer LIKE '%\''` — armazena em `before`.
      3. Se `before == 0`: log `"backfill: nenhuma linha afetada — nothing to do"` e retorna 0.
      4. Caso contrário: itera linhas afetadas (`session.execute(select(PrintJob).where(...))`), para cada uma calcula `new_printer = normalize_printer_name(row.printer)` e se `new_printer != row.printer`, faz UPDATE em batch (acumular IDs e fazer um único `session.execute(update(PrintJob).where(PrintJob.id.in_(batch)).values(printer=new_value))` — ou um por linha se simplicidade for prioritária a 50k linhas).
      5. Commit.
      6. Conta novamente para verificar: `after` deve ser 0.
      7. Log `f"backfill: before={before} after={after} fixed={before-after}"`.
      8. Retorna `before - after`.

    - Bloco `if __name__ == "__main__":` chamando `main()`.
    - `logging.basicConfig(level=logging.INFO)` no topo do `__main__` (consistente com Fase 2).

    Idempotência garantida: depois da primeira execução, `before == 0` e o script sai sem alterar nada. Rodar 2 vezes seguidas → segunda execução loga `"nothing to do"`.

    Notas:
    - **NÃO** chamar este script no `lifespan` — operação de migração de dado é manual e auditável (D-22, R-5 do RESEARCH).
    - Adicionar comentário no topo: `"""Backfill GAP-02-01: limpa aspas em print_jobs.printer existentes. Idempotente. Rodar manualmente após deploy do fix:\n  docker compose exec backend python -m scripts.backfill_printer_quotes\n"""`.
  </action>
  <verify>
    <automated>
      docker compose exec backend python -m scripts.backfill_printer_quotes 2>&1 | tee /tmp/backfill1.log
      docker compose exec backend python -m scripts.backfill_printer_quotes 2>&1 | tee /tmp/backfill2.log
      # Esperado: segunda execução loga "nothing to do" ou "fixed=0"
      grep -E "before=|nothing to do" /tmp/backfill2.log
      docker compose exec backend python -c "
      from app.db.session import SessionLocal
      from sqlalchemy import text
      s = SessionLocal()
      n = s.execute(text(\"SELECT COUNT(*) FROM print_jobs WHERE printer LIKE '\\\"%' OR printer LIKE '%\\\"'\")).scalar()
      print(f'remaining_dirty={n}')
      assert n == 0, n
      s.close()
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/scripts/__init__.py existe (pode ser vazio)
    - backend/scripts/backfill_printer_quotes.py contém docstring mencionando "Idempotente" e "GAP-02-01"
    - backend/scripts/backfill_printer_quotes.py contém `from app.services.normalization import normalize_printer_name`
    - backend/scripts/backfill_printer_quotes.py contém `from app.db.session import SessionLocal`
    - backend/scripts/backfill_printer_quotes.py NÃO contém referência a `lifespan` (não roda no startup automático)
    - Primeira execução: `python -m scripts.backfill_printer_quotes` sai 0; loga `before=N after=0 fixed=N` com N > 0 SE houver linhas sujas; ou `before=0 nothing to do` se não houver.
    - Segunda execução consecutiva: loga `before=0` ou equivalente (idempotência).
    - Após primeira execução, `SELECT COUNT(*) FROM print_jobs WHERE printer LIKE '"%' OR printer LIKE '%"'` retorna 0.
  </acceptance_criteria>
  <done>Script standalone idempotente limpou (ou confirmou limpo) o volume db_data existente; segunda execução é no-op.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Mover GAP-02-01 para resolved/ e atualizar STATE.md</name>
  <read_first>
    - .planning/todos/pending/GAP-02-01-parser-printer-quote.md — arquivo atual
    - .planning/STATE.md — lista de "Gaps Abertos"
  </read_first>
  <files>
    .planning/todos/pending/GAP-02-01-parser-printer-quote.md (deletado),
    .planning/todos/resolved/GAP-02-01-parser-printer-quote.md (criado),
    .planning/STATE.md
  </files>
  <action>
    1. Criar `.planning/todos/resolved/` se não existir.
    2. Mover (rename) `.planning/todos/pending/GAP-02-01-parser-printer-quote.md` para `.planning/todos/resolved/GAP-02-01-parser-printer-quote.md`.
    3. No arquivo movido, atualizar o frontmatter:
       - `status: pending` → `status: resolved`
       - Adicionar campo `resolved: "<data ISO>"` e `resolved_in: 03-02`.
    4. Adicionar seção `## Resolução` ao final do arquivo:
       - Referenciar `03-INVESTIGATION-printer-quote.md` (causa observada).
       - Referenciar `app/services/normalization.py` (fix prospectivo).
       - Referenciar `scripts/backfill_printer_quotes.py` (fix retroativo).
       - Referenciar `tests/test_normalization.py` e `tests/test_parser.py::test_parser_strips_printer_quote_regression_gap_02_01`.
    5. Atualizar `.planning/STATE.md`:
       - Tabela "Gaps Abertos": remover linha `GAP-02-01`.
       - Decrementar `gaps_open` no YAML frontmatter de `2` para `1`.
       - Adicionar bullet em "Decisões Registradas": `**2026-XX-XX (Fase 3 plan 02):** GAP-02-01 resolvido — normalize_printer_name no parser + backfill idempotente; investigação documentada em 03-INVESTIGATION-printer-quote.md.`
  </action>
  <verify>
    <automated>
      test ! -f .planning/todos/pending/GAP-02-01-parser-printer-quote.md
      test -f .planning/todos/resolved/GAP-02-01-parser-printer-quote.md
      grep -E "^status: resolved" .planning/todos/resolved/GAP-02-01-parser-printer-quote.md
      grep -E "^resolved_in:.*03-02" .planning/todos/resolved/GAP-02-01-parser-printer-quote.md
      grep -c "GAP-02-01" .planning/STATE.md
      grep "gaps_open: 1" .planning/STATE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - .planning/todos/pending/GAP-02-01-parser-printer-quote.md NÃO existe
    - .planning/todos/resolved/GAP-02-01-parser-printer-quote.md existe
    - O arquivo movido contém `status: resolved` no frontmatter
    - O arquivo movido contém `resolved_in: 03-02` no frontmatter
    - O arquivo movido contém seção `## Resolução` referenciando os 4 artefatos do fix
    - .planning/STATE.md frontmatter contém `gaps_open: 1` (decrementado de 2)
    - .planning/STATE.md tabela "Gaps Abertos" NÃO contém linha com `GAP-02-01`
    - .planning/STATE.md contém entrada nova em "Decisões Registradas" referenciando "Fase 3 plan 02" e o fix
  </acceptance_criteria>
  <done>Gap fechado, documentado e movido — STATE.md reflete redução de 2 para 1 gap aberto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Linha bruta page_log → parser | Conteúdo controlado pelo CUPS (não pelo cliente Windows) — confiança média. |
| Script backfill → volume db_data | Operação destrutiva (UPDATE) sobre dados de produção (Fase 2). Idempotência + guard `before==0` mitigam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | backfill UPDATE em volume db_data | mitigate | Idempotência garantida: rodar 2x é no-op. Guard `before==0` → no-op. Backup do volume é responsabilidade do operador (DEPLOY-03 Fase 5). |
| T-03-02-02 | DoS | backfill rodando em 50k linhas — pode demorar | accept | Op manual (não no lifespan). Em SQLite + UPDATE com índice = instantâneo (< 1s para 50k). |
| T-03-02-03 | Information Disclosure | Investigação coletando linhas reais com username AD | mitigate | Documento de investigação fica em `.planning/` (gitignored se contiver PII sensível — verificar antes de commit). |
| T-03-02-04 | Spoofing | normalize_printer_name pode mascarar nomes legítimos de printers que naturalmente contêm aspas | accept | Cenário implausível (aspas em nome de impressora). Caso ocorra, o normalizer mantém o miolo sem as aspas — comportamento aceitável. |
</threat_model>

<verification>
Após execução deste plano:

1. `pytest backend/tests/test_normalization.py` — 7+ passed
2. `pytest backend/tests/test_parser.py` — todos passed incluindo o teste de regressão
3. `python -m scripts.backfill_printer_quotes` — primeira vez: limpa N linhas; segunda vez: `before=0 nothing to do`
4. `docker compose exec backend python -c "from app.db.session import SessionLocal; from sqlalchemy import text; s=SessionLocal(); n=s.execute(text(\"SELECT COUNT(*) FROM print_jobs WHERE printer LIKE '\\\"%' OR printer LIKE '%\\\"' OR printer LIKE '\\'%' OR printer LIKE '%\\''\")).scalar(); assert n==0, n; print('clean')"` → "clean"
5. Imprimir um novo job (ou simular via pytest com page_log fake) e verificar que `print_jobs.printer` entra sem aspas
6. `.planning/todos/resolved/GAP-02-01-parser-printer-quote.md` existe com `status: resolved`
7. `.planning/STATE.md` frontmatter `gaps_open: 1`
8. `.planning/phases/03-backend-api/03-INVESTIGATION-printer-quote.md` existe com linha bruta capturada
</verification>

<success_criteria>
- Dados NOVOS gravados pelo watcher têm `printer` sem aspas (fix prospectivo via parser)
- Dados ANTIGOS no volume db_data foram limpos pelo backfill (fix retroativo)
- Script backfill é idempotente (rodar N vezes é seguro)
- Causa do bug documentada com evidência bruta (não inventada)
- Teste de regressão usa linha real capturada da VM — previne re-introdução
- GAP-02-01 movido para resolved e contabilizado em STATE.md
</success_criteria>

<output>
Criar `.planning/phases/03-backend-api/03-02-SUMMARY.md` quando concluído.
</output>
