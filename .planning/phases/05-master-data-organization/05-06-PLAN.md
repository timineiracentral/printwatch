---
phase: 05-master-data-organization
plan: "06"
type: execute
wave: 3
depends_on:
  - "05-03"
  - "05-04"
files_modified:
  - backend/app/api/v1/import_routes.py
  - backend/app/services/import_service.py
  - backend/app/import_templates/
  - backend/tests/test_import_csv.py
autonomous: true
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-03
  - IMPORT-04
  - IMPORT-05

must_haves:
  truths:
    - "POST /api/v1/import/{entity} aceita CSV multipart; entity in printers|departments|cost-centers|users"
    - "GET /api/v1/import/templates/{entity} retorna text/csv attachment"
    - "Resposta JSON com total, created, updated, skipped, errors[{line, message}]"
    - "strict=false (default) commit linhas válidas; strict=true rollback total em qualquer erro"
    - "Códigos dept/CC normalizados UPPERCASE no import"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-05-06a | CSV formula injection em Excel | medium | Documentar prefixo ' para células; sanitizar campos que começam com =+-@ |
| T-05-06b | Upload enorme OOM | medium | Limite 5MB no UploadFile / validação tamanho |
</threat_model>

<objective>
Import CSV bulk com relatório por linha (D-23–D-26).
</objective>

<context>
@.planning/phases/05-master-data-organization/05-RESEARCH.md
@.planning/phases/05-master-data-organization/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Templates CSV estáticos</name>
  <read_first>
    - .planning/phases/05-master-data-organization/05-CONTEXT.md (D-25)
  </read_first>
  <action>
    Criar templates em `backend/app/import_templates/`:
    - departments.csv: code,name,cost_center_code
    - cost_centers.csv: code,name
    - users.csv: cups_username,display_name,department_code,cost_center_code
    - printers.csv: display_name,cups_queue_name,ip_address,manufacturer_model,location,department_code
    GET templates/{entity} retorna FileResponse com Content-Disposition attachment.
  </action>
  <acceptance_criteria>
    - GET `/api/v1/import/templates/users` retorna 200 Content-Type text/csv
    - Header row corresponde aos campos acima
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Import service e rota POST</name>
  <read_first>
    - backend/app/services/departments_service.py
    - backend/app/core/normalize.py
  </read_first>
  <action>
    `import_service.py`: parse CSV utf-8-sig; validar cada linha; upsert por natural key (code, cups_username, cups_queue_name).
    Query param `strict: bool = False`.
    Router `import_routes.py` prefix `/import`.
    Teste: 50 users, 2 linhas inválidas → created=48, errors len=2 com line numbers.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_import_csv.py -q` exit 0
    - strict=true com 1 erro → 0 rows persisted (count in DB)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Download template; editar; POST import; verificar relatório
- [ ] `pytest tests/test_import_csv.py -q`
</verification>
