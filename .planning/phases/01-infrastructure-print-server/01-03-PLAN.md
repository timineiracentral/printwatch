---
phase: 01-infrastructure-print-server
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - scripts/setup-printer.sh
  - docs/vm-setup.md
  - README.md
autonomous: true
requirements:
  - SERVER-03
  - SERVER-01
  - DEPLOY-02
user_setup:
  - service: test-printer
    why: "Cadastro de impressora HP/Samsung real"
    env_vars:
      - name: TEST_PRINTER_URI
        source: "IP da impressora na rede REDACTED_LAN — descobrir via lpinfo -v ou documentação do equipamento"
      - name: TEST_PRINTER_DRIVER
        source: "everywhere para IPP; ou PPD específico para socket://IP:9100"

must_haves:
  truths:
    - "Script setup-printer.sh cadastra impressora de teste de forma idempotente via lpadmin"
    - "Documentação descreve preparação da VM com IP VM_HOST e hostname printwatch"
    - "Passo manual equivalente ao script está documentado para operadores"
    - "Impressora aparece em lpstat -p após execução do script"
  artifacts:
    - path: scripts/setup-printer.sh
      provides: "Cadastro idempotente lpadmin"
      contains: "lpadmin"
    - path: docs/vm-setup.md
      provides: "Checklist VM Ubuntu 22.04"
      contains: "VM_HOST"
  key_links:
    - from: scripts/setup-printer.sh
      to: docker compose cups
      via: "docker compose exec cups lpadmin"
      pattern: "docker compose exec.*lpadmin"
    - from: scripts/setup-printer.sh
      to: .env
      via: "TEST_PRINTER_NAME, TEST_PRINTER_URI, TEST_PRINTER_DRIVER"
      pattern: "TEST_PRINTER"
---

<objective>
Entregar cadastro idempotente de impressora HP/Samsung via script versionado e documentação de preparação da VM — completando a cadeia até a fila CUPS pronta para jobs.

Purpose: SERVER-03 exige suporte IPP/socket; D-10/D-11 exigem script reexecutável + doc manual; D-16–D-18 exigem VM prep documentado.
Output: `scripts/setup-printer.sh`, `docs/vm-setup.md`, seção README apontando para docs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-print-server/01-CONTEXT.md
@.planning/phases/01-infrastructure-print-server/01-RESEARCH.md
@.env.example
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scripts/setup-printer.sh idempotente</name>
  <read_first>
    @.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-10, D-11, D-12)
    @.planning/phases/01-infrastructure-print-server/01-RESEARCH.md (Pattern 3)
    @.env.example
  </read_first>
  <action>
    Criar `scripts/setup-printer.sh` executável com `set -euo pipefail`:

    - Carregar variáveis de `.env` na raiz do repo (source `.env` se existir, senão `.env.example` com WARN)
    - Defaults: `TEST_PRINTER_NAME`, `TEST_PRINTER_URI`, `TEST_PRINTER_DRIVER` de env
    - Executar lpadmin **dentro do container**: `docker compose exec -T cups bash -c '...'` (D-10)
    - Lógica idempotente (D-11):
      - Se `lpstat -p "$PRINTER_NAME"` OK e URI atual == TEST_PRINTER_URI → echo "already configured" exit 0
      - Se impressora existe com URI diferente → `lpadmin -p NAME -v URI -m DRIVER -E` (update)
      - Se não existe → `lpadmin -p NAME -v URI -m DRIVER -E` (create)
    - Após cadastro: `cupsaccept` + `cupsenable`
    - Suportar URIs: `ipp://.../ipp/print` com `-m everywhere`; `socket://IP:9100` com PPD PostScript (D-12)
    - Fallback documentado: se TEST_PRINTER_URI apontar para `cups-pdf`, usar driver `cups-pdf` para teste sem hardware (discrição RESEARCH A2)

    NÃO criar UI web (D-09, SERVER-04 é Fase 5).
  </action>
  <acceptance_criteria>
    - Script existe e é executável
    - Segunda execução consecutiva exit 0 sem erro "already exists"
    - `docker compose exec -T cups lpstat -p "$TEST_PRINTER_NAME"` retorna impressora enabled após primeira execução
    - Script usa variáveis TEST_PRINTER_* do .env
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/setup-printer.sh && grep -q 'lpadmin' scripts/setup-printer.sh && grep -q 'lpstat -p' scripts/setup-printer.sh</automated>
  </verify>
  <done>Impressora de teste cadastrável de forma idempotente (SERVER-03, D-10, D-11)</done>
</task>

<task type="auto">
  <name>Task 2: Documentar preparação da VM em docs/vm-setup.md</name>
  <read_first>
    @.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-16, D-17, D-18, D-21)
    @.planning/phases/01-infrastructure-print-server/01-RESEARCH.md (VM Prep Checklist)
    @.planning/PROJECT.md
  </read_first>
  <action>
    Criar `docs/vm-setup.md` em PT-BR com checklist operacional:

    1. **VM existente** Ubuntu 22.04 no XCP-ng — reutilizar, não provisionar nova (D-16)
    2. **Hostname**: `sudo hostnamectl set-hostname printwatch` — verificar com `hostname` (D-18)
    3. **IP estático**: `VM_HOST/16` via netplan — exemplo YAML com placeholder gateway/DNS; nota para ajustar gateway real da rede (D-17)
    4. **Docker**: `apt install docker.io docker-compose-plugin`; `usermod -aG docker $USER` (DEPLOY-01)
    5. **Deploy PrintWatch**: clone repo → `cp .env.example .env` → editar TEST_PRINTER_URI e credenciais → `docker compose up -d --build`
    6. **Cadastro impressora**: `./scripts/setup-printer.sh`
    7. **Verificação rápida**: `bash scripts/validate-phase1.sh --quick`

    Incluir seção "Passo manual equivalente ao setup-printer.sh" com comandos `docker compose exec cups lpadmin ...` (D-10).
    Sem observabilidade, AD, HA (D-22).
  </action>
  <acceptance_criteria>
    - `docs/vm-setup.md` existe
    - Contém `VM_HOST` e `printwatch`
    - Contém comandos docker compose e setup-printer
    - Contém seção de cadastro manual lpadmin
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'VM_HOST' docs/vm-setup.md && grep -q 'printwatch' docs/vm-setup.md && grep -q 'lpadmin' docs/vm-setup.md</automated>
  </verify>
  <done>VM prep documentado para operadores (D-16–D-18, DEPLOY-02)</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar README e executar cadastro de impressora</name>
  <read_first>
    @docs/vm-setup.md
    @scripts/setup-printer.sh
    @PRD.md
  </read_first>
  <action>
    Criar ou atualizar `README.md` na raiz (greenfield — criar se ausente) com:

    - Título PrintWatch e descrição one-liner
    - Link para `docs/vm-setup.md` como guia de deploy Fase 1
    - Quick start: 4 comandos (`cp .env.example .env`, editar, `docker compose up -d --build`, `./scripts/setup-printer.sh`)
    - Nota: Fase 1 valida apenas CUPS + page_log; dashboard vem nas Fases 3–4 (D-04)

    Executar `./scripts/setup-printer.sh` com container cups running. Se TEST_PRINTER_URI for placeholder sem impressora real, configurar fallback `ipp://localhost/printers/PDF` ou driver cups-pdf no `.env` para permitir cadastro de fila de teste.

    Atualizar `scripts/validate-phase1.sh --quick` para verificar existência de `scripts/setup-printer.sh` como PASS (não WARN).
  </action>
  <acceptance_criteria>
    - README.md referencia docs/vm-setup.md
    - `./scripts/setup-printer.sh` exit 0 (com impressora real ou fallback cups-pdf)
    - `docker compose exec -T cups lpstat -p` lista TEST_PRINTER_NAME
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'vm-setup.md' README.md && bash scripts/setup-printer.sh && docker compose exec -T cups lpstat -p "${TEST_PRINTER_NAME:-test_printer}"</automated>
  </verify>
  <done>Impressora cadastrada e documentação de deploy acessível (SERVER-03, D-21)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador → setup-printer.sh | Script executa lpadmin com privilégios admin CUPS |
| Container → impressora física LAN | Jobs encaminhados para IP externo ao container |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | setup-printer.sh | mitigate | Variáveis de URI só via .env versionado como example; operador controla .env local |
| T-01-07 | Denial of Service | lpadmin misuse | mitigate | Script idempotente; não remove impressoras existentes; valida lpstat antes |
| T-01-02 | Spoofing | Impressora remota | accept | URI configurada manualmente pelo admin; validação de ownership fora escopo MVP |
| T-01-SC | Tampering | apt packages | accept | Sem novos pacotes neste plan |
</threat_model>

<verification>
`./scripts/setup-printer.sh` exit 0; `lpstat -p` mostra impressora; docs/vm-setup.md completo; README linkado.
</verification>

<success_criteria>
- SERVER-03: impressora HP/Samsung (ou cups-pdf fallback) cadastrada via lpadmin idempotente
- SERVER-01: fila CUPS pronta para receber jobs IPP da rede
- DEPLOY-02: documentação complementa .env.example
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-print-server/01-03-SUMMARY.md` when done
</output>
