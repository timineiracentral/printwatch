---
phase: 06-costing-chargeback
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - backend/app/services/jobs_service.py
  - backend/app/schemas/jobs.py
  - backend/app/api/v1/jobs.py
  - backend/app/schemas/job_lines.py
  - backend/tests/test_jobs_cost.py
  - backend/tests/test_jobs_color_patch.py
autonomous: true
requirements:
  - COST-02
  - COST-03

must_haves:
  truths:
    - "D-09/D-10: JobOut com pages_billable, pages_pending_color, pages_mono, pages_color, estimated_cost"
    - "D-08: PATCH linha print_jobs define mono|color + source manual"
    - "D-20: stats_service.py inalterado (sem campos de custo)"
    - "Correção manual recalcula custo no GET jobs subsequente"
---

<objective>
Enriquecer API de jobs com métricas faturáveis/custo e endpoint de correção manual de color_mode por linha.
</objective>

<context>
@.planning/phases/06-costing-chargeback/06-CONTEXT.md
@backend/app/services/jobs_service.py
@backend/app/schemas/jobs.py
@backend/app/services/cost_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregação billable + estimated_cost</name>
  <read_first>
    - backend/app/services/jobs_service.py (_build_aggregated_query)
    - backend/app/services/cost_service.py
    - .planning/phases/06-costing-chargeback/06-RESEARCH.md (billable aggregation)
  </read_first>
  <action>
    Refatorar agregação: subquery ou expressões CASE na contagem por grupo:
    pages_mono = SUM(CASE WHEN color_mode='mono' THEN 1 ELSE 0 END)
    pages_color = SUM(CASE WHEN color_mode='color' THEN 1 ELSE 0 END)
    pages_pending_color = SUM(CASE WHEN color_mode IS NULL THEN 1 ELSE 0 END)
    pages_billable = pages_mono + pages_color
    estimated_cost = soma line_cost por linha do grupo (chamar cost_service por linha ou SQL join rates — preferir batch rate lookup por distinct timestamps no grupo para performance).
    Estender JobOut e list/detail endpoints.
  </action>
  <acceptance_criteria>
    - JobOut schema contém pages_billable, pages_pending_color, pages_mono, pages_color, estimated_cost (Optional Decimal/float serializado)
    - test_jobs_cost.py: grupo com 2 mono + 1 NULL → pages_billable=2, pages_pending_color=1
    - `grep estimated_cost backend/app/services/stats_service.py` sem matches
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: GET linhas brutas para correção</name>
  <read_first>
    - backend/app/services/jobs_service.py
    - backend/app/schemas/jobs.py
  </read_first>
  <action>
    GET /api/v1/jobs/lines com query obrigatórias: printer, username, job_id, job_name, minute_bucket (ISO minute SP ou UTC conforme agregação).
    Retorna lista JobLineOut: id, timestamp, color_mode, color_mode_source, pages=1.
    Usado pela UI de correção manual (Plano 05).
  </action>
  <acceptance_criteria>
    - Endpoint retorna 200 com array de linhas do grupo
    - 422 se faltar qualquer query de agrupamento
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: PATCH color-mode por linha</name>
  <read_first>
    - backend/app/api/v1/jobs.py
    - backend/app/db/models.py (PrintJob)
  </read_first>
  <action>
    Adicionar schema ColorModePatch body: color_mode Literal['mono','color'].
    PATCH /api/v1/jobs/lines/{line_id}/color-mode: atualiza print_jobs.color_mode e color_mode_source='manual'; 404 se id inexistente.
  </action>
  <acceptance_criteria>
    - PATCH linha NULL→mono faz GET jobs agregado incrementar pages_mono e estimated_cost > 0 quando rates existem
    - `pytest -q tests/test_jobs_color_patch.py` exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- pytest test_jobs_cost + test_jobs_color_patch + suite
</verification>
