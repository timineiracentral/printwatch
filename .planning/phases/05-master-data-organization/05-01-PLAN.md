---
phase: 05-master-data-organization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/versions/
  - backend/app/db/models.py
  - backend/app/db/base.py
  - backend/app/main.py
  - backend/requirements.txt
  - docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - DATA-07

must_haves:
  truths:
    - "D-19 Alembic obrigatório; D-20 tabelas mestre; D-21 índice matcher; D-22 WAL"
    - "Alembic inicializado em backend/alembic/ com revision inicial criando printers, departments, cost_centers, users"
    - "print_jobs.printer_id existe como FK nullable para printers.id sem quebrar INSERT do watcher"
    - "Todas entidades mestre têm is_active, created_at, updated_at"
    - "alembic upgrade head aplica schema em DB vazio e em DB v1.0 existente"
    - "CORSMiddleware allow_methods inclui GET, POST, PATCH, PUT, DELETE"
    - "SQLite WAL habilitado no startup quando journal_mode não for WAL"
  artifacts:
    - path: backend/alembic/versions/
      provides: "Revision(s) com batch_alter_table para print_jobs no SQLite"
    - path: backend/app/db/models.py
      provides: "Printer, Department, CostCenter, User + PrintJob.printer_id"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-05-01 | Migration irreversível corrompe DB produção | high | Testar downgrade -1; backup volume antes deploy |
| T-05-02 | FK em print_jobs bloqueia INSERT watcher | high | printer_id nullable; sem FK enforcement no watcher path |
</threat_model>

<objective>
Fundação de schema e migrations: Alembic + tabelas mestre + coluna printer_id + CORS para métodos de escrita + WAL.

Output: `alembic upgrade head` sucesso; modelos importáveis; pytest existente ainda passa.
</objective>

<context>
@.planning/phases/05-master-data-organization/05-CONTEXT.md
@.planning/phases/05-master-data-organization/05-RESEARCH.md
@backend/app/db/models.py
@backend/app/main.py
@backend/app/db/session.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inicializar Alembic e dependência</name>
  <read_first>
    - backend/app/db/base.py
    - backend/app/db/models.py
    - backend/requirements.txt
    - .planning/phases/05-master-data-organization/05-RESEARCH.md
  </read_first>
  <action>
    Adicionar `alembic` ao requirements.txt.
    Executar `alembic init alembic` dentro de `backend/`.
    Configurar `alembic.ini` sqlalchemy.url para ler `DATABASE_URL` do mesmo env que `app.core.config.settings`.
    Em `alembic/env.py`: importar `Base` de `app.db.base` e `target_metadata = Base.metadata`; importar todos os modelos para registrar metadata.
  </action>
  <acceptance_criteria>
    - `backend/alembic/env.py` contém `target_metadata`
    - `alembic.ini` existe em `backend/`
    - `alembic history` exit 0 em `backend/`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Modelos SQLAlchemy e revision inicial</name>
  <read_first>
    - backend/app/db/models.py
    - .planning/phases/05-master-data-organization/05-CONTEXT.md (domain_model)
  </read_first>
  <action>
    Adicionar modelos Printer, Department, CostCenter, User conforme entity model aprovado.
    Adicionar `printer_id: Mapped[Optional[int]]` em PrintJob com `ForeignKey("printers.id")`, nullable.
    Criar revision `alembic revision -m "master_data_tables"`:
    - CREATE TABLE printers, departments, cost_centers, users com constraints UNIQUE em codes e cups_queue_name
    - Usar `op.batch_alter_table('print_jobs')` para adicionar printer_id no SQLite
    - Criar índice parcial ou composto para matcher: jobs com printer_id IS NULL
    FK departments.cost_center_id → cost_centers; users.department_id → departments; printers.department_id → departments (nullable).
  </action>
  <acceptance_criteria>
    - `grep -l "class Printer" backend/app/db/models.py` encontra arquivo
    - Revision em `backend/alembic/versions/` contém `batch_alter_table` para print_jobs
    - `departments.code` e `cost_centers.code` NOT NULL UNIQUE no migration
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: CORS, WAL e lifespan hook para migrations</name>
  <read_first>
    - backend/app/main.py
    - backend/app/db/session.py
  </read_first>
  <action>
    Alterar CORSMiddleware: `allow_methods=["GET","POST","PATCH","PUT","DELETE"]` mantendo allow_origins de settings.
    No lifespan startup (após ensure_indexes): executar `PRAGMA journal_mode=WAL` se retorno != 'wal'.
    Documentar em `.env.example`: `RUN_MIGRATIONS_ON_STARTUP=false` (default); migrations via comando explícito.
    NÃO auto-run alembic no watcher path.
  </action>
  <acceptance_criteria>
    - `backend/app/main.py` contém POST e PATCH em allow_methods
    - `pytest -q` exit 0 (suite existente)
  </acceptance_criteria>
</task>

<task type="blocking" autonomous="false">
  <name>Task 4: [BLOCKING] Schema push — alembic upgrade head</name>
  <read_first>
    - backend/alembic/versions/ (última revision)
    - docker-compose.yml (volume db_data)
  </read_first>
  <action>
    Com backend container parado ou em dev local:
    `cd backend && alembic upgrade head`
    Verificar colunas com `sqlite3` ou script: tabelas printers, departments, cost_centers, users existem; print_jobs.printer_id nullable.
    Testar `alembic downgrade -1` seguido de `upgrade head` em cópia do DB de dev.
  </action>
  <acceptance_criteria>
    - `alembic current` mostra head
    - `pytest tests/test_migrations.py -q` exit 0 (criar teste mínimo se ausente)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd backend && alembic upgrade head`
- [ ] `cd backend && pytest -q`
- [ ] OpenAPI ainda carrega em `/api/v1/docs`
</verification>
