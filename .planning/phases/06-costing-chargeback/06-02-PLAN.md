---
phase: 06-costing-chargeback
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - backend/app/services/cost_service.py
  - backend/app/schemas/cost_rates.py
  - backend/app/api/v1/cost_rates.py
  - backend/app/api/v1/__init__.py
  - backend/tests/test_cost_service.py
  - backend/tests/test_cost_rates.py
autonomous: true
requirements:
  - COST-01
  - COST-02
  - COST-04

must_haves:
  truths:
    - "D-02: rate_at(timestamp) retorna tarifa vigente na data do evento"
    - "D-03: API expõe histórico e tarifa current em BRL"
    - "D-04: aggregate_cost_by_dimension disponível para exports (não stats/summary)"
    - "POST cost-rates insere nova vigência sem DELETE histórico"
    - "Sem tarifa configurada: funções retornam None/0 sem exception"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-06-03 | Tarifa errada em jobs antigos | high | Testes com 2 vigências + timestamps distintos |
| T-06-04 | Float rounding | medium | Decimal only |
</threat_model>

<objective>
Serviço de custo read-path + API CRUD/histórico de tarifas globais.
</objective>

<context>
@.planning/phases/06-costing-chargeback/06-CONTEXT.md
@.planning/phases/06-costing-chargeback/06-RESEARCH.md
@backend/app/db/models.py
@backend/app/api/v1/cost_centers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: cost_service core</name>
  <read_first>
    - backend/app/db/models.py (CostRate, PrintJob)
    - .planning/phases/06-costing-chargeback/06-RESEARCH.md
  </read_first>
  <action>
    Criar backend/app/services/cost_service.py:
    - rate_at(db, at: datetime) -> CostRate | None
    - line_cost(rate, color_mode: str) -> Decimal | None (None se color_mode não mono/color)
    - aggregate_cost_by_dimension(db, date_from, date_to, dimension: Literal['cost_center','department']) -> list[dict] com pages_mono, pages_color, estimated_cost, bucket labels — aplicar regras D-11..D-14 (excluir outside_policy via policy_service)
    Usar Decimal; format_money_brl helper para strings opcionais.
  </action>
  <acceptance_criteria>
    - test_cost_service.py: job 2024-01 com rate valid_from 2024-06 usa rate antiga se existir registro anterior
    - test_cost_service.py: color_mode NULL contribui 0 a mono/color e incrementa pending counter
    - `pytest -q tests/test_cost_service.py` exit 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Schemas + router cost-rates</name>
  <read_first>
    - backend/app/schemas/cost_centers.py
    - backend/app/api/v1/cost_centers.py
  </read_first>
  <action>
    Criar schemas CostRateCreate (rate_mono, rate_color, valid_from optional), CostRateRead (id, rates, valid_from, created_at).
    Router GET /cost-rates (list desc), GET /cost-rates/current, POST /cost-rates (201).
  </action>
  <acceptance_criteria>
    - `grep cost_rates backend/app/api/v1/__init__.py` inclui router prefix /cost-rates
    - POST segunda tarifa deixa count >= 2 no GET list
    - `pytest -q tests/test_cost_rates.py` exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- pytest test_cost_service + test_cost_rates
- Confirmar stats_service.py não importa cost_service (grep)
</verification>
