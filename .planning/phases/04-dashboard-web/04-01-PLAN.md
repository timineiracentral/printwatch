---
phase: 04-dashboard-web
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/index.html
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/vite-env.d.ts
  - frontend/vitest.config.ts
  - frontend/src/lib/filters.ts
  - frontend/src/lib/dates.ts
  - frontend/src/lib/media.ts
  - frontend/src/lib/filters.test.ts
  - frontend/src/lib/dates.test.ts
  - frontend/src/lib/media.test.ts
autonomous: true
requirements:
  - DASH-06
user_setup: []

must_haves:
  truths:
    - "frontend/ compila com npm run build sem erro"
    - "Vitest verde para lib/filters, lib/dates, lib/media"
    - "vite.config.ts proxy /api → localhost:8000 sem rewrite (dev DX D-57)"
  artifacts:
    - path: "frontend/package.json"
      provides: "React 18 + Vite + Tailwind v4 + TanStack Query deps pinadas"
      contains: "\"react\":"
    - path: "frontend/src/lib/filters.ts"
      provides: "URL ↔ JobFilters round-trip"
      contains: "parseFiltersFromUrl"
    - path: "frontend/vitest.config.ts"
      provides: "Wave 0 unit tests"
      contains: "jsdom"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "backend:8000"
      via: "server.proxy /api"
      pattern: "proxy.*localhost:8000"
---

## Phase Goal

**As a** administrador de TI, **I want to** acessar o dashboard na rede local via browser na porta 80, **so that** posso auditar impressões sem expor API diretamente na rede.

<objective>
Vertical slice 1a: scaffold frontend Vite React-TS + Tailwind v4 + libs puras testadas (filters, dates, media).

Purpose: Estabelece o projeto `frontend/` e contratos de URL/data antes da camada API e UI. Deploy nginx :80 está no plan **04-07** (depende deste plano).

Output: `cd frontend && npm run build` exit 0; `npm test -- --run` verde; `vite.config.ts` com proxy `/api` para dev.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dashboard-web/04-CONTEXT.md
@.planning/phases/04-dashboard-web/04-RESEARCH.md
@.planning/phases/04-dashboard-web/04-UI-SPEC.md
@docker-compose.yml
@scripts/validate-phase3.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold frontend Vite React-TS + Tailwind v4 + Query provider</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-RESEARCH.md — Standard Stack, Package Legitimacy Audit, Pattern 1 (Vite proxy)
    - .planning/phases/04-dashboard-web/04-UI-SPEC.md — CSS variables :root tokens
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-38, D-39, D-44, D-45, D-57, D-64
  </read_first>
  <files>
    frontend/package.json,
    frontend/package-lock.json,
    frontend/index.html,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    frontend/tsconfig.app.json,
    frontend/tsconfig.node.json,
    frontend/src/main.tsx,
    frontend/src/App.tsx,
    frontend/src/index.css,
    frontend/src/vite-env.d.ts
  </files>
  <action>
    Criar `frontend/` com `npm create vite@latest . -- --template react-ts` (no diretório frontend vazio).

    Instalar e **pin** dependências per D-38/D-39 e RESEARCH Package Legitimacy (todos Approved):
    - react@^18.3.1, react-dom@^18.3.1 (NÃO usar React 19)
    - @tanstack/react-query@^5.100.x
    - @headlessui/react@^2.2.10, lucide-react@^1.16.0, date-fns@^4.3.0, @date-fns/tz@^1.5.0
    - dev: tailwindcss@^4.3.0, @tailwindcss/vite@^4.3.0, vitest@^3.x, @testing-library/react, jsdom

    `vite.config.ts`: plugins react + tailwindcss; `server.proxy` para `/api` → `http://localhost:8000` **sem rewrite** (preserva `/api/v1` — D-57).

    `src/index.css`: `@import "tailwindcss";` + tokens CSS de 04-UI-SPEC (`--bg-canvas`, `--accent`, etc.) per D-01..D-03.

    `src/main.tsx`: `QueryClientProvider` com `retry: 1`, `refetchOnWindowFocus: false` (D-39).

    `src/App.tsx`: página única placeholder pt-BR "PrintWatch — carregando…" — **sem** React Router (D-44).

    `package.json` scripts: `"test": "vitest"`, `"build": "tsc -b && vite build"`.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contém react 18.x e @tanstack/react-query
    - frontend/package.json NÃO contém react-router-dom, @mui/material, redux, zustand
    - vite.config.ts contém proxy `/api` sem rewrite
    - npm run build exit 0
  </acceptance_criteria>
  <done>Frontend compila; dev proxy /api configurado; tokens visuais base em index.css.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Libs puras + Vitest Wave 0 (filters, dates, media)</name>
  <read_first>
    - backend/app/schemas/jobs.py — JobFilters campos e defaults page=1 size=50
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-23, D-16, D-31..D-33, D-42
    - .planning/phases/04-dashboard-web/04-RESEARCH.md — Pattern 3, Pattern 4
  </read_first>
  <files>
    frontend/vitest.config.ts,
    frontend/src/lib/filters.ts,
    frontend/src/lib/dates.ts,
    frontend/src/lib/media.ts,
    frontend/src/lib/filters.test.ts,
    frontend/src/lib/dates.test.ts,
    frontend/src/lib/media.test.ts
  </files>
  <behavior>
    - filters: round-trip URLSearchParams ↔ JobFilters; omitir chaves vazias; defaults page=1 size=50 (D-23, D-28)
    - dates: presetToday/Last7Days/MonthToDate retornam yyyy-MM-dd coerentes com TZ America/Sao_Paulo (D-16, D-42)
    - media: iso_a4 → A4, na_letter → Carta, unknown → raw (D-31, D-32)
  </behavior>
  <action>
    Implementar **antes** dos testes (TDD RED→GREEN):

    `lib/filters.ts`: `parseFiltersFromUrl(search: string): JobFilters`, `filtersToSearchParams(f)`, `clearFiltersDefaults()` → page=1 size=50 sem filtros (D-22, D-23). Tipos espelhando Pydantic (D-65).

    `lib/dates.ts`: `TZ = 'America/Sao_Paulo'`; `presetToday`, `presetLast7Days`, `presetMonthToDate` usando `TZDate.tz` + `format` (D-16, D-42). **Não** usar `new Date()` local do browser para presets.

    `lib/media.ts`: `formatMediaLabel(raw)` mapa D-32; fallback raw (D-33).

    `vitest.config.ts`: environment jsdom; alias `@/` se útil.

    Escrever testes cobrindo behaviors acima; rodar até verde.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - 3 arquivos de teste existem e passam
    - filters.test cobre round-trip e clear defaults
    - dates.test fixa TZ ou usa datas conhecidas
    - media.test cobre A4, Carta, fallback
  </acceptance_criteria>
  <done>Libs puras testadas; base para hooks sem lógica duplicada nos componentes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → nginx:80 | Rede local; sem TLS (D-55) |
| nginx → backend:8000 | Rede Docker interna |
| npm install (build) | Supply chain — pacotes pinados em RESEARCH audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I | nginx:80 sem auth | accept | MVP rede local + firewall; auth v2 |
| T-04-02 | I | API exposta via proxy | mitigate | Backend sem ports host; apenas /api via nginx |
| T-04-03 | T | XSS em job_name futuro | mitigate | React escape default; proibir dangerouslySetInnerHTML |
| T-04-SC | T | npm ci no Docker build | mitigate | Pacotes [Approved] em 04-RESEARCH.md Package Legitimacy Audit; pin lockfile |

</threat_model>

<verification>
- `cd frontend && npm run build` sem erro
- `cd frontend && npm test -- --run` verde
</verification>

<success_criteria>
- Frontend Vite compila; libs filters/dates/media com testes Vitest
- Deploy nginx :80 entregue no plan 04-07 (dependência explícita)
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-web/04-01-SUMMARY.md` when done
</output>
