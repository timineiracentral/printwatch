---
phase: 02-log-pipeline-data-layer
plan: "02"
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/services/__init__.py
  - backend/app/services/parser.py
  - backend/app/services/tail_reader.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_parser.py
  - backend/tests/test_tail_reader.py
  - backend/pytest.ini
autonomous: true
requirements:
  - CAPTURE-01
  - CAPTURE-02
  - CAPTURE-03

must_haves:
  truths:
    - "parse_page_log_line retorna dict com todos os 10 campos mapeados do PAGE_LOG_REGEX (CAPTURE-01)"
    - "Campo '-' do CUPS é convertido para None — nunca string vazia (D-09)"
    - "Username mantido como recebido (ex: DOMAIN\\\\usuario) sem normalização (D-08)"
    - "Timestamp parsado com timezone-aware via strptime %z (pitfall 6)"
    - "parse_page_log_line retorna None para linha que não casa com regex (pitfall 7)"
    - "TailReader.read_new_lines() faz seek(offset) ao reiniciar com mesmo inode (D-05, CAPTURE-03)"
    - "TailReader detecta mudança de inode e reabre do início (D-05, logrotate)"
    - "Suite pytest backend/tests/ passa 100% sem container rodando"
  artifacts:
    - path: "backend/app/services/parser.py"
      provides: "PAGE_LOG_REGEX, _null_if_dash(), parse_page_log_line()"
      exports: ["PAGE_LOG_REGEX", "_null_if_dash", "parse_page_log_line"]
    - path: "backend/app/services/tail_reader.py"
      provides: "TailReader com checkpoint inode+offset"
      exports: ["TailReader"]
    - path: "backend/tests/test_parser.py"
      provides: "Testes unitários do parser (todos os campos, NULL sentinel, username AD, timestamp tz)"
    - path: "backend/tests/test_tail_reader.py"
      provides: "Testes unitários do TailReader (seek restart, inode change)"
    - path: "backend/pytest.ini"
      provides: "Config pytest com testpaths = tests"
  key_links:
    - from: "backend/tests/test_parser.py"
      to: "backend/app/services/parser.py"
      via: "from app.services.parser import parse_page_log_line"
      pattern: "from app\\.services\\.parser import"
    - from: "backend/tests/test_tail_reader.py"
      to: "backend/app/services/tail_reader.py"
      via: "from app.services.tail_reader import TailReader"
      pattern: "from app\\.services\\.tail_reader import"
---

<objective>
Vertical slice 2 (paralelo ao Plano 01): "parser e tail_reader funcionam corretamente — provado por testes".

Implementa os dois módulos core de processamento de dados usando ciclo RED→GREEN→REFACTOR:
- `parser.py`: PAGE_LOG_REGEX → dict com regras D-08 (username), D-09 (NULL sentinel), timezone-aware timestamp
- `tail_reader.py`: TailReader com checkpoint inode+offset (D-04, D-05) e detecção de logrotate

Estes módulos podem ser desenvolvidos e testados em isolamento sem o container rodando — TailReader usa arquivos temporários, parser usa strings.

Purpose: Garantir por testes que o núcleo do pipeline (parse + tail) funciona corretamente antes de integrá-lo ao watcher no Plano 03.

Output: parser.py + tail_reader.py implementados e cobertos por testes pytest que passam em ambiente local (sem Docker).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-log-pipeline-data-layer/02-CONTEXT.md
@.planning/phases/02-log-pipeline-data-layer/02-RESEARCH.md
@.planning/phases/02-log-pipeline-data-layer/02-PATTERNS.md

@scripts/validate-phase1.sh
@cups/cupsd.conf.template
</context>

<feature>
  <name>Parser e TailReader com TDD</name>
  <files>
    backend/app/services/parser.py,
    backend/app/services/tail_reader.py,
    backend/tests/test_parser.py,
    backend/tests/test_tail_reader.py,
    backend/tests/conftest.py,
    backend/pytest.ini
  </files>

  <behavior>
    ### parser.py
    - Test: parse_page_log_line com linha sample da Fase 1 → dict com printer="test_printer", username="DOMAIN\usuario", job_id=42, pages=3, color_mode=None (era "-"), host_origin="192.168.1.10", job_name="relatorio.pdf", media="na_iso_a4_210x297mm", sides="one-sided"
    - Test: _null_if_dash("-") → None
    - Test: _null_if_dash("color") → "color"
    - Test: _null_if_dash("  -  ") → None (com espaços)
    - Test: parse_page_log_line com linha malformada → None (não lança exceção)
    - Test: parse_page_log_line("") → None
    - Test: timestamp retornado é datetime com tzinfo não-None (timezone-aware)
    - Test: username "DOMAIN\user.example" mantido sem alteração (D-08)
    - Test: status no dict retornado é "allowed" (D-15)
    - Test: copies no dict retornado é None (não presente no PageLogFormat)

    ### tail_reader.py
    - Test: TailReader sobre arquivo vazio → read_new_lines() retorna []
    - Test: Escrever linha no arquivo → read_new_lines() retorna [linha] sem "\n"
    - Test: Ler duas vezes sem escrever novo conteúdo → segunda leitura retorna []
    - Test: Simular restart com mesmo inode: criar TailReader2 com state_repo já populado (mesmo inode) → seek(offset) e não re-lê linhas antigas
    - Test: Simular logrotate (inode diferente): TailReader com state mostrando inode=X mas arquivo atual com inode=Y → reabre do início e lê linha nova do arquivo
  </behavior>

  <implementation>
    ### Fase RED (escrever testes primeiro)
    Criar backend/pytest.ini com testpaths = tests. Criar backend/tests/__init__.py e backend/tests/conftest.py com fixtures: sample_page_log_line (string com linha de amostra da Fase 1), tmp_log_file (arquivo temporário via tmp_path), stub_state_repo (objeto simples com get()/upsert()). Escrever test_parser.py e test_tail_reader.py com todos os casos acima. Executar pytest — deve FALHAR (módulos não existem ainda).

    ### Fase GREEN (implementar até testes passarem)

    **parser.py:** Importar PAGE_LOG_REGEX literal de scripts/validate-phase1.sh (linha 20). Definir _null_if_dash(value: str) → Optional[str]: retorna None se value.strip() == "-" senão value.strip(). Definir parse_page_log_line(line: str) → Optional[dict]: re.match no line.strip(); se None retorna None; mapear grupos 1-10 conforme tabela RESEARCH.md §Mapeamento PAGE_LOG_REGEX; timestamp via datetime.strptime(m.group(4), "%d/%b/%Y:%H:%M:%S %z"); adicionar status="allowed" e copies=None ao dict retornado.

    **tail_reader.py:** TailReader.__init__(path: str, state_repo) chama _recover_checkpoint(). _recover_checkpoint(): lê state = state_repo.get(); current_inode = os.stat(path).st_ino; se state existe e state.inode == current_inode → abre arquivo, seek(state.byte_offset); senão → abre arquivo no início, inode=current_inode, offset=0. read_new_lines() → verifica inode atual via os.stat().st_ino; se mudou, fecha handle, reabre do início; readlines(); se há linhas, self._offset = fh.tell() e state_repo.upsert(inode=self._inode, byte_offset=self._offset); retorna [l.rstrip("\n") for l in lines if l.strip()]. Usar encoding="utf-8", errors="replace".

    ### Fase REFACTOR (se necessário)
    Extrair constante PAGE_LOG_PATH = "/var/log/cups/page_log" para config. Garantir type hints completos.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page_log content → parser | Conteúdo do log pode ter caracteres inesperados de nomes de documentos Windows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | parser.py PAGE_LOG_REGEX | mitigate | Linhas que não casam com regex retornam None e são descartadas — nunca propagadas ao banco (pitfall 7 do RESEARCH.md) |
| T-02-05 | Tampering | tail_reader.py open() encoding | mitigate | Usar encoding="utf-8", errors="replace" — nomes de documentos Windows com chars especiais não quebram o stream (pitfall anti-padrão do RESEARCH.md) |
| T-02-SC | Tampering | pip install dependências de teste (pytest) | mitigate | pytest já incluso no requirements.txt verificado no Package Legitimacy Audit |
</threat_model>

<verification>
Após execução deste plano:

1. `cd backend && pip install -r requirements.txt && pytest tests/ -v` — 100% pass, sem warnings de deprecação SQLAlchemy
2. `pytest tests/test_parser.py -v` — pelo menos 10 testes passando
3. `pytest tests/test_tail_reader.py -v` — pelo menos 5 testes passando (seek restart + inode change)
4. `pytest tests/ --tb=short 2>&1 | grep -E "passed|failed"` — mostra "X passed, 0 failed"
</verification>

<success_criteria>
- Todos os testes pytest passam sem container Docker rodando
- parse_page_log_line processa linha sample com username "DOMAIN\usuario" retornando campo username inalterado
- _null_if_dash("-") retorna None (não "" nem "None")
- TailReader seek restart provado por teste: segunda instância com mesmo inode não re-lê linhas antigas
- TailReader logrotate provado por teste: inode diferente → reabre do início
</success_criteria>

<output>
Criar `.planning/phases/02-log-pipeline-data-layer/02-02-SUMMARY.md` quando concluído.
</output>
