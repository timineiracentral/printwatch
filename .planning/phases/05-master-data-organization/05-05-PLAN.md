---
phase: 05-master-data-organization
plan: "05"
type: execute
wave: 3
depends_on:
  - "05-03"
files_modified:
  - backend/app/services/printer_matcher.py
  - backend/app/api/v1/admin.py
  - backend/app/main.py
  - backend/tests/test_matcher.py
autonomous: true
requirements:
  - INV-04
  - INV-05

must_haves:
  truths:
    - "D-01 watcher não resolve printer_id; D-02 matcher 60s batch; D-03 on-save; D-04 backfill idempotente"
    - "Matcher roda a cada 60s no lifespan; atualiza só print_jobs WHERE printer_id IS NULL"
    - "Batch limitado (default 500) por ciclo"
    - "match_jobs_for_queue(cups_queue_name) chamado on-save printer (BackgroundTasks)"
    - "POST /api/v1/admin/backfill-printer-ids idempotente até remaining_null estável"
    - "Watcher INSERT não define printer_id"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-05-05 | Matcher full table scan trava SQLite | high | LIMIT batch; índice partial; WAL |
| T-05-06 | Matcher importado no watcher | high | Módulo isolado; grep CI watcher imports |
</threat_model>

<objective>
Serviço assíncrono de vínculo printer_id (D-01–D-04) desacoplado do capture pipeline.
</objective>

<context>
@.planning/phases/05-master-data-organization/05-CONTEXT.md
@.planning/research/PITFALLS.md
@backend/app/main.py
@backend/app/watcher/handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: printer_matcher service</name>
  <read_first>
    - backend/app/core/normalize.py
    - backend/app/db/models.py
    - backend/app/services/printers_service.py
  </read_first>
  <action>
    Criar `services/printer_matcher.py`:
    - `resolve_printer_id(session, printer_name: str) -> int | None` usando normalize + lookup printers.cups_queue_name
    - `match_batch(session, limit=500) -> int` contagem atualizada
    - `match_jobs_for_queue(session, cups_queue_name: str) -> int`
    Garantir UPDATE só onde printer_id IS NULL.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_matcher.py -q` exit 0 com fixture 2 jobs órfãos + 1 printer
    - Segunda execução match_batch retorna 0 updates
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Lifespan asyncio loop + admin backfill</name>
  <read_first>
    - backend/app/main.py
    - backend/app/api/v1/printers.py
  </read_first>
  <action>
    No lifespan: `asyncio.create_task` loop sleep 60s chamando match_batch em SessionLocal(); cancel task no shutdown.
    Criar `api/v1/admin.py` router: POST `/backfill-printer-ids` loop batches até 0 ou max_iterations=1000; response `{matched_total, remaining_null}`.
    Wire `schedule_match_for_queue` em printers create/update para BackgroundTasks.add_task.
    Verificar handler/watcher: nenhum import de printer_matcher.
  </action>
  <acceptance_criteria>
    - `grep printer_matcher backend/app/watcher` vazio
    - OpenAPI lista POST `/api/v1/admin/backfill-printer-ids`
    - pytest matcher + integration mínima exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] Registrar printer; inserir job teste com printer string; matcher preenche printer_id em 60s
- [ ] `pytest tests/test_matcher.py -q`
</verification>
