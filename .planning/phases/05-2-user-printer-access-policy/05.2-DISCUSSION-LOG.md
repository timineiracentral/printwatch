# Phase 5.2: User–Printer Access Policy — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `05.2-CONTEXT.md`.

**Date:** 2026-05-27  
**Phase:** 5.2 — User–Printer Access Policy  
**Areas discussed:** Edição de atribuições, Limites/selector, Export TI, Outside policy, Filas detectadas

---

## Onde editar atribuições

| Option | Description | Selected |
|--------|-------------|----------|
| A | Só ficha do usuário | |
| B | Usuário + impressora (ambos editáveis) | |
| C | Usuário (escrita) + impressora somente leitura | ✓ |

**User's choice:** 1C — com complementos: fonte única de escrita na ficha do usuário; impressora read-only + atalhos para usuários relacionados; evitar divergência entre telas.

**Notes:** API write scoped to user; printer page lists users with navigation links only.

---

## Máximo de impressoras por usuário

| Option | Description | Selected |
|--------|-------------|----------|
| A | Ilimitado (validação técnica apenas) | ✓ |
| B | Cap fixo (5–10) | |
| C | Cap configurável global | |

**User's choice:** 2A — ilimitado no modelo; UI escalável com multi-select pesquisável, agrupamento dept/localização, impressoras mais usadas recentemente primeiro.

**Notes:** Aviso soft opcional se >10 vínculos; não bloqueia salvar.

---

## Export roteiro TI

| Option | Description | Selected |
|--------|-------------|----------|
| A | Só CSV | |
| B | Só página imprimível | |
| C | CSV + página imprimível (mesmo dataset) | ✓ |

**User's choice:** 3C — dataset idêntico; campos: display_name, username, printer display name, cups_queue_name, ipp_url, default, department, location; layout imprimível minimalista “roteiro de instalação”.

---

## Outside policy (auditoria)

| Option | Description | Selected |
|--------|-------------|----------|
| A | Só CSV export | |
| B | Coluna + badge | |
| C | Coluna + badge + filtro | ✓ |
| D | Só badge | |

**User's choice:** 4C — badge discreto, tooltip “Usuário imprimiu em impressora não atribuída”; não marcar órfãos/incompletos; regra estrita (usuário cadastrado + printer_id + fora da lista).

**Notes:** Usuário sem atribuições → não é violação (`outside_policy = false`).

---

## discovered-queues vs unmapped-queues

| Option | Description | Selected |
|--------|-------------|----------|
| A | 5.2: só unmapped-queues; labels operacionais | ✓ |
| B | 5.2: unmapped + lpstat discovered | |
| C | unmapped agora; discovered depois | |

**User's choice:** 5A — sem lpstat; API leitura rápida independente de CUPS; terminologia: Fila detectada / mapeada / não cadastrada.

---

## Diretrizes adicionais (confirmadas)

- Política 100% observacional; nunca enforcement.
- Nenhuma regra bloqueia impressão.
- Watcher/pipeline desacoplados.
- Funciona sem vínculos cadastrados.

## Claude's Discretion

- Formato exato de `ipp_url` (template IPP) — definir no plano/research com base em deploy atual.
- Toggle coluna outside_policy vs ícone-only — planner pode escolher desde que badge+filtro existam.

## Deferred Ideas

- `GET /printers/discovered-queues` via lpstat no container cups → 5.3 ou Fase 8 (fleet).
