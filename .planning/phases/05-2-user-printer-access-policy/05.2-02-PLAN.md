---
phase: 05.2-user-printer-access-policy
plan: "02"
type: execute
wave: 2
depends_on:
  - "05.2-01"
files_modified:
  - backend/app/services/user_printer_access_service.py
  - backend/app/api/v1/users.py
  - backend/app/api/v1/printers.py
  - backend/tests/test_user_printer_access_api.py
autonomous: true
requirements:
  - ACCESS-01
  - ACCESS-02
  - ACCESS-03

must_haves:
  truths:
    - "GET/PUT /api/v1/users/{id}/printer-access funcionam; PUT é replace atômico"
    - "No máximo um is_default=true por usuário após PUT; 422 se dois defaults"
    - "GET /api/v1/printers/{id}/users read-only; sem POST/PATCH neste path"
    - "Falha 404 se user/printer inexistente ou inativo no payload"
    - "P52-AC-01: assign 3 printers + 1 default via API"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-52-02a | Race dois defaults concorrentes | low | Transaction + clear defaults before insert |
| T-52-02b | Escrita dupla UI impressora vs usuário | medium | Apenas PUT em user scope (D-07) |
</threat_model>

<objective>
API de atribuição N:N com validação de default único e vista invertida read-only na impressora.
</objective>

<context>
@.planning/phases/05-2-user-printer-access-policy/05.2-CONTEXT.md (D-05–D-08, D-29)
@backend/app/api/v1/users.py
@backend/app/services/users_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: user_printer_access_service</name>
  <read_first>
    - backend/app/services/users_service.py
    - backend/app/schemas/user_printer_access.py
    - backend/app/db/models.py
  </read_first>
  <action>
    Criar `user_printer_access_service.py`:
    - `list_for_user(db, user_id, include_inactive=False) -> list[PrinterAccessRead]` com join Printer para display_name
    - `replace_for_user(db, user_id, payload: PrinterAccessReplace) -> list[PrinterAccessRead]`:
      * Validar user exists e is_active
      * Validar cada printer_id exists e printer.is_active
      * Rejeitar duplicate printer_id no payload (422)
      * Rejeitar count(is_default=True) > 1 (422)
      * Em transaction: upsert rows (update is_active/is_default se par existe; insert novos; set is_active=false para pares removidos do payload OU delete rows not in payload — preferir soft is_active=false)
      * Ao setar novo default: zerar is_default nos demais do user_id
    - `list_users_for_printer(db, printer_id) -> list[UserRead]` (minimal: id, display_name, cups_username, is_default flag)
  </action>
  <acceptance_criteria>
    - `replace_for_user` com dois is_default=True levanta HTTPException 422
    - `replace_for_user` com lista vazia deixa user sem atribuições ativas
    - pytest `test_replace_assignments_default_unique` passa
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Rotas users printer-access</name>
  <read_first>
    - backend/app/api/v1/users.py
    - backend/app/api/v1/__init__.py
  </read_first>
  <action>
    Em users.py (registrar rotas **antes** de `/{user_id}` se conflito de path — usar path `/printer-access` no router com prefix users já em `/{user_id}`):
    - GET `/{user_id}/printer-access` → list_for_user
    - PUT `/{user_id}/printer-access` → replace_for_user
    OpenAPI tags users.
  </action>
  <acceptance_criteria>
    - `curl -s http://localhost:8000/api/v1/openapi.json` contém `/api/v1/users/{user_id}/printer-access`
    - PUT com 3 printer_ids e 1 default retorna 3 rows, exatamente 1 is_default=true
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: GET printers/{id}/users read-only</name>
  <read_first>
    - backend/app/api/v1/printers.py
  </read_first>
  <action>
    Adicionar GET `/{printer_id}/users` em printers.py chamando `list_users_for_printer`.
    Response model: lista com user id, display_name, cups_username, is_default (bool).
    Sem métodos mutáveis neste endpoint (D-07).
  </action>
  <acceptance_criteria>
    - Após PUT user assignments, GET printer users lista o usuário com is_default correto
    - `pytest tests/test_user_printer_access_api.py -q` exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `pytest backend/tests/test_user_printer_access_api.py -q`
- [ ] Swagger manual: assign 3 printers, toggle default
</verification>
