---
phase: 01-infrastructure-print-server
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - scripts/validate-phase1.sh
  - docs/vm-setup.md
  - docs/phase1-validation.md
autonomous: false
requirements:
  - SERVER-01
  - SERVER-02
  - SERVER-03
user_setup:
  - service: windows-client
    why: "Validação job remoto IPP (D-13 modo 2)"
    dashboard_config:
      - task: "Adicionar impressora IPP no Windows"
        location: "Configurações → Impressoras → Adicionar impressora manual → URL http://VM_HOST:631/printers/<nome>"

must_haves:
  truths:
    - "Job local via lp dentro do container gera linha válida no page_log"
    - "page_log contém nome da impressora e timestamp parseável"
    - "Instruções para validação remota IPP desde PC Windows REDACTED_LAN documentadas"
    - "Suite validate-phase1.sh completa (sem --quick) passa no gate da fase"
  artifacts:
    - path: scripts/validate-phase1.sh
      provides: "Suite completa local + estrutura remota"
      contains: "PAGE_LOG_REGEX"
    - path: docs/phase1-validation.md
      provides: "Procedimento E2E e critérios D-14"
      contains: "DOMINIO\\usuario"
  key_links:
    - from: scripts/validate-phase1.sh
      to: /var/log/cups/page_log
      via: "docker compose exec cups grep PAGE_LOG_REGEX"
      pattern: "page_log"
    - from: docs/phase1-validation.md
      to: CUPS IPP endpoint
      via: "http://VM_HOST:631/printers/"
      pattern: "VM_HOST:631"
---

<objective>
Validar end-to-end a cadeia compose→CUPS→impressora→page_log: job local com lp, regex SPEC §3.2, e procedimento documentado para job remoto IPP (D-13).

Purpose: Objetivo exclusivo da Fase 1 é page_log válido (D-04); critérios D-14 definem aceite de username, impressora e timestamp.
Output: Suite de validação completa, docs E2E, checkpoint humano para job Windows remoto.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-print-server/01-CONTEXT.md
@.planning/phases/01-infrastructure-print-server/01-VALIDATION.md
@.planning/phases/01-infrastructure-print-server/01-RESEARCH.md
@SPEC.md
@scripts/setup-printer.sh
@scripts/validate-phase1.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender validate-phase1.sh com job local e regex page_log</name>
  <read_first>
    @SPEC.md (§3.2 — PAGE_LOG_REGEX)
    @.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-13 modo 1, D-14, D-15)
    @.planning/phases/01-infrastructure-print-server/01-RESEARCH.md (Validar job local, Regex page_log)
    @scripts/validate-phase1.sh
  </read_first>
  <behavior>
    - Após job local: page_log contém nova linha matching PAGE_LOG_REGEX da SPEC
    - Campo printer na linha == TEST_PRINTER_NAME
    - Campo username contém backslash quando job enviado com -U 'DOMINIO\usuario'
    - Timestamp entre colchetes parseável (formato CUPS DD/Mon/YYYY:HH:MM:SS)
  </behavior>
  <action>
    Estender `scripts/validate-phase1.sh`:

    - Definir `PAGE_LOG_REGEX='^(\S+)\s+(\S+)\s+(\d+)\s+\[(.+?)\]\s+total\s+(\d+)\s+(\S+)\s+(\S+)\s+(.+?)\s+(\S+)\s+(\S+)$'` (SPEC §3.2)
    - Modo full (sem `--quick`):
      1. Pré-checks do --quick
      2. Verificar container cups running
      3. Enviar job local (D-13.1): `docker compose exec -T cups bash -c 'echo "PrintWatch phase1 test" | lp -d '"$TEST_PRINTER_NAME"' -U '"'"'DOMINIO\usuario'"'"' -t phase1-local-test'`
      4. `sleep 3`
      5. Capturar última linha: `docker compose exec -T cups tail -1 /var/log/cups/page_log`
      6. Validar regex; extrair e assert printer == TEST_PRINTER_NAME; assert username contém `\` (D-14)
      7. Verificar timestamp não vazio entre `[` e `]`
    - `--quick` permanece rápido (&lt;30s); full target &lt;90s
    - Exit 1 com mensagem clara em qual check falhou

    Se impressora cups-pdf: job completa localmente sem hardware — page_log ainda deve registrar linha.
  </action>
  <acceptance_criteria>
    - `bash scripts/validate-phase1.sh` (full) exit 0 com container + impressora configurados
    - page_log recebe linha após job lp
    - Regex SPEC valida a linha
    - printer field == TEST_PRINTER_NAME
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/validate-phase1.sh</automated>
  </verify>
  <done>Validação automatizada job local + page_log atende SERVER-02 e D-13 modo 1 (D-14 parcial username)</done>
</task>

<task type="auto">
  <name>Task 2: Documentar validação remota IPP e critérios page_log</name>
  <read_first>
    @SPEC.md (§6 — Configuração Windows)
    @.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-13 modo 2, D-14)
    @.planning/phases/01-infrastructure-print-server/01-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <action>
    Criar `docs/phase1-validation.md` em PT-BR:

    **Seção 1 — Job local (automático)**
    - Referência a `bash scripts/validate-phase1.sh`

    **Seção 2 — Job remoto IPP (D-13.2)**
    - Pré-requisito: PC Windows na rede REDACTED_IP/16
    - URL: `http://VM_HOST:631/printers/<TEST_PRINTER_NAME>` (D-17)
    - Protocolo IPP; driver nativo HP/Samsung ou IPP Class Driver (SPEC §6)
    - Passos numerados para adicionar impressora e imprimir página de teste

    **Seção 3 — Verificação page_log pós-job remoto**
    - Comando: `docker compose exec cups grep '<TEST_PRINTER_NAME>' /var/log/cups/page_log | tail -1`
    - Checklist D-14: linha presente; username formato `DOMINIO\usuario` (documentar se Windows enviar formato diferente — risco A1 RESEARCH); nome impressora correto; timestamp recente

    **Seção 4 — ACL (manual opcional)**
    - curl de IP REDACTED_LAN → 200; de fora da faixa → 403 (SERVER-01)

    Atualizar `docs/vm-setup.md` com link para phase1-validation.md no final do checklist.
  </action>
  <acceptance_criteria>
    - docs/phase1-validation.md existe com URL IPP VM_HOST:631
    - Documenta critérios D-14 explicitamente
    - Documenta procedimento Windows IPP
    - vm-setup.md linka phase1-validation.md
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'VM_HOST:631' docs/phase1-validation.md && grep -q 'DOMINIO' docs/phase1-validation.md && grep -q 'phase1-validation.md' docs/vm-setup.md</automated>
  </verify>
  <done>Procedimento E2E documentado para operador e UAT (D-13, D-14)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — validação remota IPP e aceite page_log</name>
  <read_first>
    @docs/phase1-validation.md
    @.planning/phases/01-infrastructure-print-server/01-CONTEXT.md (D-13, D-14)
  </read_first>
  <what-built>
    Cadeia local validada automaticamente; documentação completa para teste remoto IPP desde PC Windows na rede REDACTED_LAN; suite `validate-phase1.sh` full verde para job local.
  </what-built>
  <how-to-verify>
    1. Confirmar `bash scripts/validate-phase1.sh` exit 0 (job local + page_log)
    2. De um PC Windows em REDACTED_IP/16, adicionar impressora IPP: `http://VM_HOST:631/printers/<TEST_PRINTER_NAME>`
    3. Imprimir página de teste logado com conta AD
    4. Na VM: `docker compose exec cups tail -5 /var/log/cups/page_log` — verificar linha com impressora correta e timestamp recente
    5. Verificar campo username: ideal `DOMINIO\usuario` (D-14); se diferente, anotar formato observado para Fase 2
    6. (Opcional) curl :631 de IP fora REDACTED_IP/16 → esperar 403
  </how-to-verify>
  <acceptance_criteria>
    - Job local: validate-phase1.sh full verde
    - Job remoto: linha nova no page_log OU documentação de bloqueio (sem PC Windows disponível — UAT parcial aceito com nota no SUMMARY)
    - Timestamp na linha corresponde ao horário do job (±5 min)
  </acceptance_criteria>
  <resume-signal>Digite "approved" se aceite completo, ou descreva o formato de username observado / bloqueios de rede</resume-signal>
  <verify>
    <automated>bash scripts/validate-phase1.sh --quick</automated>
  </verify>
  <done>Fase 1 gate humano satisfeito — cadeia compose→CUPS→impressora→page_log validada (D-04, D-13, D-14)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PC Windows → CUPS IPP | Job remoto com username do cliente |
| page_log → futuro watcher Fase 2 | Log contém usernames e job names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | page_log content | mitigate | Volume Docker não publicado; acesso só via docker exec; LogFilePerm 0644 |
| T-01-02 | Spoofing | Job remoto IPP | mitigate | ACL REDACTED_IP/16; validação manual confirma origem na LAN |
| T-01-09 | Repudiation | page_log audit | accept | MVP sem assinatura de jobs; suficiente para Fase 1 walking skeleton |
| T-01-10 | Tampering | page_log file | mitigate | Volume persistente; CUPS controla escrita; sem mount rw externo nesta fase |
</threat_model>

<verification>
Gate da fase: `bash scripts/validate-phase1.sh` full verde; checkpoint humano para job remoto; page_log com linha regex-valid após job local.
</verification>

<success_criteria>
- Critério ROADMAP #4: job de teste gera linha page_log no formato esperado
- D-13: validação local automatizada + remota documentada/executada
- D-14: printer name e timestamp verificados; username documentado
- Walking skeleton completo: compose→CUPS→impressora→page_log
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-print-server/01-04-SUMMARY.md` when done
</output>
