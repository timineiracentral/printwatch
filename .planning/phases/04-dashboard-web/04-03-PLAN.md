---
phase: 04-dashboard-web
plan: "03"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - frontend/src/index.css
  - frontend/src/components/ui/Button.tsx
  - frontend/src/components/ui/Input.tsx
  - frontend/src/components/ui/Skeleton.tsx
  - frontend/src/components/ui/ErrorBanner.tsx
  - frontend/src/components/ui/EmptyState.tsx
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/components/layout/Sidebar.tsx
  - frontend/src/components/layout/PageHeader.tsx
  - frontend/src/App.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Usuário vê shell PrintWatch com sidebar Jobs ativa e área principal"
    - "Título da página: Histórico de impressão"
    - "Slot de ações no header reservado para Exportar CSV"
    - "Layout desktop-first com sidebar ~220px"
  artifacts:
    - path: "frontend/src/components/layout/AppShell.tsx"
      provides: "Sidebar + main layout"
      min_lines: 25
    - path: "frontend/src/components/ui/Button.tsx"
      provides: "variants secondary/ghost"
      contains: "secondary"
    - path: "frontend/src/App.tsx"
      provides: "AppShell wrapping content"
      contains: "AppShell"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/layout/AppShell.tsx"
      via: "import AppShell"
      pattern: "AppShell"
---

<objective>
Shell visual e primitivos UI conforme 04-UI-SPEC — fundação para cards, filtros e tabela nos plans 04–06.

Purpose: Estabelece hierarquia visual Apple/PaperCut (D-01..D-09) e layout D-47 antes de componentes de dados.

Output: Browser em :80 mostra sidebar "PrintWatch", nav "Jobs" ativa (tint verde), título "Histórico de impressão", área main com fundo canvas #F5F5F7.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-web/04-UI-SPEC.md
@.planning/phases/04-dashboard-web/04-CONTEXT.md
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Primitivos UI (Button, Input, Skeleton, ErrorBanner, EmptyState)</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-UI-SPEC.md — Button variants, States, Accessibility
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-04, D-07, D-08, D-64
  </read_first>
  <files>
    frontend/src/components/ui/Button.tsx,
    frontend/src/components/ui/Input.tsx,
    frontend/src/components/ui/Skeleton.tsx,
    frontend/src/components/ui/ErrorBanner.tsx,
    frontend/src/components/ui/EmptyState.tsx,
    frontend/src/index.css
  </files>
  <action>
    Implementar ~6 primitivos (D-64) com Tailwind + CSS variables (sem shadcn/MUI):

    `Button`: variants `secondary` (outline — reservado Export D-34), `ghost` (Limpar filtros D-22). **Não** usar variant primary filled salvo CTA futuro (UI-SPEC). Focus ring 2px `--focus-ring` offset 2px (D-08).

    `Input`: border `--border`, radius ~8px, padding confortável; labels associados via props ou wrapper.

    `Skeleton`: pulse 1.5s; `@media (prefers-reduced-motion: reduce)` sem animação (D-07, D-49).

    `ErrorBanner`: fundo discreto, copy "Não foi possível carregar os dados." + botão "Tentar novamente" (D-51, UI-SPEC).

    `EmptyState`: heading + body + optional CTA ghost (D-50).

    Refinar `index.css` tokens se faltarem `--row-hover`, `--accent-tint`, etc. (D-01..D-03).

    Aplicar `font-family` system stack per **D-05**: `system-ui, -apple-system, "Segoe UI", Roboto, sans-serif` no `body` em `index.css` (sem Inter no MVP).

    Proibido: gradientes, sombras pesadas, MUI (D-04, D-38).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Button exporta variant secondary e ghost
    - Skeleton respeita prefers-reduced-motion no CSS
    - Nenhum import de @mui ou @radix-ui/themes
  </acceptance_criteria>
  <done>Primitivos reutilizáveis prontos para FilterBar e tabela.</done>
</task>

<task type="auto">
  <name>Task 2: AppShell + Sidebar + PageHeader + wire App</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-UI-SPEC.md — Layout Contract, Copywriting
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-46, D-47, D-48, D-34
  </read_first>
  <files>
    frontend/src/components/layout/AppShell.tsx,
    frontend/src/components/layout/Sidebar.tsx,
    frontend/src/components/layout/PageHeader.tsx,
    frontend/src/App.tsx
  </files>
  <action>
    `Sidebar` (D-47): largura 220px; logo texto "PrintWatch" 17px semibold; item nav "Jobs" único, estado active com `--accent-tint` bg + `--accent` text (UI-SPEC). lucide-react ícone opcional (D-41).

    `PageHeader`: título "Histórico de impressão" (Display 24px semibold); slot `actions` à direita — placeholder `<Button variant="secondary">Exportar CSV</Button>` disabled ou noop até plan 06 (D-34 visibilidade).

    `AppShell`: grid/flex sidebar + main; main `max-w` ~1400px mx-auto; padding lg 24px; fundo canvas (D-46 desktop-first).

    `App.tsx`: montar AppShell; children placeholder "Conteúdo em construção" removido nos plans 04–05. **Sem** React Router (D-44).

    Mobile &lt;1024px: sidebar colapsável simples (hamburger) — funcional não polish (D-46).
  </action>
  <verify>
    <automated>cd frontend && npm run build && docker compose up -d --build nginx 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - curl -sf http://localhost/ | grep -q "PrintWatch\|root"
    - App.tsx usa AppShell sem BrowserRouter
    - PageHeader mostra título pt-BR correto
  </acceptance_criteria>
  <done>Shell completo visível em produção :80; hierarquia visual estabelecida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static UI components | Sem dados sensíveis hardcoded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | S | UI fingerprinting | accept | Dashboard interno apenas |
| T-04-08 | E | Clickjacking | accept | Rede local MVP |

</threat_model>

<verification>
- Build frontend OK
- Visual smoke: http://localhost mostra shell
</verification>

<success_criteria>
- Layout match UI-SPEC shell diagram
- Copy pt-BR no header
- Acessibilidade focus ring nos interativos
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-web/04-03-SUMMARY.md` when done
</output>
