---
phase: 05-master-data-organization
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - backend/app/core/normalize.py
  - backend/app/services/normalization.py
  - backend/app/services/parser.py
  - backend/scripts/backfill_printer_quotes.py
  - backend/tests/test_normalization.py
autonomous: true
requirements:
  - INV-05
  - DATA-04

must_haves:
  truths:
    - "normalize_printer_name vive em app.core.normalize e é reexportada por services/normalization.py (compat)"
    - "parser, matcher (futuro), import e API usam o mesmo módulo core"
    - "Watcher pode importar app.core.normalize sem importar modelos SQLAlchemy"
    - "Testes existentes test_normalization.py passam sem alteração de comportamento"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-05-02a | Divergência de normalize entre matcher e parser | medium | Único módulo core + testes idempotência |
</threat_model>

<objective>
Centralizar normalização em `app.core.normalize` (D-05) preparando matcher, import e CRUD sem acoplamento ao pacote services.
</objective>

<context>
@.planning/phases/05-master-data-organization/05-CONTEXT.md
@backend/app/services/normalization.py
@backend/tests/test_normalization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar app.core.normalize e migrar implementação</name>
  <read_first>
    - backend/app/services/normalization.py
    - backend/app/core/config.py
  </read_first>
  <action>
    Criar `backend/app/core/normalize.py` com `normalize_printer_name(raw: str | None) -> str | None` — copiar lógica idêntica da implementação atual.
    Em `services/normalization.py`: `from app.core.normalize import normalize_printer_name` e reexportar (deprecation comment opcional).
    Atualizar `parser.py` e `scripts/backfill_printer_quotes.py` para importar de `app.core.normalize` diretamente.
  </action>
  <acceptance_criteria>
    - `backend/app/core/normalize.py` define `normalize_printer_name`
    - `pytest backend/tests/test_normalization.py -q` exit 0
    - `grep -r "from app.services.normalization" backend/app/watcher` retorna vazio
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Adicionar normalize_org_code para dept/CC</name>
  <read_first>
    - .planning/phases/05-master-data-organization/05-CONTEXT.md (D-16)
  </read_first>
  <action>
    Adicionar `normalize_org_code(raw: str | None) -> str | None` em `app/core/normalize.py`: strip, upper, retorna None se vazio.
    Testes: `test_normalize_org_code_uppercases`, `test_normalize_org_code_none`.
  </action>
  <acceptance_criteria>
    - `normalize_org_code("  fin  ") == "FIN"`
    - pytest exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `cd backend && pytest tests/test_normalization.py -q`
</verification>
