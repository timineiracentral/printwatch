---
phase: 05.2-user-printer-access-policy
plan: "05"
type: execute
wave: 3
depends_on:
  - "05.2-02"
  - "05.2-03"
  - "05.2-04"
files_modified:
  - backend/app/api/v1/users.py
  - backend/app/services/ti_export_service.py
  - backend/tests/test_ti_export_api.py
  - frontend/src/pages/settings/TiExportPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/jobs/JobsTable.tsx
  - frontend/src/components/filters/FilterBar.tsx
  - frontend/src/hooks/useUrlFilters.ts
  - frontend/src/components/export/ExportCsvButton.tsx
autonomous: true
requirements:
  - ACCESS-05

must_haves:
  truths:
    - "GET /api/v1/users/{id}/ti-export retorna JSON e CSV com campos D-14"
    - "Página imprimível roteiro TI com window.print()"
    - "JobsTable badge discreto fora da política + tooltip D-19"
    - "FilterBar filtro outside_policy"
    - "D-15–D-16: página imprimível minimal + CSV PT-BR mesmo dataset"
    - "D-18: coluna opcional jobs — badge default; toggle coluna se tempo permitir"
    - "P52-AC-03, P52-AC-04, P52-AC-05 verificáveis manualmente"
---

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-52-05a | IPP URL inválido sem IP | low | ipp_url null + nota no export |
| T-52-05b | CSV injection em display_name | low | Escapar campos CSV padrão |
</threat_model>

<objective>
Export roteiro TI por usuário e indicadores de conformidade soft na auditoria de jobs.
</objective>

<context>
@.planning/phases/05-2-user-printer-access-policy/05.2-CONTEXT.md (D-13–D-19, D-31)
@frontend/src/pages/JobsPage.tsx
@frontend/src/components/jobs/JobsTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ti-export backend</name>
  <read_first>
    - backend/app/services/user_printer_access_service.py
    - backend/app/db/models.py (Printer, User)
    - .planning/phases/05-2-user-printer-access-policy/05.2-RESEARCH.md (ipp_url)
  </read_first>
  <action>
    Criar `ti_export_service.py`:
    - `build_ti_export_rows(db, user_id) -> list[dict]` com campos: display_name, username (cups_username), printer_display_name, cups_queue_name, ipp_url, is_default, department (user.department.name/code), location (printer.location)
    - ipp_url = f"ipp://{printer.ip_address}/printers/{printer.cups_queue_name}" se ip_address else None
    Rota GET `/{user_id}/ti-export` em users.py:
    - default JSON list
    - query `format=csv` → StreamingResponse text/csv UTF-8, headers PT-BR: Nome,Usuario,Fila,URL_IPP,Padrao,Departamento,Local
  </action>
  <acceptance_criteria>
    - `pytest tests/test_ti_export_api.py -q` exit 0
    - CSV response Content-Type contém text/csv
    - User com 2 assignments ativas → 2 linhas JSON
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: TiExportPage imprimível</name>
  <read_first>
    - frontend/src/App.tsx (rotas settings)
    - frontend/src/pages/settings/UsersPage.tsx
  </read_first>
  <action>
    Rota `/settings/users/:userId/ti-export` componente TiExportPage:
    - Fetch JSON ti-export
    - Título "Roteiro de instalação — {display_name}"
    - Tabela simples sem AppShell chrome (@media print hide nav)
    - Botões: "Baixar CSV" (link com format=csv), "Imprimir" onClick window.print()
    Link na UsersPage edit: "Exportar roteiro TI" abre nova aba ou navigate.
  </action>
  <acceptance_criteria>
    - Rota registrada em App.tsx
    - Print CSS oculta sidebar/header (class print:hidden nos elementos de chrome)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Jobs badge + filtro</name>
  <read_first>
    - frontend/src/components/jobs/JobsTable.tsx
    - frontend/src/hooks/useUrlFilters.ts
    - frontend/src/components/filters/FilterBar.tsx
    - frontend/src/types/api.ts (JobOut)
  </read_first>
  <action>
    Adicionar `outside_policy?: boolean` em JobOut type.
    JobsTable: se job.outside_policy, render Badge variant warning texto "Fora da política" title tooltip "Usuário imprimiu em impressora não atribuída".
    useUrlFilters: param `outside_policy` true/false.
    FilterBar: Select ou checkbox "Fora da política" wired ao param.
    ExportCsvButton: sem mudança se backend já inclui coluna (plan 05.2-03).
  </action>
  <acceptance_criteria>
    - URL `?outside_policy=true` filtra tabela
    - Badge visível apenas quando outside_policy true
    - npm run build exit 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] P52-AC-03: export TI com 3 campos obrigatórios + default
- [ ] P52-AC-04: job fora política com badge; impressão não testada no app (manual note)
- [ ] P52-AC-05: user sem assignments sem badge em jobs
- [ ] `pytest backend/tests/test_ti_export_api.py -q`
</verification>
