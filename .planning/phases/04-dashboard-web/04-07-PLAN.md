---
phase: 04-dashboard-web
plan: "07"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - nginx/Dockerfile
  - nginx/default.conf
  - docker-compose.yml
  - .env.example
  - scripts/validate-phase4.sh
autonomous: true
requirements:
  - DASH-01

must_haves:
  truths:
    - "Admin abre http://localhost/ e vê SPA React (não 404 nginx)"
    - "GET http://localhost/api/v1/health retorna 200 via proxy nginx → backend"
    - "Backend sem ports:8000 no host — API só via nginx ou docker exec (D-56 RESOLVED)"
    - "bash scripts/validate-phase4.sh --quick passa checks Wave 0"
  artifacts:
    - path: "nginx/default.conf"
      provides: "SPA try_files + proxy /api/"
      contains: "proxy_pass http://backend:8000/api/"
    - path: "docker-compose.yml"
      provides: "serviço nginx ports 80:80 depends_on backend"
      contains: "nginx:"
    - path: "scripts/validate-phase4.sh"
      provides: "Nyquist Wave 0 — BASE_URL default http://localhost"
      contains: "validate-phase4"
  key_links:
    - from: "nginx/default.conf"
      to: "backend:8000"
      via: "location /api/ proxy_pass"
      pattern: "proxy_pass http://backend:8000/api/"
---

<objective>
Vertical slice 1b: deploy nginx :80 + proxy API + validate-phase4.sh Wave 0.

Purpose: Entrega DASH-01 (acesso browser na rede local). Separa infra Docker do scaffold frontend (plan 04-01) para respeitar orçamento de escopo por plano.

Output: `docker compose up -d --build nginx`; `curl -sf http://localhost/` HTML; `curl -sf http://localhost/api/v1/health` ok; `validate-phase4.sh --quick` verde.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-dashboard-web/04-CONTEXT.md
@.planning/phases/04-dashboard-web/04-RESEARCH.md
@docker-compose.yml
@scripts/validate-phase3.sh
@frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: nginx multi-stage + docker-compose + validate-phase4.sh</name>
  <read_first>
    - .planning/phases/04-dashboard-web/04-RESEARCH.md — Pattern 6 nginx + compose; Open Questions (RESOLVED) #1 backend ports
    - .planning/phases/04-dashboard-web/04-CONTEXT.md — D-52..D-58, D-63, D-67
    - scripts/validate-phase3.sh — estrutura pass/fail/check_http (analog per 04-PATTERNS.md)
    - docker-compose.yml — remover ports do backend se existir 8000:8000 (D-56)
  </read_first>
  <files>
    nginx/Dockerfile,
    nginx/default.conf,
    docker-compose.yml,
    .env.example,
    scripts/validate-phase4.sh
  </files>
  <action>
    Criar `nginx/Dockerfile` multi-stage: node:22-alpine build frontend (`npm ci && npm run build`); nginx:1.27-alpine copia `dist/` + `default.conf` (D-53).

    `nginx/default.conf` per D-54/D-63:
    - `listen 80`; `try_files $uri $uri/ /index.html` em `location /`
    - `location /api/ { proxy_pass http://backend:8000/api/; }` (trailing slashes consistentes)
    - gzip on; cache immutable para `*.js`, `*.css` (D-63)

    `docker-compose.yml`: adicionar serviço `nginx` build context `.` dockerfile `nginx/Dockerfile`, `ports: ["80:80"]`, `depends_on: [backend]`, `restart: unless-stopped` (D-56). **Remover** `ports: 8000:8000` do serviço `backend` se presente — debug via `docker compose exec backend curl http://127.0.0.1:8000/api/v1/health` (RESEARCH Open Questions RESOLVED #1).

    `.env.example`: seção Fase 4:
    - `ALLOWED_ORIGINS=http://localhost:5173,http://localhost,http://VM_HOST` (D-58)
    - `VITE_API_BASE_URL=` (vazio = same-origin — D-40)

    `scripts/validate-phase4.sh`: espelhar validate-phase3; `BASE_URL="${BASE_URL:-http://localhost}"`. Checks: nginx up, GET /, health, jobs shape, stats shape, printers, export headers, `cd frontend && npm test --run`. Flag `--quick`.
  </action>
  <verify>
    <automated>docker compose up -d --build nginx 2>&1; sleep 8; bash scripts/validate-phase4.sh --quick</automated>
  </verify>
  <acceptance_criteria>
    - curl -sf http://localhost/ retorna HTML com elemento root
    - curl -sf http://localhost/api/v1/health contém status ok
    - docker compose config não expõe backend:8000 no host (sem ports 8000 no serviço backend)
    - validate-phase4.sh --quick exit 0
  </acceptance_criteria>
  <done>Cadeia E2E infra DASH-01; validate-phase4 Wave 0 verde.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I | nginx:80 sem auth | accept | MVP rede local |
| T-04-02 | I | API via proxy only | mitigate | Backend sem port bind host |

</threat_model>

<verification>
- `docker compose up -d --build nginx` sem erro
- `bash scripts/validate-phase4.sh --quick` verde
</verification>

<success_criteria>
- Dashboard acessível em http://localhost/ (porta 80)
- API health via same-origin /api/v1/health
</success_criteria>

<output>
Create `.planning/phases/04-dashboard-web/04-07-SUMMARY.md` when done
</output>
